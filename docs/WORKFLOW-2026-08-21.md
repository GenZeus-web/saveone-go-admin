# 🛠️ WORKFLOW & STATE — ที่เดียวจบเรื่องการทำงาน + สถานะโปรเจกต์

> ไฟล์นี้รวม 3 อย่างที่เคยกระจายหลายที่ไว้ด้วยกัน: กฎการทำงาน + สถานะล่าสุด + ประวัติการแก้ (Fix Log) + งานค้าง
> ขึ้นแชทใหม่ อ่านไฟล์นี้ก่อนเสมอ

---

## 🔖 สถานะล่าสุด

**เวอร์ชันปัจจุบัน: v2.11.1** (21 ส.ค. 2569) · ผัง 3D **v0.14.0** (20 ส.ค. 2569 · รอบนี้ไม่แตะ)
⏳ **รอเจ้าของทดสอบ** — v2.11.1 หน้าตาตามดีไซน์ (ยังไม่ deploy · บนเว็บจริงตอนนี้คือ v2.11.0)

> 🔴 **บทเรียนรอบ v2.11.0 — "วัดผ่าน" ไม่เท่ากับ "ตรงดีไซน์"**
> v2.11.0 ตรวจด้วยการวัดค่าจาก DOM แล้วผ่านหมด (ไม่ล้นจอ, drawer เลื่อนถูก, ตาราง scroll ได้)
> แต่เจ้าของเปิดบนมือถือจริงแล้วบอก *"ไม่เหมือนที่อยากได้เลย"* — เพราะสิ่งที่วัดได้คือ**กลไก** ไม่ใช่**หน้าตา**
> เทียบ token สีทีหลังพบว่า **ตรงกับดีไซน์ 0 ตัว** (พื้น `#1a1f2e` vs `#12141c` · ทอง `#f0a500` vs `#e8a33d` · มุม 8px vs 14–18px)
> **ต่อไป: งานที่อ้างอิงดีไซน์ ต้องเทียบ token สี/มุม/ระยะ กับไฟล์ดีไซน์ให้ครบก่อนบอกว่าเสร็จ** และถ้าดูภาพจริงไม่ได้ ต้องบอกให้ชัด อย่าขึ้นตารางติ๊กถูกให้ดูเหมือนตรวจครบ
> ⚠️ **รอบนี้มีไฟล์ใหม่ 2 ตัวที่ต้อง deploy คู่กับ index.html** — `apple-touch-icon.png` + `manifest.webmanifest`
> ลืมอัป = ไอคอนหน้าจอโฮมกลายเป็นภาพหน้าจอเว็บ (บทเรียนเดียวกับ `model-bn/` รอบ v2.10.4)
✅ **Firestore Rules v2.10.0 publish แล้ว** (9 ส.ค. · diff กับ `firestore-rules-v2.10.0.txt` ตรงกันเป๊ะทุกตัวอักษร) — ตัว v2.9.0 ยังอยู่ในประวัติ Console กดย้อนได้
✅ **deploy ครบทั้ง 2 repo แล้ว** — รอบ v2.10.4 (20 ส.ค. 69) อัป 3 อย่าง: `Market3D-SR/index.html` + **โฟลเดอร์ใหม่ `model-bn/`** (2 ไฟล์ 1.28 MB) + `SaveOneGo-v2.10.4/index.html` · **เจ้าของทดสอบบนเว็บจริงผ่านครบแล้ว**
> 📌 **บทเรียนรอบนี้:** กฎเดิม *"ทับเฉพาะ index.html"* **ใช้ไม่ได้เมื่อมีไฟล์โมเดล/asset ใหม่** — ถ้าลืมอัป `model-bn/` หน้า BN จะขึ้นแต่พื้นตาราง โมเดล 404 · ต่อไปถ้ารอบไหนเพิ่มโฟลเดอร์ asset ใหม่ ให้จดไว้ในหัวข้อ deploy ทุกครั้ง

| ตรวจอะไร | ผล |
|---|---|
| เว็บหลัก ป้ายเวอร์ชัน | `v2.10.4` ✅ |
| ผัง 3D ป้ายเวอร์ชัน | `SS · v0.14.0` ✅ |
| **ผัง 3D บางนา `?branch=BN`** — โมเดลขึ้นจริง (ยืนยันว่า `model-bn/` deploy ครบ) | ✅ |
| **ปุ่ม "ผังตลาด 3D · บางนา · พรีวิว" ใน sidebar เว็บหลัก** | ✅ |
| ผัง 3D เหลือ 4 แท็บ (ยุบ ⚙️ ที่ชื่อซ้ำกับ 🛠️) | ✅ |
| กล่อง "ปรับผัง" เห็นแค่แท็บ 🛠️ จัดผัง แท็บเดียว | ✅ |
| ด่านล็อกผังกลาง (ต้องปลดล็อกก่อนกด ☁/🗑/↩) | ✅ |
| manager BG/BN เห็นลิงก์ผัง 3D | ✅ |

### 📌 เหลือทำเองนอกโค้ด (ไม่ด่วน ทำเมื่อไหร่ก็ได้)
1. **ติ๊ก `showRawData` ออก** ให้ manager ที่มีอยู่ก่อนแล้ว (ตอนนี้มี `TestSG@saveone.go` คนเดียว) — คีย์นี้อ่านด้วย `!== false` (fail-open) การเปลี่ยน `PERM_DEFAULT_ON` มีผลกับ **ผู้ใช้ที่สร้างใหม่เท่านั้น**
2. **ใส่ `bmBranches`** ให้ user ที่ต้องการเทียบสาขา — ทำในหน้าจัดการผู้ใช้ได้เลย (แถว "สาขาที่เทียบได้ใน Benchmark") · ไม่ใส่ = Benchmark เห็นแค่สาขาตัวเอง ไม่พัง
3. **ตัดสินใจเรื่อง "ความสูงเสากล้องรอบนอก"** ว่าจะให้ตามไปกับผังกลางไหม (ดูผลตรวจใน Fix Log v2.10.0)

> **📌 กฎที่ได้จากรอบนี้ — Rules กับโค้ดต้องขึ้นให้ถูกลำดับ**
> รอบนี้ publish Rules ก่อนที่ LAY-05 (ด่านล็อกผังกลาง) จะขึ้นเว็บ → มีช่วงที่สิทธิ์เขียนเปิดแล้วแต่ปุ่มยังกดรวดเดียวได้
> **นั่นคือเหตุที่ผังกลาง 6 ส.ค. หายไป** (ไม่ใช่บั๊ก — สิทธิ์เปิดจริง + ปุ่มกดง่ายเกินไป)
> **กฎ: Rules ที่ "เปิดสิทธิ์เขียน" ต้อง publish ทีหลังโค้ดที่คุมการกดเสมอ** · ส่วน Rules ที่ "ปิดสิทธิ์" ให้ publish ก่อนโค้ด

<details><summary>สถานะ v2.9.0 (เวอร์ชันก่อนหน้า) — กดดู</summary>

**เวอร์ชัน: v2.9.0** (6 ส.ค. 2569) · ผัง 3D **v0.12.0** (6 ส.ค. 2569)
✅ **เจ้าของทดสอบผ่านแล้ว** (ทดสอบผ่าน `node _serve-both.js` ในเครื่อง)
✅ **Firestore Rules v2.9.0 publish แล้ว** 6 ส.ค. 20:44 — ตัวเก่า 5:40 PM ยังอยู่ในประวัติ Console กดย้อนได้
✅ **deploy ครบทั้ง 2 repo + ตรวจจากเว็บจริงแล้ว** (6 ส.ค. 21:xx)

| ตรวจอะไร | ผล |
|---|---|
| เว็บหลัก ป้ายเวอร์ชัน | `v2.9.0` ✅ |
| แท็บใหม่ (`tab-compare/forecast/rawdata/heatmap/benchmark`) | ครบ 5 ✅ |
| checkbox สิทธิ์ในกล่องเพิ่มผู้ใช้ | 12 ตัว ✅ (เดิม 6) |
| `.col-rev` ในแท็บวันฝน | 5 จุด ✅ |
| ผัง 3D ป้ายเวอร์ชัน | `SS · v0.12.0` ✅ |
| `centralBox` / `saveCentralLayout` / `useCentralLayout` / `__m3dCentral` | โหลดครบ ✅ |
| **ปุ่ม ☁ บันทึกเป็นผังกลาง (ไม่เคยรันมาก่อน)** | **รันจริงสำเร็จ** — ผังกลางล่าสุด 06 ส.ค. 21:11 โดย `anansit@saveone.go` ✅ |

| **login เป็น manager → ไม่เห็นปุ่ม ☁ บันทึกเป็นผังกลาง** | ผ่าน ✅ |
| **pentest แบบ SEC-06 — manager เขียน `layouts/SS` ได้ `permission-denied`** | ผ่าน ✅ |

> ✅ **ทดสอบครบทุกข้อแล้ว — ไม่มีรายการค้าง** (เจ้าของยืนยัน 6 ส.ค. 69)
> Rules กันได้จริงที่ระดับฐานข้อมูล ไม่ใช่แค่ซ่อนปุ่มฝั่งหน้าจอ

> ### ⚠️ GitHub Pages ช้า/รายงานผิด (6 ส.ค. 69) — อ่านก่อนถ้าอาการกลับมา
> **อาการ:** `build` เขียว แต่ `deploy` วน `deployment_queued` จน timeout 10 นาที → ขึ้นแดง
> **ไม่ใช่ความผิดโค้ด** — เวลาค่อยๆ ยืดขึ้นเรื่อยๆ ก่อนพัง: 49 วิ → 2:42 → 4:51 → 5:47 → 10:49 ❌
> ถ้าเป็นปัญหาไฟล์/setting จะพังทันที ไม่ใช่ค่อยๆ ช้า · ทั้ง 2 repo พังพร้อมกันช่วง 20:00–21:00
> **แดง ≠ ไฟล์ไม่ขึ้น** — ผัง 3D ขึ้นแดงแต่ไฟล์ publish จริง (เช็คแล้วได้ v0.12.0 + `centralBox`)
> **📌 กฎใหม่: อย่าเชื่อสีของ workflow — เช็คป้ายเวอร์ชันบนเว็บจริงแทน**
> ```
> เว็บหลัก → ป้ายข้างชื่อ "เซฟวันโก" ต้องเป็น v2.9.0
> ผัง 3D  → ป้ายหัวต้องเป็น SS · v0.12.0
> ```
> เช็คแบบ bypass cache: เปิด `…/index.html?cb=<เลขมั่ว>` หรือ Ctrl+Shift+R
> **ถ้าอยากให้รายงานถูก** (ยังไม่ได้ทำ): เปลี่ยน Settings → Pages → Source จาก *Deploy from a branch* เป็น *GitHub Actions*
> แล้วเขียน `.github/workflows/deploy.yml` เอง ใส่ `actions/deploy-pages@v4` + `timeout: 1800000` (30 นาที แทน default 10 นาที)
> \+ `concurrency: {group: pages, cancel-in-progress: true}` กันคิวต่อกันจนพังทั้งแถว
> ⚠️ เปลี่ยน Source = เปลี่ยนวิธี deploy ทั้งระบบ **อย่าทำพร้อมงานอื่น** แยกเป็นรอบของตัวเอง

</details>

### สถาปัตยกรรมตอนนี้ (เปลี่ยนไปจากเดิมทั้งหมดใน v2.8.0)

```
[Google Sheet] --(Apps Script trigger ทุก 10 นาที)--> [Firestore: data/{6 เอกสาร}]
                                                                |
                                                    [เว็บอ่านตรงนี้ < 1 วิ]
```

**Apps Script ไม่อยู่ในเส้นทางที่ผู้ใช้ต้องรอแล้ว** — มันค้าง/พังเมื่อไหร่ เว็บก็ยังเปิดเร็วเท่าเดิม
แค่ข้อมูลเก่าลง (ช้าสุด 10 นาที) · ป้ายบนหัวเว็บบอกเวลาที่ข้อมูลถูกเขียนขึ้นเสมอ

### ของที่ต้อง deploy — 2 repo แยกกัน

| โฟลเดอร์ในเครื่อง | repo | หมายเหตุ |
|---|---|---|
| `SaveOneGo-v2.11.0/index.html` | `saveone-go-admin` | ตัวหลัก |
| `SaveOneGo-v2.11.0/apple-touch-icon.png` | `saveone-go-admin` | 🆕 v2.11.0 · 180×180 · ไอคอนหน้าจอโฮม **ลืมอัป = ไอคอนเป็นภาพหน้าจอ** |
| `SaveOneGo-v2.11.0/manifest.webmanifest` | `saveone-go-admin` | 🆕 v2.11.0 · `display:standalone` |
| `Market3D-SR/index.html` | `3D-Market-SR` | **อัปตัวนี้ก่อนเสมอ** (v2.11.0 ไม่ได้แก้ ไม่ต้องอัป) |

> ⚠️ **`logo.png` ไม่มีในเครื่อง** อยู่บน repo อย่างเดียว — อย่าลบไฟล์อื่นทิ้ง ไม่งั้นรูปรถหาย (UI-02)
> ⚠️ **`index.html` ชื่อซ้ำกัน 2 โฟลเดอร์** เช็คก่อนแก้ทุกครั้ง (`firebaseConfig` = เว็บหลัก · `BRANCH`/`THREE` = ผัง 3D)

### โครงไฟล์

```
Web-SG-DB/
├── SaveOneGo-v2.10.4/    index.html + WORKFLOW.md + knowledge 6 ไฟล์ + diagram
│                         + firestore-rules-v2.10.0.txt (ตัวที่ publish อยู่) + v2.9.0.txt (ตัวถอย)
├── Market3D-SR/          index.html + model/ + 7_market3d-logic.md + spec
├── _archive/             ทุกเวอร์ชันเก่า + เอกสาร setup ที่ใช้ครั้งเดียวจบ
└── _serve-both.js        ตัวเปิดเซิร์ฟเวอร์ทดสอบ 2 เว็บพร้อมกัน
```

> ✅ `_serve-both.js` **หาโฟลเดอร์ `SaveOneGo-v…` ที่เลขสูงสุดเอง** — เปลี่ยนชื่อโฟลเดอร์แล้วไม่ต้องแก้ไฟล์นี้

### ตัวถอยกลับที่มีอยู่

- `_archive/SaveOneGo-v2.9.0-pre-perm06/` + `Market3D-SR-v0.12.0-pre-lay03/` = **ก่อนงานรอบ v2.10.0** ✅ (copy ก่อนแก้ครบ) — ตัวนี้คือ **v2.9.0 / v0.12.0 ตัวจริง** ที่เคย deploy
- `_archive/SaveOneGo-v2.10.3/` + `Market3D-SR-v0.13.0/` = **ก่อนงานรอบ v2.10.4** ✅ (เว็บหลัก 263,853 bytes · ผัง 3D 505,389 bytes — ตรวจแล้วตรงกับตัวก่อนแก้เป๊ะทั้งคู่)
- `_archive/Market3D-SR-v0.13.0/` = ผัง 3D ตัวที่เคย deploy (**ไม่มี `model/`** — 45 ไฟล์ 23 MB ไม่เคยถูกแก้ ไม่ต้องเก็บซ้ำ ดู README ในโฟลเดอร์)
- `_archive/SaveOneGo-v2.8.0-pre-perm/` + `Market3D-SR-pre-layouts/` = **ก่อนงานรอบ v2.9.0** (backup ครบ ไม่ผิดกฎเหมือนรอบ v2.8.0)
- `_archive/Market3D-SR-v0.12.0/` = ผัง 3D ตัวที่ deploy อยู่ตอนนี้
- `_archive/SaveOneGo-v2.8.0/` = ตัวที่เคย deploy ก่อน v2.9.0 (ตอนนี้เว็บหลักยังเสิร์ฟตัวนี้อยู่)
- `_archive/SaveOneGo-v2.6.5/` + `-pre-sbadmin/` = ก่อนงานรอบ 6 ส.ค.
- `_archive/SaveOneGo-v2.7.1-pre-arch/` = **ว่าง** (ลืม copy ก่อนแก้ — กู้จาก GitHub ได้ ดู README ในโฟลเดอร์)
- `_archive/Market3D-SR-v0.11.1/` = ผัง 3D ตัวปัจจุบัน · `-pre-sbadmin/` = ก่อนแก้ 6 ส.ค.
- `_archive/v2.8.0-setup-docs/` = คู่มือติดตั้ง ARCH-01 (ใช้ครั้งเดียวจบแล้ว เก็บเผื่อเพิ่มสาขาใหม่)
- **Apps Script โค้ดเดิม (`doPost`) ยังอยู่ครบ ไม่ได้ลบ** — ถอยกลับไปใช้ v2 ได้ทันที

> **📌 ขึ้นแชทใหม่:** เปิดโฟลเดอร์ `SaveOneGo-v2.x.x` ที่เลขสูงสุด (ตอนนี้ = **v2.10.4**) แล้วอ่าน `WORKFLOW.md` ในนั้น
> **⚠️ ก่อนลงมือแก้ทุกครั้ง:** เช็คป้ายเวอร์ชันหัว `index.html` ว่าตรงกับบรรทัดบนนี้ — รอบ v2.6.0 เคยหลุดไปแก้ต่อจาก v2.5.6 เพราะเปิดไฟล์ผิดโฟลเดอร์ (ดู MERGE-01)
> **⚠️ copy เข้า `_archive/` ก่อนเริ่มแก้เสมอ** — รอบ v2.8.0 ลืมทำ ต้องไปพึ่ง GitHub


> **📌 ขึ้นแชทใหม่:** ไฟล์นี้อยู่ในโฟลเดอร์เวอร์ชันล่าสุด (เลขสูงสุด) — เปิดโฟลเดอร์ `SaveOneGo-v2.x.x` ที่ใหม่ที่สุดแล้วอ่าน `WORKFLOW.md` ในนั้น
> **⚠️ ก่อนลงมือแก้ทุกครั้ง:** เช็คป้ายเวอร์ชันหัว `index.html` ว่าตรงกับ "เวอร์ชัน" บรรทัดบนนี้ — รอบ v2.6.0 เคยหลุดไปแก้ต่อจาก v2.5.6 เพราะเปิดไฟล์ผิดโฟลเดอร์ (ดู MERGE-01)

### v2.10.0 แก้อะไรไป
0. **KIND-01** 🔴 ทาชนิดล็อก (ร้านประจำ/โต๊ะ/ที่จอดรถ) แล้วบันทึกผังกลาง/รีเฟรช แล้วหาย — ตัวคืนค่ารับกลับแค่ `'sell'` (บั๊กของเดิม)
0. **LAY-05** 🔒 ผังกลาง: ต้องปลดล็อกก่อนถึงกดบันทึก/ลบ/ย้อนได้ + บอกว่าเวอร์ชันไหนจะหาย + ปุ่มลบที่ย้อนคืนได้
0. **DRAG-01** 🐛 กด Esc ระหว่างลากกรอบค้าง แล้วล็อกกลายเป็นสีชมพูค้าง (บั๊กของเดิม เจอตอนทดสอบรอบนี้)
0. **UI-06b + LAY-03b** 🔴 รอบแก้ตาม feedback เจ้าของ — ยุบแท็บ ⚙️ ที่ชื่อซ้ำความหมายกับ 🛠️ ทิ้ง รวมเครื่องมือแก้ผังไว้ที่เดียว · ปลดตัวซ่อนลิงก์ผัง 3D ให้ BG/BN เข้าได้
1. **BM-04** ⚖️ Benchmark ใช้ได้จริงเมื่อเปิดสิทธิ์ — ถอดด่าน "ต้องมี > 1 สาขา" ออก + field ใหม่ `bmBranches` (สาขาที่เทียบได้ แยกจากสาขาที่ดูแล)
2. **WI-01** 🔮 What If ผูกคู่กับพยากรณ์ที่หน้าตั้งสิทธิ์ — ติ๊ก What If แล้วพยากรณ์ติดตามอัตโนมัติ (มันฝังอยู่ในแท็บพยากรณ์ ไม่ใช่แท็บของตัวเอง)
3. **PERM-06** 🔑 ค่าตั้งต้น manager ใหม่: +วันฝน +What If −ข้อมูลดิบ
4. **LAY-03** 🗺️ ผัง 3D เปิดให้ทุกคนเข้าดู/แก้/บันทึกลงเครื่องได้ทุกสาขา · deploy ผังกลางได้เฉพาะสาขาตัวเอง (เดิม admin เท่านั้น)
5. **UI-06** 🛠️ กล่อง "ปรับผัง" ทำให้ดูเป็นเครื่องมือ ไม่ใช่หมายเหตุ + ย้ายปุ่มล้างค่าปรับผังเข้ามาอยู่ด้วยกัน

### v2.9.0 แก้อะไรไป
1. **PERM-03** 🔑 เพิ่มสิทธิ์ 6 ตัว (เปรียบเทียบเดือน · พยากรณ์ · ข้อมูลดิบ · ปฏิทินล็อก · Benchmark · ผัง 3D) — คุมรายคนได้แล้ว
2. **PERM-04** 🚪 ค้างอยู่ในแท็บที่เพิ่งโดนถอดสิทธิ์ → เด้งกลับหน้าภาพรวม
3. **FD-01** 🌧 แท็บวันฝนเปิดให้ manager ได้ — แยกตัวเลขเงินไปผูก `showRevenue`
4. **LAY-01** ☁️ ผังกลาง 3D บน Firestore — admin กดบันทึกครั้งเดียว ทุกคนเห็นเหมือนกัน ไม่ต้องส่งไฟล์ทางไลน์

### v2.8.0 แก้อะไรไป
1. **ARCH-01** 🏗️ Apps Script ดันข้อมูลขึ้น Firestore เองทุก 10 นาที · เว็บอ่าน Firestore แทนการยิงถาม Apps Script → เปิดเว็บ < 1 วิ ผู้ใช้ไม่ต้องรอ Apps Script อีกเลย

### v2.7.1 แก้อะไรไป
1. **PERM-01** ⚡ ยิงเฉพาะท่อของสาขาที่ผู้ใช้มีสิทธิ์ + cache แยกตามชุดสาขา + ซ่อน Benchmark เมื่อมีสาขาเดียว

### v2.7.0 แก้อะไรไป
1. **USR-01** 👥 หน้าจัดการผู้ใช้ในเว็บ — เพิ่ม/แก้ role/สาขา/สิทธิ์/ลบ ไม่ต้องเข้า Firebase Console (admin เท่านั้น)
2. **UI-05** 🎨 ปรับหน้าตาตามสไตล์ SB Admin 2 — shadow + แถบสีซ้ายบนการ์ด/เมนู (คงสีทองเดิม)

### v2.6.5 แก้อะไรไป
1. **LINK-01** เพิ่มลิงก์ "🗺️ ผังตลาด 3D" ใน sidebar → เปิดแท็บใหม่ไป `/3D-Market-SR/?branch=SS`

### v2.6.4 แก้อะไรไป
1. **UI-04** บล็อกเป้าคั่นกลาง KPI 2 ชุด ดันกราฟตกจอ → ย้ายลงใต้ `statsSection` + พับเก็บได้เหลือแถบเดียว

### v2.6.3 แก้อะไรไป
1. **CACHE-01** เก็บข้อมูลไว้ในเครื่อง → **เปิดเว็บเห็นทันที 0 วิ** ไม่ต้องรอ Apps Script อีก · ดึงของใหม่เบื้องหลัง · ล้มเหลวก็ใช้ของเก่าต่อพร้อมป้ายบอกเวลา

### v2.6.2 แก้อะไรไป
> รวมงานทั้งวันของ 28 ก.ค. ไว้เป็นเวอร์ชันเดียว (เดิมแยกเป็น 2.6.1/2.6.2/2.6.3)

**ทำให้เร็ว** — โหลด **71.7 วิ → 9.3 วิ**

1. **PERF-03** ยิง 6 ท่อพร้อมกันเกินลิมิต Google → `fetchPool` คุมที่ **2 ท่อ** + เรียงสลับสาขา
2. **PERF-04** `FETCH_TIMEOUT` 45→12 วิ · `FETCH_BACKOFF` 2→1 วิ (ท่อค้าง ~48 วิ → ~15 วิ)

**ทำให้วัดได้**

3. **PERF-01** ตัววัดเวลาต่อท่อ + สรุปท้ายรอบใน console
4. **PERF-05** แก้เกณฑ์เตือนให้เทียบกับค่าที่ควรเป็นจริง (ของเดิมเตือนผิดทุกครั้ง)

**ทำให้พังแล้วรู้ตัว**

5. **NET-01** `fetchCSV` ไม่มี timeout → `AbortController` + retry 2 ครั้ง backoff
6. **NET-03** แบนเนอร์แดงเตือนเมื่อสาขาไหนโหลดไม่ครบ (เดิมเงียบใน console)
7. **NET-04** `catch` ไม่ซ่อน splash → error ถูกบัง เห็นเป็นหน้าโหลดค้าง
8. **CHT-01** `getContext` null ตอนข้อมูลว่าง → guard + try/catch
9. **SEC-07** ✅ Apps Script: retry Firebase + ยืด cache 5→50 นาที (deploy ครบ 3 สาขาแล้ว)

> **เคยลองแล้วถอนออก:** NET-02 (ยิงทีละท่อในสาขาเดียวกัน) — เชื่อว่า ST+NON ชนกันคือต้นเหตุ แต่พิสูจน์แล้วว่าไม่ใช่ และทำให้เวลาโหลดคูณสอง

### v2.6.0 ทำอะไรไป
1. **ENH-05** เทียบเป้า vs ยอดจริง + เตือนเมื่อหลุดเป้า (เป้าแยกสาขา×โซน เก็บ localStorage, แบนเนอร์เตือน, กล่องเป้าเดือน+เพซ, รายการ 5 วันล่าสุด)
2. **MERGE-01** 🔧 v2.6.0 เดิมถูกแก้ต่อจาก v2.5.6 ทำให้งาน v2.5.7 (พยากรณ์อิงเดือนปฏิทิน) + v2.5.8 (Benchmark เทรนด์/ป้ายเตือน) หายไป → merge กลับเข้ามาครบแล้ว (คงเลข v2.6.0 ตามเดิม) — ตัวก่อน merge เก็บไว้ที่ `_archive/index-v2.6.0-premerge.html`

### v2.5.8 แก้อะไรไป
1. **BM-01** Benchmark: กราฟ "ล็อกเฉลี่ย/วัน" แท่ง → **กราฟเส้นเทรนด์รายเดือน** + ตัดวันที่ล็อก = 0 ออกก่อนเฉลี่ย
2. **BM-02** ป้ายเตือนอัตโนมัติ 🔴 "ไหลลงไม่หยุด" เมื่อสาขาตกต่อเนื่อง ≥ 3 เดือน
3. **BM-03** ใส่ deadband 3% กันสาขาที่ "เจอพื้นแล้วนิ่ง" (BN) โดนธงแดงผิด

### v2.5.7 แก้อะไรไป
1. **FCST-02** ตัดบล็อก "พยากรณ์เดือนหน้า (ฉาย 30 วันข้างหน้า)" → การ์ดคู่อิงเดือนปฏิทินทั้งคู่
2. **FCST-03** บล็อก "เดือนนี้" แสดงตลอดถ้ามีข้อมูล (เดิมหายตอนเดือนจบ)
3. **FCST-04** เติมการ์ดรายรับฝั่ง "เดือนนี้" ให้สมมาตร (gate ตาม role)

### v2.5.6 แก้อะไรไป
1. **DATE-01** กำหนดเองเลือกวันที่แล้วเลื่อน +1 วัน → parse เป็นเวลา local
2. **CMP-05** เฉลี่ยรายรับ (หน้าเปรียบเทียบ) ไม่มีลูกน้ำ → ใช้ `fmtN()`
3. **HEAT-01** heatmap เรียงแนวตั้ง → flex-wrap แนวนอน, cell คงขนาด
4. **FCST-01** พยากรณ์เอาเดือนไม่จบมาเฉลี่ย + อ่านงง → แยกเดือนไม่จบ + จัด 2 กลุ่ม (📅 เดือนนี้ / 🔮 เดือนหน้า)

---

## 📏 กฎการทำงาน (ต้องทำทุกครั้ง)
- แก้โค้ดใช้ **str_replace เท่านั้น** ห้ามเขียนทับทั้งไฟล์
- **ตรวจก่อนแก้เสมอ** เจอปัญหาอื่นบอกก่อน อย่าแก้พ่วงเงียบๆ
- ทุก bug แก้ครอบคลุม **ทุก role × ทุกสาขา × ทุกโซน**
- ทดสอบ syntax + logic ก่อนส่ง
- ภาษาไทยตลอด ตอบสั้นตรงประเด็น

## ✅ ขั้นตอนจบงานแต่ละรอบ

> **📌 เขียนใหม่ 20 ส.ค. 69** — ของเดิมมี 4 ข้อ จบที่ "อัปเดตไฟล์นี้" ไม่มีข้อเอกสารอื่น / ไม่มีข้อ sync Project / ไม่มีข้อ deploy
> ผลคือรอบ v2.10.4 กลายเป็นนึกออกทีละอย่างแล้วไปกวนเจ้าของเป็นหางว่าว (จบงาน → อ้าวเอกสาร → อ้าว index → อ้าว Firebase → อ้าว Project)
> **ต่อไปเช็คให้ครบทั้ง 7 ข้อก่อนบอกว่าจบ**

**1. แก้ + ทดสอบ** (syntax `node --check` + logic)

**2. bump version** — ต้องครบ 4 จุด ไม่ใช่แค่ป้ายหัว
- [ ] ป้ายหัว `index.html` (เว็บหลัก: ข้างชื่อ "เซฟวันโก" · ผัง 3D: `#verTag`)
- [ ] `APP_VER` ในโค้ด (ผัง 3D — คนละที่กับป้ายหัว **ลืมบ่อย**)
- [ ] บรรทัดสถานะบนสุดของไฟล์นี้
- [ ] Fix Log ด้านล่าง — **งานผัง 3D ให้ซ้อนใต้เวอร์ชันเว็บหลัก** (เหมือน LAY-01 ใต้ v2.9.0 · LAY-03 ใต้ v2.10.0) ไม่ใช่ตั้งเป็นหัวข้อแยก

**3. ถามเจ้าของยืนยันว่าเสร็จจริง**

**4. เอกสารที่เกี่ยวข้อง — เช็คทุกตัว ไม่ใช่แค่ไฟล์นี้**
- [ ] `2_web-logic.md` — แตะโครงเว็บหลัก / ลิงก์ / สิทธิ์ไหม
- [ ] `7_market3d-logic.md` — แตะผัง 3D ไหม (+ bump เลขเวอร์ชันหัวไฟล์ด้วย)
- [ ] `6_project-setup.md` — **เพิ่มไฟล์/โฟลเดอร์ที่ต้อง deploy ไหม** หรือเปลี่ยนวิธี deploy
- [ ] `firestore-rules-*.txt` — เปลี่ยนสิทธิ์/collection ไหม · **ไม่ได้เขียน Firestore เพิ่ม = ไม่ต้องแตะ**
- [ ] `1_business-rules` / `3_apps-script-code` / `4_walkin-system` / `5_revenue-calculator` — แตะกฎธุรกิจ/ราคา/Apps Script ไหม

**5. จัดไฟล์ในเครื่อง**
- [ ] copy โฟลเดอร์เก่าเข้า `_archive/SaveOneGo-vX.X.X/` (ใส่ `index.html` ตัว**ก่อนแก้**)
- [ ] เปลี่ยนชื่อโฟลเดอร์ทำงานเป็นเลขใหม่ (`_serve-both.js` หาเลขสูงสุดเอง ไม่ต้องแก้)

**6. ทำสำเนาสำหรับ Project แล้ว sync ขึ้น Claude**

> **📌 กติกาไฟล์ index — มี 2 ตัวต่อ 1 เว็บ อย่าสับสน**
> | ไฟล์ | ไว้ทำอะไร |
> |---|---|
> | `index.html` | **ตัวจริง** — แก้ตัวนี้ · deploy ตัวนี้ |
> | `index_WB.html` (เว็บหลัก) · `index_MAP3D.html` (ผัง 3D) | **สำเนาสำหรับอัปขึ้น Project** — copy จาก `index.html` ตอนปิดงาน ชื่อตรงกับชื่อ doc ใน Project |
>
> **📌 ทำไมชื่อ `index_MAP3D` ไม่ใช่ `index_3D-SR`** (เปลี่ยนชื่อ 20 ส.ค. 69) — ชื่อเดิมตั้งตอนมีแต่ศรีสมาน (`SR`) แต่ไฟล์นี้**คุมทุกสาขาในตัวเดียว** (SS ผังจริง · BN พรีวิว · BG อนาคต) ชื่อเดิมทำให้เข้าใจผิดว่าต้องมีไฟล์ `3D-BN` แยกอีกตัว ซึ่งไม่มี
> ⚠️ **ชื่อ repo `3D-Market-SR` และ URL `/3D-Market-SR/` ไม่เปลี่ยน** — เปลี่ยนแล้วต้องตามแก้ลิงก์ในเว็บหลัก 2 จุด + `_serve-both.js` + เอกสารทุกไฟล์ และลิงก์เก่าที่เคยส่งกันไว้จะ 404 หมด · **`SR` ในชื่อ repo = ชื่อเดิมตอนมีสาขาเดียว ไม่ได้แปลว่ารองรับแค่ศรีสมาน**
>
> ทำไมต้องมีสำเนา: Project เป็นที่เดียวที่แชทใหม่อ่านได้ทันทีโดยไม่ต้องต่อเครื่อง · ไฟล์ในเครื่องอ่านได้เฉพาะตอนเปิด Claude desktop + ต่อโฟลเดอร์ไว้
>
> 🔴 **สำเนา = ก็อปแล้วเปลี่ยนชื่อเท่านั้น — ห้ามเขียนใหม่ ห้ามสร้างเอง ห้ามย่อ**
> มันต้องเหมือน `index.html` ทุกไบต์ (ขนาดต้องเท่ากันเป๊ะ) · ถ้าไม่เท่า = ก็อปพลาด ทำใหม่
> เหตุผล: มันคือ "ตัวจริงเวอร์ชันล่าสุด" ที่เอาให้ Claude อ่าน — ถ้าเนื้อหาไม่ตรงกับตัวที่ deploy จริง แชทหน้าจะอ่านโค้ดผิดตัวแล้วแก้ผิด

- [ ] `cp index.html index_WB.html` และ `cp Market3D-SR/index.html Market3D-SR/index_MAP3D.html`
- [ ] เช็คว่าขนาดไฟล์เท่ากันทั้งคู่ก่อนอัป
- [ ] อัปขึ้น Project: `WORKFLOW.md` + เอกสารที่แก้ในข้อ 4 + `index_WB.html` + `index_MAP3D.html`

**7. บอกรายการ deploy ให้ครบ** — ไม่ใช่แค่ "อัป index.html"
- [ ] ระบุ **ทุกไฟล์/โฟลเดอร์** ที่ต้องขึ้น repo รวม asset ใหม่ (เช่น `model-bn/` รอบ v2.10.4)
- [ ] ระบุลำดับ: **ผัง 3D ก่อน แล้วค่อยเว็บหลัก** (ไม่งั้นปุ่มพาไปหน้าผังตัวเก่า)
- [ ] ระบุวิธีเช็คหลังขึ้น — **ดูป้ายเวอร์ชันบนเว็บจริง อย่าเชื่อสีของ workflow**

---

## 5. ประวัติการแก้ไข (Fix Log)

### v2.11.1 (21 ส.ค. 2569) — 🎨 RWD-02 หน้าตาตามดีไซน์จริง (ตามหลัง v2.11.0 ที่ทำแค่ layout)

> **ทำไมต้องมีรอบนี้:** v2.11.0 ทำ "ไม่ล้นจอ" สำเร็จ แต่ยังเป็น**สี/มุม/ทรงของแดชบอร์ดเดสก์ท็อปเดิม**
> เจ้าของเปิดบนมือถือจริงแล้วบอกว่าไม่เหมือนดีไซน์เลย · ตรวจแล้ว token สีตรงดีไซน์ **0 ตัว**

**ขอบเขต: เฉพาะ `@media(max-width:768px)` + เฉพาะโหมดมืด — เดสก์ท็อปกับโหมดสว่างไม่กระทบ (ยืนยันด้วยการวัดแล้ว)**
โค้ดแยกเป็นบล็อก `RWD-02` ก้อนเดียวจบ **ลบทั้งบล็อกก็กลับไปเป็น v2.11.0 ได้ทันที**

- **ชุดสีใหม่ตามดีไซน์** — เพราะทุก component ใช้ `var()` อยู่แล้ว จึง override ตัวแปรที่ `body:not(.light-mode)` ที่เดียว เปลี่ยนทั้งหน้า

  | | เดิม (เดสก์ท็อป) | ใหม่ (มือถือ) |
  |---|---|---|
  | พื้นหลัง | `#1a1f2e` | `#12141c` |
  | การ์ด | `#242938` | `#1b1e29` |
  | ทอง | `#f0a500` | `#e8a33d` |
  | เขียว / แดง | `#3dd68c` / `#e05c5c` | `#4fbf82` / `#e2685f` |

- **การ์ด** มุมโค้ง 8px → **16px** · **เอาแถบสีขีดซ้ายออก** (`border-left:3px` เป็นภาษาแดชบอร์ดเดสก์ท็อป ดีไซน์ไม่มี) · เงานุ่มขึ้น · ตัวเลข KPI 22px → 26px
- **เมนูรายงาน 8 อัน ย้ายเข้า drawer** ตามดีไซน์ (`#sbNavHost`) เป็นรายการแนวตั้ง สูง 43px
  > 📌 วิธี: **ย้ายโหนดจริงด้วย `appendChild` ไม่ใช่ copy** → `onclick="setView(...)"` เดิมติดไปด้วยทั้งหมด **ไม่ต้องแตะ JS ของ view เลยสักบรรทัด**
  > `syncNavPlacement()` จำที่อยู่เดิมไว้ครั้งแรก แล้วย้ายกลับเองตอนกลับจอใหญ่ · ตรวจสลับจอไปกลับหลายรอบแล้ว ไม่มีเมนูซ้ำ/หาย (ยังเป็น 1 แถบ 8 ปุ่ม)
- แท็บ/สาขา/โซน ปรับเป็นทรงเม็ดยามุม 10px · active = พื้น `#232735` + เส้นทองจางแบบดีไซน์

**ทดสอบ** — `node --check` ผ่านทั้ง 2 block · วัดที่ 375×812: token สีตรงดีไซน์ **ครบทุกตัว** · การ์ด 16px ไม่มีแถบซ้าย · เมนู 8 อันอยู่ใน drawer
**ที่ 1280×800: เดิมทุกอย่าง** (`#1a1f2e` · ทอง `#f0a500` · การ์ด 8px + แถบซ้าย 3px · เมนูกลับไปเป็นแท็บใน `mainContent`) ✅ · โหมดสว่างบนมือถือก็ไม่โดน ✅

**📐 ได้ handoff spec ระหว่างทาง** — เจ้าของส่ง `Mobile app for hair dashboard.zip` มา ข้างในมี `README.md` ที่เป็นสเปกตัวเลขจริง
แตกไว้ที่ **`Web-SG-DB/_design-handoff/`** (นอกโฟลเดอร์ deploy) · เทียบแล้วแก้เพิ่ม 4 จุดที่เดาผิด:
- text secondary `#c9cbd6` → **`#9599ab`** (ผมหยิบสีจากการ์ดใบหนึ่งในดีไซน์ ไม่ใช่ค่าที่สเปกกำหนด)
- มุมการ์ด 16px ทุกใบ → **14px มาตรฐาน / 18px การ์ด hero**
- ขอบจอซ้ายขวา 12px → **20px** · ระยะห่างการ์ด 10px → **14px**

> 📌 **สเปกเขียนไว้ว่าดีไซน์นี้ตั้งใจให้ทำเป็นแอปมือถือแยก (React Native/Flutter) ไม่ใช่เว็บ responsive**
> รอบนี้เราเอา*ภาษาการออกแบบ* (สี/ทรง/ระยะ/เมนูใน drawer) มาใส่เว็บเดิม ซึ่งได้หน้าตาใกล้เคียง
> แต่ของที่ผูกกับโครงแอปจริง — ลำดับบล็อกหน้าแรก, การ์ดแยกโซนแบบ hero, ปุ่ม back รายหน้า — **ยังไม่ได้ทำ** ถ้าอยากได้ครบต้องคุยกันว่าจะดันเว็บต่อ หรือทำแอปแยกตามสเปก

> ⚠️ **ยังไม่ได้ดูด้วยตา** — ตัวรับภาพหน้าจอใน session นี้ใช้ไม่ได้ ตรวจได้แค่ค่าที่วัดจาก DOM
> **ต้องให้เจ้าของเปิดดูจริงก่อนปิดงาน** · ส่วนที่ยังไม่ได้ทำตามดีไซน์: ลำดับบล็อกในหน้าแรก, โปรไฟล์/สวิตช์โหมดมืด/ออกจากระบบใน drawer (ของเดิมอยู่เมนูมุมขวาบนแล้ว รอเจ้าของตัดสินว่าจะซ้ำไหม)

### v2.11.0 (21 ส.ค. 2569) — 📱 RWD-01 มือถือใช้ได้จริง + PWA-01 เพิ่มลงหน้าจอโฮม

> **CSS/HTML/JS หน้าตาล้วน — ไม่แตะ logic คำนวณรายรับ/ส่วนลด/เป้า, โครงสร้างข้อมูล, permission gate ใดๆ**
> (แนวเดียวกับ v2.10.3 UX/UI polish) · อ้างอิงดีไซน์: Claude Design project `eb851e11…` ไฟล์ `SaveOneGo Mobile.dc.html`

**RWD-01) ของเดิมพังยังไง** — media query `max-width:768px` เดิมสั่ง `.sidebar` เป็นแถวแบบ flex-wrap วางไว้**บนสุดของหน้า**
ผลคือบนจอ 390px ต้องเลื่อนผ่านตัวกรอง (สาขา/โซน/ช่วงเวลา) ~300px ก่อนจะเห็นตัวเลขสักตัว

- **sidebar → drawer** `position:fixed` กว้าง 78% (สูงสุด 310px) เลื่อนออกจากซ้าย ตามดีไซน์
  - เปิดด้วยปุ่มแฮมเบอร์เกอร์ `#mnavBtn` ที่หัวเว็บ (โผล่เฉพาะ ≤768px) · ปิดด้วยแตะฉากหลัง / ปุ่มเดิม / Esc
  - ใช้คลาส `.mopen` **แยกจาก `.collapsed` ของจอใหญ่** เพื่อไม่ให้ 2 โหมดตีกัน
    + เขียน `.sidebar.collapsed` ทับในโหมดมือถือ กันปุ่มพับของจอใหญ่ทำ sidebar ค้างซ่อน
  - ตัวกรอง (สาขา/โซน/ช่วงเวลา) แตะแล้ว drawer **ค้างเปิด** ตั้งใจ เพราะมักตั้งหลายอย่างติดกัน · ปิดเองเฉพาะลิงก์ที่พาออกจากหน้า (ผัง 3D)
- **การ์ด KPI เหลือคอลัมน์เดียว** · กราฟทุกกริด (`charts-grid/grid2/row/row2/topbot/pn-grid`) เหลือ 1 คอลัมน์
- **แท็บรายงาน `.vtabs`** เลื่อนแนวนอนแทนตัดบรรทัด + `position:sticky` ค้างหัวจอตอนเลื่อนอ่าน
- **ตาราง** `.fd-table`/`.dtbl` ใส่ `min-width:560px` ให้เลื่อนแนวนอนในกล่องตัวเองจริงๆ แทนบีบคอลัมน์จนอ่านไม่ออก
- **กันทั้งหน้าเลื่อนซ้ายขวา** `.content{overflow-x:hidden}` + `.content *{max-width:100%}`
  > 📌 จุดที่เกือบพลาด: `max-width:100%` **ไม่**ทำให้ตารางเลิกเลื่อน เพราะ CSS ให้ `min-width` ชนะ `max-width` เสมอ — ตรวจบนเบราว์เซอร์จริงแล้ว ตาราง 704px ยังเลื่อนในกล่อง 325px ได้ปกติ
- **แตะง่ายบนจอสัมผัส** ปุ่ม/แถวรายการสูง ≥44px · ช่องกรอกทุกชนิด `font-size:16px` (ต่ำกว่านี้ Safari ซูมหน้าเองตอนแตะ)
- **หัวเว็บบนมือถือ** ซ่อนของที่ล้น: ปุ่มพับ sidebar (`#sidebarBtn`), ป้ายเวลาอัปเดต, ชื่อผู้ใช้ในปุ่มโปรไฟล์, `brand-sub`

**PWA-01) กด Share > เพิ่มลงหน้าจอโฮม แล้วเปิดมาไม่มีแถบ URL**
- `<meta name="apple-mobile-web-app-capable" content="yes">` = ตัวที่ทำให้ไม่มีแถบ URL
- `apple-mobile-web-app-title` = "เซฟวันโก" (ชื่อใต้ไอคอน) · `status-bar-style` = `black-translucent`
- `viewport` เพิ่ม `viewport-fit=cover` + CSS `env(safe-area-inset-*)` กันเนื้อหาชนจอบาก/แถบล่าง iPhone
  - safe-area ใส่ไว้ใน `@media(display-mode:standalone)` = **มีผลเฉพาะตอนเปิดจากไอคอนหน้าจอโฮม** เปิดใน Safari ปกติหน้าตาไม่เปลี่ยน
- **ไฟล์ใหม่ 2 ตัว** `apple-touch-icon.png` (180×180 · "SG" ทองบนพื้น `#1a1f2e` ตามสีแบรนด์) + `manifest.webmanifest` (`display:standalone`)
  > ทำไมไม่ใช้ `logo.png` เดิม: มันเป็นโลโก้แนวนอน iOS จะบีบ/ใส่ขอบขาวให้ ไอคอนไม่สวย · และ `logo.png` ไม่มีในเครื่อง (อยู่บน repo อย่างเดียว) เช็คสัดส่วนไม่ได้

**ทดสอบแล้ว** — `node --check` ผ่านทั้ง 2 script block (module + script) · เปิดจริงผ่าน `_serve-both.js` ที่ 375×812 (iPhone) และ 1280×800
- drawer ปิด `left:-292.5px` → เปิด `left:0` ✅ · การ์ด KPI 1 คอลัมน์ ✅ · แท็บ sticky + เลื่อนได้ (772 > 353) ✅
- ตารางเลื่อนในกล่อง (704 > 325) แต่**ทั้งหน้าไม่เลื่อนซ้ายขวา** (`scrollWidth 375 = innerWidth 375`) ✅
- **จอใหญ่ 1280px ไม่กระทบเลย** — sidebar กลับเป็น `static` 210px, แฮมเบอร์เกอร์ซ่อน, ปุ่มพับ/ป้ายเวลากลับมา, `.dtbl` min-width กลับเป็น 0 ✅
- console เหลือ 404 เดียวคือ `logo.png` ซึ่งเป็นของเดิม (ไม่มีในเครื่อง อยู่บน repo)

### v2.10.4 (20 ส.ค. 2569) — 🗺️ LAY-04 preview บางนา (ผัง 3D v0.14.0 + ลิงก์เข้าเว็บหลัก)

**ที่มา:** เจ้าของได้ไฟล์โมเดล 3D ขนาดจริงของบางนามา (`site-base-plate.obj/.mtl`) — ตรวจแล้วเป็น**โมเดลสเกลพื้นที่แบบทั่วไป** (ลานจอด `parking_yard` · เส้นช่องจอด `bay_line_0..393` · เส้นถนน · คนสเกลอ้างอิง `scale_figure_*`) **ไม่ใช่ CAD สำรวจจริงของตลาดบางนา** ยังไม่มีเลขแถว/ล็อกสักตัว จึงทำเป็น **preview mode** แทนที่จะรอ CAD จริงต่อไปเรื่อยๆ

**เส้นแบ่งที่ใช้ตัดสินว่าเครื่องมือไหนเปิดได้:** *"เครื่องมือนี้ต้องรู้ว่าล็อกอยู่ตรงไหนไหม"* — ถ้าต้องรู้ = ปิด (รอ CAD) · ถ้าไม่ต้องรู้ = เปิดใช้ได้จริงเลย ตรงกับที่ §6.1 (ของที่วางเอง) และ §9.1 (พื้นที่อีเวนต์อิสระ) ของผัง SS ออกแบบไว้แต่แรกว่าไม่พึ่งพิกัดแถว/ล็อก

| Fix | ปัญหา | แก้อะไร |
|---|---|---|
| **LAY-04-preview** 🗺️ | บางนายังเข้าผัง 3D ไม่ได้เลย ขึ้นข้อความ "สาขานี้ยังไม่มีผัง 3D" ทั้งที่มีไฟล์โมเดลแล้ว | **Market3D-SR/index.html:** เพิ่ม `BRANCH_PREVIEW = ['BN']` + `PREVIEW_MODEL.BN` **แยกเส้นทางออกจาก `BRANCH_READY` ทั้งหมด** — ด่าน `if(!BRANCH_READY.includes(branch))` เดิมเช็คต่อว่าอยู่ใน `BRANCH_PREVIEW` ไหม ถ้าใช่เรียก `bootPreview(branch)` แล้ว `return` ไม่แตะ `stop()` เดิม<br>**โหลดโมเดล:** เพิ่ม `OBJLoader.js` + `MTLLoader.js` + `OrbitControls.js` จาก CDN three.js **r128 ตัวเดียวกับที่ใช้อยู่** (ไม่ขึ้นเวอร์ชันใหม่ กัน API ชนกับ GLTFLoader/SkeletonUtils เดิม)<br>🎯 **กล้องจัดมุมเอง — ไม่ hardcode ระยะ:** `frameObject()` คำนวณ `THREE.Box3` ของโมเดลจริง → ย้ายโมเดลมาไว้กลาง origin → `radius = max(size.x,y,z) × 0.6` แล้วตั้งตำแหน่งกล้อง/`near`/`far`/`controls.target` จากค่านั้น · **นี่คือคำตอบของที่เจ้าของกังวลว่า "พื้นที่ใหญ่มาก จำลองคนแล้วแทบมองไม่เห็น"** — โมเดลจะสเกลเท่าไรก็จัดมุมได้พอดีเสมอ ไม่ต้องมานั่งปรับเลขเอง<br>**เรื่องภาพแตกตอนซูม — ไม่ต้องห่วง:** ตรวจ `.mtl` แล้ว **ไม่มี `map_Kd` (texture) สักตัว** มีแต่ `Kd` สีทึบ 11 วัสดุ = เป็นเรขาคณิตล้วน (18,361 vertices) ซูมเท่าไรก็คมเท่าเดิม ไม่ใช่ภาพ raster ที่จะแตกเป็นพิกเซล |
| **LAY-04-tools** 🧰 | เจ้าของขอ *"เครื่องมือใส่ไว้ให้ครบ แต่ยังใช้ไม่ได้ — เหมือนของที่มีแต่ทำอะไรจริงไม่ได้"* · แต่เครื่องมือส่วนใหญ่ของ SS ต้องใช้พิกัดแถว/ล็อกที่บางนายังไม่มี | **Market3D-SR/index.html:** สร้าง `#previewApp` (grid 288px + 1fr เหมือน `#app` เป๊ะ) พร้อม `#pvSide` ที่ **ใช้ CSS class เดิมของ SS ทุกตัว** — `.brand` `.sub` `.tabs` `.tabpane`/`.tabpane.show` `.grp` `<h2>` `.btn`/`.btn.sm` `.btnrow` ครบ 4 แท็บ<br>**ปิด (มีป้าย 🔒 "รอ CAD" การ์ดจาง กดไม่ได้):** ค้นหาล็อก · เลขจอง · ประเภทแถว · ทาสีล็อก · โซนโต๊ะที่อิงแถว · เปิด/ปิดเลเยอร์ · วัดระยะ/เดินชม/สแนปช็อต · เทียบ A/B · เทมเพลตผัง<br>**เปิดใช้ได้จริงแล้ว:** ✅ **วาดโซนอีเวนต์** (ลากกรอบ — raycast ลงระนาบพื้นล่องหนขนาด 3 เท่าของ bounding box แล้วขึ้นกล่องโปร่งสีฟ้า + ตั้งชื่อ) ✅ **วางของ/ป้ายเอง** (คลิกวางกรวยสีแดง) ✅ **สลับธีมสว่าง/มืด** ✅ **ล้างค่าทั้งหมด** (มี confirm)<br>**เก็บค่า:** `localStorage` คีย์แยกตามสาขา `market3d-BN-preview-events` / `market3d-BN-preview-props` — **ไม่ชนกับของ SS** (`market3d-SS-*`) และไม่ชนกับเว็บหลัก (`saveone_data_v1` / `saveone_targets_v1`) |
| **LAY-04-link** 🔗 | เข้าพรีวิวบางนาได้เฉพาะพิมพ์ `?branch=BN` ต่อท้าย URL เอง — ไม่มีทางเข้าจากเว็บหลัก (ซ้ำรอย LAY-03b ที่เคย "เปิดประตูแล้วแต่ซ่อนลูกบิด") | **เว็บหลัก index.html (2 จุด · ไม่แตะ JS คำนวณเลย):**<br>(1) เพิ่ม `<a class="branch-item" id="link-map3d-bn" href="/3D-Market-SR/?branch=BN">` ต่อจากลิงก์ SS เดิม — ป้ายเขียนชัดว่า **"บางนา · พรีวิว (รอ CAD)"** ไม่ให้เข้าใจผิดว่าเป็นผังจริง · **ไม่ใส่ `data-branch`** ด้วยเหตุผลเดียวกับ LAY-03b (ไม่งั้นโดนลูปซ่อนสาขาซ่อนทิ้ง)<br>(2) PERM-05 เปลี่ยนจากอ้าง `getElementById('link-map3d')` ตัวเดียว → `querySelectorAll('[id^="link-map3d"]')` วนซ่อน/แสดงทุกตัว — **เพิ่มสาขาใหม่ในอนาคตไม่ต้องกลับมาแก้จุดนี้อีก** · ยังคุมด้วย `showMap3D` ตัวเดียวเหมือนเดิม ไม่เพิ่มสิทธิ์ใหม่ |

> 🔴 **บั๊กที่ผมทำเองระหว่างทาง — จดไว้กันพลาดซ้ำ:** เผลอใส่ CSS `html.gate #previewApp{display:none !important}` ไว้ "กันเหตุ" — แต่ **`bootPreview()` ไม่เคยถอด class `gate` ออกจาก `<html>`** (มีแค่ `boot()` ของ SS จริงที่ถอด) พอ CSS ใช้ `!important` มันเลยทับ `style.display='grid'` ที่ JS ตั้ง = **หน้าเว็บว่างเปล่าทั้งหน้า ไม่มีแม้แต่ sidebar**
> **บทเรียน:** อย่าเขียน CSS กันเหตุที่พึ่ง class ซึ่งเส้นทางโค้ดใหม่ไม่ได้เป็นคนจัดการ · แก้แล้วโดยลบบรรทัดนั้นทิ้ง (เหลือ `#previewApp{display:none;...}` ธรรมดาที่ inline style ทับได้ปกติ)
> **ไม่แตะโค้ด/ตัวแปรของ SS เลยสักจุด** — ทุกอย่างเป็นเส้นทางแยก (`BRANCH_PREVIEW` · `bootPreview()` · `#previewApp` · คีย์ localStorage คนละชุด) · `BRANCH_READY = ['SS']` **คงเดิม** ผัง SS ทำงานเหมือนเดิมทุกประการ
> **ทดสอบแล้ว:** `node --check` ผ่านทุกรอบที่แก้ (ทั้งฝั่งผัง 3D และเว็บหลัก 2 บล็อก 33K + 97K) · เปิดจริงผ่าน `node _serve-both.js` ที่ `http://localhost:5600/3D-Market-SR/?branch=BN` — **เจ้าของทดสอบแล้วยืนยันผ่าน (20 ส.ค. 69)**
> **แถม (เจอตอนทดสอบ):** `_serve-both.js` ไม่มี MIME ของ `.obj`/`.mtl` → เติมให้แล้ว · และเพิ่มลิงก์ทดสอบ `?branch=BN` ลงหน้า root ของเซิร์ฟเวอร์ทดสอบ (⚠️ path ใน `_serve-both.js` **แยกพิมพ์เล็ก-ใหญ่** ต้อง `/3D-Market-SR/` ตัวใหญ่เท่านั้น พิมพ์เล็กจะตกไปหน้า "เลือกเปิด")
> 🔴 **ยังไม่ทำ (ไม่ใช่ของรอบนี้):**
> - **พิกัดแถว/ล็อกจริงของบางนาจาก CAD** — LAY-04 ตัวเต็มยังรอเหมือนเดิม preview ไม่ได้แทนที่ข้อนี้
> - **ยังไม่ได้สลับไปใช้ `.glb`** ที่เจ้าของอัปมาทีหลัง (859 KB เทียบกับ `.obj` 1.28 MB) — ถ้าเปลี่ยนจะได้ใช้ `GLTFLoader` ที่โหลดอยู่แล้ว **ตัด `OBJLoader`/`MTLLoader` ออกได้ทั้ง 2 ตัว** ไฟล์เล็กลง โหลดเร็วขึ้น · เป็นงานเล็ก แต่ยังไม่ทำเพราะของเดิมใช้ได้แล้วและไม่อยากแตะตอนปิดงาน
> - **ผัง 3D ของ BG (ประตูกรุงเทพ)** — ยังไม่มีทั้งโมเดลและ CAD
> **✅ backup ครบก่อนแก้รอบนี้:** `_archive/Market3D-SR-v0.13.0/` (ตรวจแล้ว **ไบต์ตรงกับตัวก่อนแก้เป๊ะ 505,389 bytes**) · เว็บหลัก: `SaveOneGo-v2.10.3/index_WB.html` (263,853 bytes = ตัวก่อนแก้)

### v2.10.3 (19 ส.ค. 2569) — 🎨 UX/UI polish รอบที่ 1 (CSS/JS ล้วน ไม่แตะ logic คำนวณ)

**ที่มา:** เจ้าของขอปรับหน้าตาให้สวยขึ้น — ทำ design critique ผ่านเว็บจริง (ภาพรวม/หมายเหตุราคา/ปฏิทินล็อก) ก่อนแก้ พบ 4 จุด แก้ครบทั้ง 4

| Fix | ปัญหา | แก้อะไร |
|---|---|---|
| KPI-COLOR 🎨 | การ์ด KPI บล็อกรายรับ 5 ใบ สีเท่ากันหมด ไม่บอกว่าใบไหนคือ "ตัวเลขสรุป" | **index.html:** เพิ่ม class `.kpi-hero` (border-left หนาขึ้น + กรอบเน้น) + `.kpi-hero-tag` (ป้าย "ตัวเลขสรุป") ใส่เฉพาะการ์ด "รายรับสุทธิรวม (ST+Non)" — ใบอื่นสีเดิมเป๊ะ ไม่แตะ |
| TARGET-BANNER 🎯 | แบนเนอร์เตือนหลุดเป้า (`#targetSummaryText`) ยัดข้อความรวมบรรทัดเดียว ขนาดตัวอักษรเท่ากันหมด อ่านเป็นก้อน | **index.html `renderTargetStatus()`:** แยก `sumHead` (หัวข้อสั้น ตัวหนา เช่น "⚠️ หลุดเป้า 5 วันติด") ออกจาก `sumDetail` (รายละเอียด สีจางกว่า) แล้ว render ด้วย `innerHTML` แทน `textContent` — เกณฑ์สี/เงื่อนไข/ตัวเลขไม่เปลี่ยนแม้แต่ตัวเดียว |
| ENH-07 🗓 | Heatmap ปฏิทินล็อก ไล่สีเทียบกับ **ค่าสูงสุดในชุดข้อมูลเอง** (`maxLock`) — ดูไม่ออกว่าทั้งเดือนต่ำกว่าเป้าหรือเปล่า เพราะวันที่เยอะสุด (ต่อให้ต่ำกว่าเป้า) ก็ได้สีเข้มสุดเสมอ | **index.html `renderHeatmap()`:** เปลี่ยนฐานคำนวณเป็นเทียบ `currentTarget()` ของสาขา+โซนที่ดูอยู่ — ถึง/เกินเป้า = โทนเขียว (ไล่เข้มตามส่วนเกิน) · ต่ำกว่าเป้า = โทนส้ม→แดง (ไล่ตามส่วนที่ขาด, ขาด>40% เริ่มแดง) · **ไม่มีเป้าตั้งไว้ (tgt≤0) → ตกกลับพฤติกรรมเดิม** (เทียบ maxLock) กันจอว่าง · legend อัปเดตข้อความ+สวอตช์ให้ตรงความหมายใหม่ · tooltip โชว์ % เทียบเป้าด้วย |
| PRICE-NOTE 📋 | หน้าหมายเหตุราคา ตาราง+สูตรแน่นเกิน อ่านยาก | **index.html (CSS ล้วน):** เพิ่ม gap ระหว่างคอลัมน์ `.pn-grid` (12→16px) + margin-top แยกจากหัวข้อ, padding ใน `.pn-zone` (13→16px), line-height ของ `.formula-box` (1.9→2.1) + `<code>` แยกบรรทัดชัดขึ้น (`display:inline-block`) — **ไม่แตะสูตรคำนวณ/ตัวเลขในตารางเลย** |

> **ไม่แตะ:** logic คำนวณรายรับ/ส่วนลด/เป้า, โครงสร้างข้อมูล, permission gates ใดๆ ทั้งสิ้น — เป็นรอบ CSS/display-only ล้วน
> **ทดสอบแล้ว:** `node --check` ผ่านทั้ง 4 บล็อกสคริปต์ (module + 3 บล็อก main) · เจ้าของเปิดไฟล์ทดสอบเองแล้วยืนยันผ่าน (19 ส.ค. 69)
> **✅ backup ครบก่อนแก้รอบนี้:** `_archive/SaveOneGo-v2.10.2/` (โฟลเดอร์เต็มก่อนแก้)

### v2.10.2 (11 ส.ค. 2569) — 🌧 แท็บวันฝน: รวมโซน Non/Car + กรองตามกลุ่ม

**ที่มา:** เจ้าของเปิดแท็บวันฝนของบางนา เลือกโซน Car แล้ว "ออนไลน์ล็อก" ขึ้น 0 ทุกแถว + ราคา/ล็อกยังโชว์ราคา ST (100→50) · และไม่ว่าจะเลือก "กลุ่มข้อมูล" อะไร ตัวเลขก็ไม่เปลี่ยน (ค้างที่ online เสมอ)

**ต้นเหตุ:** `renderFreeday()` อ่าน field ฝั่ง ST ตรงๆ (`r.onlineLock` / `r.absentLock` / `r.cancelLock` / ราคา ST) ไม่ได้สลับไปฝั่ง Non เมื่อ `applyZone('non')` zero-out field ST ทิ้ง · และไม่เคยส่งตัวแปร `group` เข้าไปคำนวณเลย (รายได้คำนวณถูกอยู่แล้วเพราะ `revNon()` อ่าน field ถูก จึงเห็นอาการแค่คอลัมน์ล็อก/ราคา ไม่ใช่ตัวเงิน)

| Fix | ปัญหา | แก้อะไร |
|---|---|---|
| FD-02 🌧 | โซน Non/Car: ล็อก/ลา/ไม่มา = 0 + ราคาโชว์ ST | **index.html `renderFreeday()`:** ใช้ `getLock(r,g)` (รวม ST+Non) แทน `r.onlineLock` ตรงๆ · ลา/ไม่มา รวม `nonAbsentLock`/`nonCancelLock` · คอลัมน์ราคา: โซน non → `getNonPrice()` (Car +50 สำหรับ WalkIn/เสริม, SS Non ไม่บวก) |
| FD-03 🔎 | ไม่กรองตาม "กลุ่มข้อมูล" (ค้างที่ online/all) | **index.html:** เพิ่ม param `g` ให้ `calcDiscount(r,g)` (ค่าเริ่มต้น `'all'` — call site อื่นไม่กระทบ) · `renderFreeday()` อ่านตัวแปร `group` ส่งต่อเข้า `getLock/revST/revNon/revSTNormal/revNonNormal/calcDiscount` ทั้งหมด · หัวคอลัมน์ (`#fdColLock`) + label KPI เปลี่ยนชื่อตามกลุ่ม · กลุ่ม WalkIn/เสริม ของ SS โชว์ราคา "(ไม่ลด)" ตามกฎ (SS ไม่ลดวันฝนให้ WalkIn) |

> **ยืนยันด้วย node:** SS · โซน ST · กลุ่มออนไลน์ = **50.0% เป๊ะ** (เสียไป 9,400 / ปกติ 18,800) — ก้อน Online ลด 50% ทั้งตัวเศษ-ตัวส่วน · `node --check` block หลักผ่าน
> **ไม่ใช่บั๊ก (ยืนยันตอนตรวจ):** กลุ่ม "ทั้งหมด" ของ SS จะได้ % < 50 เสมอ เพราะรวม WalkIn/ล็อกเสริมที่ SS ไม่ลดราคา — เป็นกฎธุรกิจ ไม่ใช่คำนวณผิด
> **แตะเฉพาะ `renderFreeday()` + `calcDiscount()` + `<th>` วันฝน 1 จุด** ไม่กระทบแท็บอื่น · ใช้ตัวแปร global `zone`/`group`/`activeBranch` เดิม ครอบทุกสาขาในตัว

### v2.10.0 (9 ส.ค. 2569) — รอบล่าสุด ⚖️ Benchmark ใช้ได้จริง + 🗺️ ผัง 3D เปิดข้ามสาขา

**ที่มา:** เจ้าของทดสอบ v2.9.0 แล้วเจอ 5 เรื่อง — 3 เรื่องที่เว็บหลัก 2 เรื่องที่ผัง 3D
**หัวใจของรอบนี้:** สิ่งที่เคยพันกันเป็นก้อนเดียวถูกแยกออกเป็น 2 แกน 2 ที่ —
`branches` (ดูแล) แยกจาก `bmBranches` (เทียบได้) · `CAN_EDIT` (แก้ผังบนจอ) แยกจาก `CAN_DEPLOY` (บันทึกของกลาง)

| Fix | ปัญหา | แก้อะไร |
|---|---|---|
| BM-04 ⚖️ | ติ๊ก `showBenchmark` ให้ manager สาขาเดียวแล้ว **ยังไม่เห็นแท็บ** — และต่อให้เห็น การ์ดก็ขึ้น "ยังไม่มีข้อมูล" 2 ใน 3 ช่อง เพราะ `myPipeKeys()` (PERM-01) โหลดแต่สาขาตัวเอง แถม Rules ยัง deny เอกสารสาขาอื่นด้วย = เห็นแท็บแต่ใช้ไม่ได้ | **แนวคิด: แยกเป็น 2 แกน** — `branches` = สาขาที่ดูแล (เห็นเต็ม ทุกแท็บ) · `bmBranches` = สาขาที่เทียบได้ (ใช้ในหน้า Benchmark เท่านั้น)<br>**index.html (module):** ถอดด่าน `branches.length > 1` ของ PERM-01/02 ออก เหลือ `perms.showBenchmark` ตัวเดียว · อ่าน field ใหม่ `bmBranches` แบบ **fail-safe** (`Array.isArray(d.bmBranches) ? … : []` — ไม่มี field = ว่าง = พฤติกรรมเดิมเป๊ะ ไม่มีใครโดนเปิดสิทธิ์เงียบๆ ตอน deploy) · admin ได้ครบ 3 สาขาเสมอ · ปุ่มใน `#bmBranchPills` ซ่อนตาม **`branches ∪ bmBranches`** ไม่ใช่ `branches` อย่างเดียว<br>**index.html (main):** เพิ่ม `myBmBranches()` = union · `myPipeKeys()` ใช้ตัวนี้แทน `myBranches()` → ได้ `bgMerged`/`bnMerged` จริง · `brSig()` รวม `bmBranches` ด้วย (`'BN+SS-BG-BN'`) กัน cache ปนกันซ้ำรอย PERM-01 · `renderDataHealth()` กรอง `loadErrors` เหลือเฉพาะ docId ของสาขาที่ **ดูแล** (`_errMine`) — สาขาที่มาจาก `bmBranches` พังไม่ต้องขึ้นแบนเนอร์แดง<br>🔴 **ชื่อชนกัน:** `let bmBranches=new Set(['SS'])` ของ UI เดิม → เปลี่ยนเป็น **`bmSelected`** · ตั้งต้นเป็น **สาขาแรกที่เทียบได้** ไม่ใช่ `'SS'` ตายตัว (`bmEnsureInit()` + `window.initBmPills()` ที่ด่าน auth เรียก) · `syncBmPills()` sync class `.sel` ให้ตรงกับ Set เสมอ (HTML เขียน `sel` ไว้ที่ SS ตายตัว) · `toggleBmBranch()` เช็คสิทธิ์ซ้ำ กันเรียกจาก console<br>**หน้าจัดการผู้ใช้:** แถวใหม่ "สาขาที่เทียบได้ใน Benchmark" (checkbox 3 สาขา) ทั้งกล่องแก้ไข (`edBm_<uid>_<br>`) และกล่องเพิ่มผู้ใช้ (`nuBm<br>`) · admin = ติ๊กครบแล้วล็อก เหมือน perms อื่น<br>**Rules:** `firestore-rules-v2.10.0.txt` — เพิ่ม `myBmBranches()` + `data/{docId}` อ่านผ่านได้ถ้า `docId.split('_')[0] in myBmBranches()`<br>**ตัวเลขเงินไม่ต้องแตะ** — สำรวจแล้วหน้า Benchmark มีจุดแสดงเงิน **จุดเดียว** (`#bmKpiRow` "รายรับรวม") ซึ่ง gate ด้วย `showRevenue` ถูกอยู่แล้ว · ที่เหลือเป็นล็อกเฉลี่ย/วันล้วน |
| WI-01 🔮 | ติ๊ก `showWhatIf` ให้ user แล้ว user ยังหาไม่เจอ | **ต้นเหตุไม่ใช่บั๊กที่ตัว gate** — `#whatif-section` เป็น `.fc-panel` ที่ฝังอยู่ **ข้างในแท็บ 🔮 พยากรณ์** (`#view-forecast`) ไม่ใช่แท็บของตัวเอง · `showForecast` ปิด = เข้าไม่ถึงเลย<br>**แก้ที่ต้นทาง (หน้าจัดการผู้ใช้) ไม่แตะ gate:** เพิ่ม `bindWhatIfPair(wi, fc)` — ติ๊ก What If → พยากรณ์ติดตาม · เอาพยากรณ์ออก → What If หลุดตาม · ใช้ทั้งกล่องแก้ไข (ผูกใหม่ทุกครั้งที่สร้างแถว) และกล่องเพิ่มผู้ใช้ (ผูกครั้งเดียวด้วย flag `nuPairBound` เพราะเป็น HTML คงที่)<br>เปลี่ยน `PERM_LABEL.showWhatIf` เป็น **"What If (อยู่ในแท็บพยากรณ์)"** ทั้งในตารางและกล่องเพิ่มผู้ใช้<br>**normalize อีกชั้นก่อนเขียน Firestore** ทั้ง `saveUser` และ `submitAddUser`: `if(payload.showWhatIf === true) payload.showForecast = true;` — กันข้อมูลเก่าที่ค้างสถานะขัดกันอยู่แล้ว |
| PERM-06 🔑 | ค่าตั้งต้นสิทธิ์ของ manager ที่สร้างใหม่ไม่ตรงกับที่ต้องการ | **index.html:** `PERM_DEFAULT_ON` **เพิ่ม** `showFreeday` + `showWhatIf` · **เอา `showRawData` ออก** (+ แก้ `checked` ใน HTML กล่องเพิ่มผู้ใช้ให้ตรงกัน)<br>**เหตุผล — อย่าแก้กลับ:** *วันฝน* ปลอดภัย ตัวเลขเงินถูกซ่อนด้วย `.col-rev` อยู่แล้ว (FD-01) เห็นแค่จำนวนล็อก/วัน · *ข้อมูลดิบ* อันตรายกว่า ให้ล็อกรายวันแยกออนไลน์/วอล์กอิน/โซนครบทุกวัน ถึงซ่อนคอลัมน์เงินก็เอาไปคูณราคาเองได้รายรับกลับมาเกือบเป๊ะ — **ซ่อนผลลัพธ์แต่ไม่ซ่อนตัวตั้ง = ไม่ได้ซ่อนจริง** · สอดคล้องกับ `showPriceNote` ที่ปิด default อยู่แล้ว |
| LAY-03 🗺️ | ผัง 3D เข้าดูได้เฉพาะสาขาที่ตัวเองดูแล และแก้/บันทึกผังกลางได้เฉพาะ admin — ผู้จัดการสาขาที่รู้ผังจริงที่สุดกลับต้องรอ admin กดให้ | **แยก `ADMIN` ก้อนเดียว (8 จุด) เป็น 2 ตัว:**<br>• `CAN_EDIT` (= ผ่านด่าน auth) → `body.admin` ที่คุม `.adminonly` 4 จุด (แท็บ ⚙️ · `#tuneGrp` · `#paneadmin` · แถวคู่มือทาวเวอร์) · `setTab('admin')` · `pickTower()` · `clearParts()`<br>• `CAN_DEPLOY` (= `branches.includes(branch)`) → `saveCentralLayout()` · `revertCentralLayout()` · ปุ่มใน `renderCentralBox()` · `__m3dCentral.save()`<br>**ด่าน auth (module):** ถอด `if(!branches.includes(branch)) stop(...)` ออกจากการ**เข้าดู** (คง `BRANCH_READY` ไว้ — BG/BN ยังไม่มีผังจริง ขึ้นข้อความเดิม) · เพิ่ม `canDeploy` ใน `window.__m3d`<br>**ป้ายสถานะ:** `#adminTag` ย้ายจาก `body.admin` ไป **`body.candeploy`** + เปลี่ยนข้อความ `แอดมิน` → **`สาขาที่คุณดูแล`** (ไม่งั้น CAN_EDIT = ทุกคน จะได้ป้าย "แอดมิน" ติดหัวกันหมด) · เพิ่ม `#crossTag` แถบบอกตอนดูข้ามสาขา ขึ้นเองด้วย `body:not(.candeploy)`<br>**แท็บ ⚙️** เปลี่ยนชื่อ `แอดมิน` → **`ปรับผัง`** ให้ตรงกับกติกาใหม่<br>**โควตาเวอร์ชัน + เลือกทับ:** `__m3dCentral.save(snapshot, {slot})` — ไม่ส่ง/นอกช่วง = พฤติกรรมเดิม (ดันเข้าหัว ตัวเก่าสุดหลุด) · ส่ง `slot` = ผังกลางตัวปัจจุบันไปทับช่องนั้นแทน เก็บเวอร์ชันที่อยากรักษาไว้ได้ · ยังเป็น `history[]` 3 ช่องในเอกสารเดียว **ไม่มี collection ใหม่** · guard 700 KB คงเดิม<br>`renderCentralBox()` แสดง `savedAt` + `savedBy` ของทุกเวอร์ชัน (ทุกคนเห็นรายการ · เฉพาะ CAN_DEPLOY กดย้อนได้) + `<select id="centralSlot">` โผล่เมื่อสำรองเต็ม 3 ช่อง<br>**Rules:** `layouts/{branch}` — `get: hasProfile()` (ดูข้ามสาขาได้) · `create, update: hasProfile() && branch in myBranches()` · `list`/`delete` ปิดเหมือนเดิม |
| **UI-06b** 🔴 | **(เจ้าของทดสอบแล้วตีกลับ)** การ์ด "ปรับผัง" **โผล่ทุกแท็บ** · และเครื่องมือแก้ผัง**ยังกระจายอยู่ 4 แท็บ** ตามที่บอกไว้แต่แรก — รอบแรกผมสำรวจแล้วสรุปผิดว่า "ไม่มีอะไรกระจาย" | **บั๊กโผล่ทุกแท็บ — ผมทำเอง:** `.tabpane#paneadmin` มี class `.adminonly` และ CSS `body.admin .adminonly{display:revert}` **specificity 0,2,0 สูงกว่า** `.tabpane{display:none}` (0,1,0) · พอ LAY-03 ทำให้ `body.admin` ติดกับทุกคน pane นั้นเลยไม่เคยถูกซ่อน → เห็นทุกแท็บ (v0.12.0 ไม่โผล่เพราะ `body.admin` ติดเฉพาะ admin ตัวจริง)<br>**ต้นเหตุที่ของกระจาย:** มี **2 แท็บที่ชื่อแปลว่าเรื่องเดียวกัน** — 🛠️ *"จัดผัง"* กับ ⚙️ *"ปรับผัง"* ของที่แก้ผังเลยไปนอนคนละที่โดยไม่มีเกณฑ์<br>**แก้:** **ยุบแท็บ ⚙️ ทิ้ง** (ลบทั้งปุ่มและ `#paneadmin`) เหลือ 4 แท็บ · เขียน `PANEL_MAP` ใหม่ทั้งตารางด้วยเกณฑ์เดียว — **"กดแล้วผังเปลี่ยนรูปร่างไหม"** ถ้าเปลี่ยน → 🛠️ จัดผัง ที่เดียวจบ<br>ย้ายเข้า 🛠️: `#tuneGrp` (วางเป็นตัวแรก เจอทันที) · **แถว** (เดิมอยู่ 📊 ยอด/ผัง) · **เทมเพลตผัง** (เดิมอยู่ 🧰 เครื่องมือ) · ชนิดล็อก · โซนอีเวนต์ · โซนโต๊ะนั่งกิน · ของที่วางเอง<br>🔴 **สไลเดอร์ "ความสูงเสากล้องรอบนอก"** (`#sMast`) — รอบแรกสำรวจตกไป เป็นค่าปรับผังแท้ๆ แต่ไปนั่งในกล่อง "องค์ประกอบ" แท็บ 👁️ แสดงผล → ห่อเป็น `#mastRow` แล้วย้ายเข้าเป็นกลุ่ม **"กล้องรอบนอก"** ท้าย `#tunePanel`<br>⚠️ ต้องย้ายใน `buildTunePanel()` **ไม่ใช่** `reorganizePanels()` — ตัวหลังรันก่อน `#tunePanel` จะมีของ จะไปโผล่หัวแผงแทนท้ายแผง · ย้ายเฉพาะ DOM `sMast.oninput` ติดไปกับ element ไม่ต้องผูกใหม่<br>**ผลพลอยได้:** เติม `ผังกลาง` + `โมเดล 3D` ลง `PANEL_MAP.set` → `console.warn('ยังไม่ได้จัดหมวด')` ที่ขึ้นทุกครั้งตั้งแต่ v2.9.0 หายไป · `setTab` รับเคส `uiPref.tab` ค้างค่า `'admin'` จากเวอร์ชันก่อนได้อยู่แล้ว (ตกกลับ `plan`) |
| **LAY-03b** 🔴 | **(เจ้าของทดสอบแล้วตีกลับ)** manager BG/BN **เข้าผัง 3D ไม่ได้** | **บั๊กที่ผมทำค้าง:** LAY-03 เปิดให้ดูข้ามสาขาได้แล้ว แต่**ลืมปลดตัวซ่อนลิงก์ฝั่งเว็บหลัก** — `<a data-branch="SS">` โดนลูป "ซ่อนสาขาที่ไม่มีสิทธิ์" ซ่อนทิ้ง = เปิดประตูแล้วแต่ซ่อนลูกบิด<br>**แก้:** ถอด `data-branch="SS"` ออก ใส่ `id="link-map3d"` แทน · PERM-05 เปลี่ยนจาก selector `a.branch-item[data-branch]` มาอ้าง id ตรงๆ → เหลือ `showMap3D` คุมตัวเดียว ตรงกับกติกา LAY-03<br>**ไม่แตะ `BRANCH_READY = ['SS']`** — `BRANCH = { SS: {} }` ไม่มีพิกัด CAD ของ BG/BN เปิดเข้าไปจะได้ **ผังปลอมที่ประกอบจากค่าตั้งต้นในโค้ด** (เลขล็อก/แถว/ถนนผิดหมด) อันตรายกว่าขึ้นข้อความบอกตรงๆ ว่ายังไม่มี — ดู **LAY-04** ในข้อ 6 |
| **KIND-01** 🔴 | **(เจ้าของเจอตอนทดสอบ · บั๊กของเดิม)** ทาชนิดล็อก (ร้านประจำ/โต๊ะ/ที่จอดรถ) แล้ว**บันทึกผังกลาง ชนิดที่ทาไม่ตามมา** | **ต้นเหตุ:** `KIND_PAINT` ทาได้ **4 ชนิด** (`sell·fixed·table·park`) แต่ตัวคืนค่าฮาร์ดโค้ดว่า `if(o[id] === 'sell')` — เขียนไว้ตั้งแต่ยุคที่ทาได้ชนิดเดียว พอ v0.9.1 เพิ่มเป็น 4 ชนิดไม่มีใครกลับมาแก้<br>**พลาดเหมือนกัน 2 จุด:** `restoreKindMap()` (→ รีเฟรชหน้าก็หาย) · `applySnapshot()` (→ หายตอนบันทึกผังกลาง · ส่งออกไฟล์ · ลิงก์แชร์ · โหลดเทมเพลต)<br>**รากจริงคือรายชื่อชนิดถูก copy ไว้ 2 ที่** → ย้าย `KIND_PAINT` ไปประกาศที่เดียวคู่ `ROW_KINDS` ในบล็อกผัง (ต้องอยู่ก่อน เพราะตัวคืนค่ารันก่อนบล็อกเครื่องมือท้ายไฟล์) แล้วให้ทั้ง 2 จุดใช้ตัวเดียวกัน<br>ตัวคืนค่าไม่เช็ค "ต่างจากชนิดฐานไหม" เพราะ `baseKindOf()` พึ่ง `FIXED_SHOPS` ที่ยังไม่ถูกเติมตอนนั้น — คีย์ที่บังเอิญเท่าชนิดฐานไม่ทำอันตราย (`kindOf()` ได้ผลเดิม) และ `applyKind()` ลบทิ้งให้เองครั้งถัดไป |
| **LAY-05** 🔒 | **(เจ้าของขอ)** เผลอกด ☁ บันทึกเป็นผังกลาง แล้ว**ผังกลางของ 6 ส.ค. หลุดหายถาวร** — `confirm()` ใบเดียวไม่พอ มือกด OK เร็วกว่าตาอ่าน · ช่องสำรองมี 3 กดบันทึกเกิน 3 ครั้งตัวเก่าสุดก็หล่น | **ใช้ท่า arm-then-fire (ปลดล็อกก่อนถึงกดได้):** ปุ่ม ☁ บันทึก · 🗑 ลบ · ↩ ย้อน **disabled ไว้ตลอด** ต้องติ๊ก "ปลดล็อก" ก่อน · ปลดแล้วนับถอยหลัง **20 วิ** แล้วล็อกกลับเอง · ยิงสำเร็จก็ล็อกกลับทันที → ต้องตั้งใจ 2 จังหวะเสมอ<br>**บอกก่อนว่าจะเสียอะไร:** confirm ระบุ **เวอร์ชันสำรองใบที่จะหายไปถาวร** (วันที่ + ใครบันทึก) ของเดิมไม่บอกเลย เจ้าของเลยไม่รู้ตัวว่า 6 ส.ค. หลุด<br>**ปุ่ม 🗑 ลบผังกลาง (ตามที่ขอ):** ทำเป็น `__m3dCentral.clear()` = เขียน `base = null` ทับ **ไม่ใช่ `deleteDoc`** → (1) `history` รอดครบ กดย้อนเอาคืนได้เสมอ ไม่ใช่ทำลายถาวร (2) ตรงกับ Rules ที่ปิด `delete` ไว้อยู่แล้ว **ไม่ต้องแก้ Rules** (3) ฝั่งอ่านเจอ `base` ไม่ใช่ object = ถือว่าไม่มีผังกลาง ตกไปใช้ `BASE_LAYOUT` ในไฟล์ ไม่มีจอว่าง<br>⚠️ **ปุ่มลบเป็นอีกทางที่ทำข้อมูลหายได้** จึงจงใจไม่ทำเป็นการลบจริง และให้อยู่หลังด่านล็อกเดียวกัน |
| **DRAG-01** 🐛 | **(เจ้าของเจอตอนทดสอบ · เป็นบั๊กของเดิม ไม่ได้เกิดจากรอบนี้)** ลากกรอบโซนโต๊ะ/พื้นที่อีเวนต์ค้างไว้ แล้ว**กด Esc ระหว่างที่ยังกดเมาส์ค้าง** → ล็อกขายของในกรอบ**กลายเป็นสีชมพูค้าง** เหมือนโดนเลือกทำโซนอีเวนต์ | **ต้นเหตุ:** handler ของ Esc ปิดแค่**ตัวโหมด** (`state.diningSelect` / `areaDraw` / `selecting`) แต่ไม่ได้ล้าง `rubber` ที่ค้างอยู่ · พอปล่อยเมาส์ `endPointer()` เห็น `rubber` ยังมี แต่ไม่มีโหมดไหนเปิดแล้ว เลยตกไปเข้า **`else rubberApply()`** ซึ่งเป็นเส้นทาง "เลือกล็อกทำโซนอีเวนต์" → ยัดล็อกเข้า `state.selected` แล้ว `paint()` ทาสี `--event` ให้<br>**รากของปัญหาคือ fallback ไร้ยาม** — บรรทัดสุดท้ายเป็น `else rubberApply()` ลอยๆ ที่วิ่งได้แม้ไม่มีโหมดไหนเปิดอยู่<br>**แก้ 2 จุด:** (1) เพิ่ม keydown ตัวหนึ่งข้าง `cancelRubber()` — Esc ตอนมี `rubber` ค้าง = ทิ้งกรอบทันที ผูกไว้ก่อน handler Escape ตัวอื่นทุกตัวเพื่อให้ล้างก่อนใคร (2) ใน `endPointer()` เปลี่ยน fallback เป็นเงื่อนไขระบุชัด `else if(state.selecting \|\| state.kindEdit)` — ไม่เข้าเงื่อนไขไหนเลย = โหมดถูกปิดระหว่างลาก → ทิ้งกรอบเฉยๆ ไม่ทำอะไรต่อ<br>**ตรวจแล้วว่าเป็นของเดิม:** โค้ดก้อนนี้ใน `_archive/Market3D-SR-v0.12.0-pre-lay03/index.html` เหมือนกันเป๊ะ |
| UI-06 🛠️ | เครื่องมือ "ปรับผัง — ลากได้ทุกตัว" หายาก มองเหมือนหมายเหตุ ไม่เหมือนปุ่ม | **สำรวจแล้ว: ตำแหน่งถูกอยู่แล้ว** — `#tuneGrp` ถูกย้ายเข้าแท็บ ⚙️ ตอนเปิดหน้าโดย `PANEL_MAP.admin` ไม่ได้กระจายไปแท็บอื่น · **ปัญหาจริงคือหน้าตา** (เป็น `<h2 class="fold closed">` ตัวเล็กสีจาง)<br>**CSS (ใช้แต่ตัวแปรที่มีอยู่ `--panel2`/`--line`/`--accent-ink` ไม่เพิ่มสีใหม่):** `#tuneGrp` เป็นการ์ดมีกรอบ + แถบสีซ้าย 3px + เงา · หัวข้อมีพื้นหลัง + `cursor:pointer` + ลูกศรสี accent<br>**HTML:** ไอคอน 🛠️ นำหน้า + ป้าย **"35 ตัวปรับ"** (นับจาก `TUNE` อัตโนมัติ) ให้เห็นตั้งแต่ยังพับว่าข้างในมีของเยอะ + บรรทัดอธิบายสั้นๆ<br>**เข้าถึงได้:** `role="button"` + `tabindex="0"` + `aria-expanded` + รับ Enter/Space (h2 ไม่ใช่ปุ่มจริง)<br>**จอเล็ก:** `input[type=range]` สูงขึ้น (20px → 26px ใน `@media (max-width:860px)`) กดง่ายขึ้น<br>**ย้ายของ 1 ตัว:** ปุ่ม **"🧹 ล้างค่าปรับผัง"** เข้ามาอยู่ใน `#tuneGrp` — เดิมเข้าถึงได้แค่ผ่าน "ล้างข้อมูล…" ในกล่อง *ข้อมูล* แท็บ 🧰 เครื่องมือ · **เรียก `run()` ตัวเดียวกันใน `CLEAR_PARTS` ไม่ได้ copy ลอจิกมา** และรายการเดิมยังอยู่ครบ<br>**ไม่แตะ logic สไลเดอร์เลย** — ยังชี้เข้า `LAYOUT`/`DINING` ตรงๆ ค่าเดียวกับการลากในผัง |

> **ผลข้างเคียงที่ตั้งใจ**
> 1. **manager สาขาเดียวเห็นแท็บ Benchmark แล้ว** (v2.7.1 PERM-02 เคยซ่อนไว้) — แต่เห็นแค่สาขาตัวเองจนกว่า admin จะใส่ `bmBranches` ให้
> 2. **manager ที่มี `bmBranches` จะโหลดเอกสารมากขึ้น** (สูงสุด 6 เอกสารเท่า admin) → `saveCache()` ต้องได้ครบทุกท่อถึงจะบันทึก · ถ้าเอกสารของสาขาที่ **เทียบเฉยๆ** อ่านไม่ได้ ป้ายเวลาจะขึ้น "อัปเดตไม่สำเร็จ" แม้ข้อมูลสาขาตัวเองจะครบ (แบนเนอร์แดงไม่ขึ้น — กรองแล้วใน `renderDataHealth`)
> 3. **`brSig()` เปลี่ยนรูปแบบ** (`'BN'` → `'BN+BN'`) → cache เก่าก่อน v2.10.0 ใช้ไม่ได้ ทุกคนโหลดใหม่รอบเดียวหลัง deploy
> 4. **ทุกคนเข้าแท็บ ⚙️ ปรับผัง ในผัง 3D ได้แล้ว** และลากผังได้ — ลากผิดกระทบแค่ `localStorage` ของตัวเอง กด "คืนค่าตั้งต้น" กลับได้ ผังกลางไม่ถูกแตะ
> 5. **ผู้จัดการสาขาบันทึกผังกลางของสาขาตัวเองได้แล้ว** — ไม่ต้องรอ admin (เป็นการ**ผ่อนกฎที่ LAY-02 ตั้งไว้** `create/update: if isAdmin()` โดยตั้งใจ)
> 6. **`window.__m3d.isAdmin` ยังอยู่แต่ไม่มีใครใช้คุมสิทธิ์แล้ว** — เก็บไว้เผื่อโค้ดเก่าอ้างถึง
>
> **ทดสอบแล้ว (จำลอง + เปิดจริงในเครื่อง)**
> - `node --check` ผ่านทุกบล็อก — เว็บหลัก 2 บล็อก (module 33K · main 94K) · ผัง 3D 4 บล็อก (รวม `m3dMain` 295K)
> - id ที่ JS อ้างถึงครบ ไม่มีตัวไหนหาย — เว็บหลัก 120 refs / 181 ids · ผัง 3D 126 refs / 143 ids
> - **จำลอง logic 33 ข้อ** (`sim-bm04.js`): admin · manager BN ไม่มี `bmBranches` (พฤติกรรมเดิมเป๊ะ: 2 ท่อ ปุ่ม BN ตัวเดียว) · manager BN + `bmBranches` ครบ (6 ท่อ ปุ่มครบ 3 ตั้งต้น SS) · showRevenue ปิด → การ์ดขึ้น `—` · ปิด `showBenchmark` → ซ่อนแท็บ · cache 3 ลายเซ็นไม่ชนกัน · `renderDataHealth` ไม่เตือนสาขาที่มาจาก `bmBranches` แต่ยังเตือนสาขาตัวเอง · WI-01 normalize · PERM-06 ค่าตั้งต้น
> - **จำลอง Rules 19 ข้อ** (`sim-rules.js`) + ตรวจตัวไฟล์ Rules ว่าเขียนตรงตามที่ตั้งใจ 8 ข้อ: manager BN อ่าน `layouts/SS` ผ่าน · เขียน `layouts/SS` denied · เขียน `layouts/BN` ผ่าน · admin ที่ `branches=['SS']` เขียน `layouts/BG` denied · delete denied · list denied · `data/` อ่านข้ามสาขาผ่าน `bmBranches` เท่านั้น · `data/` write ปิดทุกกรณี · `users/` ไม่ถูกแตะ
> - **จำลอง `__m3dCentral.save()` 10 ข้อ**: slot 0/2 ทับถูกช่อง · ไม่ส่ง/นอกช่วง = ดันเข้าหัวแบบเดิม · ไม่เกิน 3 ช่องเสมอ · ยังไม่เคยมีผังกลาง = history ว่าง · เช็คสิทธิ์ใน save() เอง
> - **เปิดจริงผ่าน `localhost:5600`** — เว็บหลักป้าย `v2.10.0` · WI-01 คู่ติ๊กทำงาน 2 ทิศ · ปุ่ม Benchmark ซ่อน/ตั้งต้นถูกทั้ง 3 เคส · เรียก `toggleBmBranch('SS')` จาก console ตอนไม่มีสิทธิ์ = ไม่มีผล
> - **ผัง 3D เปิดจริง ฉีดสคริปต์หลักรันจริง 2 เคส** — manager BN เปิดผัง SS: `CAN_EDIT=true` `CAN_DEPLOY=false` แถบ `#crossTag` ขึ้น ป้าย `สาขาที่คุณดูแล` ไม่ขึ้น ไม่มีปุ่ม ☁/ย้อน แต่เห็น `savedBy` · manager SS เปิดผัง SS: ปุ่มครบ + `<select>` เลือกช่อง 4 ตัวเลือก + ส่ง `{slot:1}` ไป `save()` ถูกจริง · `#tuneGrp` อยู่ใน `#paneadmin` แสดงป้าย "35 ตัวปรับ" · โมเดล .glb โหลดครบ 24 ไฟล์ 200 ทุกตัว · ธีมสว่าง/มืด + `@media (max-width:860px)` ไม่พัง
>
> **ผลตรวจ "อะไรตามไปกับผังกลางบ้าง" (ทดสอบจริง: ตั้งค่าทุกเครื่องมือ → บันทึก → ล้าง → ดึงกลับ → เทียบ)**
> ✅ ตามมาครบ 10 ตัว: ยอดจองที่จำลอง · ซ่อน/แสดงแถว · ประเภทแถว · โซนอีเวนต์จากล็อก · กรอบโต๊ะที่ลาก · โซนโต๊ะอัตโนมัติ · พื้นที่อีเวนต์อิสระ · **ชนิดล็อกที่ทาเอง (KIND-01 แก้แล้ว)** · ของที่วางเอง · สไลเดอร์ปรับผัง 35 ตัว
> 🔴 **ไม่ตามมา — ยังไม่ได้แก้ รอเจ้าของตัดสินใจ:**
> - **ความสูงเสากล้องรอบนอก (`#sMast`)** — ไม่มีคีย์ใน `snapshotState()` เลย ไม่ถูกบันทึกที่ไหนทั้งนั้น (localStorage / ไฟล์ / ผังกลาง) รีโหลดกลับเป็น 6.0 ม. เสมอ · **ควรแก้** เพราะเป็นค่าปรับผัง และ UI-06 ย้ายมันเข้าไปอยู่ในแผงปรับผังแล้ว คนจะยิ่งคาดหวังว่ามันเซฟ · แก้ = เพิ่มคีย์ใน `snapshotState` + คืนค่าใน `applyStagedLayout`
> - **องค์ประกอบ (เปิด/ปิดเลเยอร์)** — ไม่ถูกบันทึกเหมือนกัน แต่**น่าจะถูกแล้ว** เป็น "ตอนนี้อยากมองอะไร" ไม่ใช่รูปร่างผัง แนวเดียวกับธีม/แท็บที่ค้าง
>
> ✅ **ปิดแล้ว (9 ส.ค. 69):** Rules v2.10.0 publish จริง (diff ตรงเป๊ะ) · deploy ครบทั้ง 2 repo · **เจ้าของทดสอบผ่านแล้ว**
>
> 🟡 **ยังไม่เคยเจอของจริง — ไม่ใช่ของค้าง แต่ยังไม่มีเคสให้ลอง**
> - **ยังไม่มี user ที่มี field `bmBranches` จริงในฐานข้อมูล** — จะได้ลองก็ต่อเมื่อเจ้าของไปติ๊กให้ใครสักคน
> - **ยังไม่ได้ pentest แบบ SEC-06 กับ Rules ใหม่** — manager BN เขียน `layouts/SS` ควรได้ `permission-denied` (จำลองแล้วผ่าน แต่ยังไม่ได้ยิงของจริง)
> - **ยังไม่ได้ลองปุ่ม 🗑 ลบผังกลาง / ↩ ย้อนกลับ กับ Firestore จริง** — ในเครื่องใช้ `__m3dCentral` ตัวปลอม
>
> **✅ backup ครบก่อนแก้รอบนี้:** `_archive/SaveOneGo-v2.9.0-pre-perm06/` + `_archive/Market3D-SR-v0.12.0-pre-lay03/`
> **⚠️ ที่ต้องรู้เรื่องความปลอดภัย:** BM-04 เปิดให้ manager อ่าน **CSV ดิบรายวัน** ของสาขาอื่นได้ผ่าน F12 ไม่ใช่แค่ค่าเฉลี่ยที่หน้าจอแสดง — ดู **SEC-08** ในข้อ 6 (งานถัดไปที่ควรทำ)
> **หมายเหตุระหว่างทาง (ของเดิม ไม่ได้แก้):** กล่อง "ผังกลาง" ในผัง 3D ไม่มีชื่อใน `PANEL_MAP` → ตกค้างอยู่แท็บ 🧰 เครื่องมือ + ขึ้น `console.warn('[แผงควบคุม] ยังไม่ได้จัดหมวด')` ทุกครั้งที่เปิดหน้า (มีมาตั้งแต่ v2.9.0) · `getElementById` ที่อ้าง `btnLoad`/`btnSnap`/`snapSum` ก็ยังเป็นแบบเดิม

### v2.9.0 (6 ส.ค. 2569) — 🔑 สิทธิ์รายฟีเจอร์ + ☁️ ผังกลาง 3D

**ที่มา:** เจ้าของอยากคุม Benchmark รายคนได้ (เดิมซ่อนแค่ตอนมีสาขาเดียว) · อยากให้ manager ดูแท็บวันฝนได้แต่ไม่เห็นตัวเลขเงิน · และผัง 3D ที่แก้แล้วต้องส่งไฟล์ JSON ทางไลน์ให้คนอื่นนำเข้าเอง

| Fix | ปัญหา | แก้อะไร |
|---|---|---|
| PERM-03 🔑 | มีสิทธิ์แค่ 6 ตัว — แท็บเปรียบเทียบเดือน/พยากรณ์/ข้อมูลดิบ/ปฏิทินล็อก/Benchmark/ผัง 3D ไม่มีสิทธิ์คุมเลย | **index.html:** `PERM_KEYS` 6→**12** ตัว (เพิ่ม `showCompare` `showForecast` `showRawData` `showHeatmap` `showBenchmark` `showMap3D`) + `PERM_LABEL` + `PERM_DEFAULT_ON`<br>• **คีย์ใหม่อ่านด้วย `!== false` ไม่ใช่ `=== true`** — ผู้ใช้เดิมที่ยังไม่มี field นี้ใน Firestore เห็นเหมือนเดิมทุกอย่าง ไม่มีแท็บหายเงียบๆ ตอน deploy (คีย์เดิม 6 ตัวยังใช้ `=== true` ตามเดิม ไม่แตะ)<br>• ใส่ `id` ให้แท็บ compare/forecast/rawdata/heatmap แล้ว gate ใน `onAuthStateChanged`<br>• **Benchmark ต้องผ่าน 2 ด่าน** — `showBenchmark` **และ** `branches.length > 1` (คง PERM-01/PERM-02 ของ v2.7.1 ไว้ครบ)<br>• หน้าจัดการผู้ใช้: ตารางแก้ไขรายแถววน `PERM_KEYS` อยู่แล้ว **ไม่ต้องแตะ** · กล่องเพิ่มผู้ใช้เป็น HTML คงที่ → เพิ่ม checkbox 6 ตัว (`nuShow…`) ติ๊กไว้ default |
| PERM-04 🚪 | ถอดสิทธิ์แล้วผู้ใช้ที่ค้างอยู่ในแท็บนั้นยังเห็นข้อมูลต่อ | **index.html:** หลัง gate เสร็จ ถ้าแท็บที่ `.active` อยู่ถูก `display:none` → `setView('overview')` |
| FD-01 🌧 | manager เปิดแท็บวันฝนไม่ได้เลย ทั้งที่อยากให้ดู เพราะทั้งแท็บมีแต่ตัวเลขเงิน | **index.html:** แยก "เข้าแท็บได้" (`showFreeday`) ออกจาก "เห็นตัวเลขเงิน" (`showRevenue`) — ใส่ class `.col-rev` เดิมให้ KPI เงิน 4 ใบ · กราฟส่วนลดเป็นบาท · คอลัมน์เงิน 4 ช่อง (ทั้ง `<th>` และ `<td>`)<br>• เพิ่ม KPI "ออนไลน์ล็อกรวม" ให้ manager มีตัวเลขดูแทนที่ช่องว่าง<br>• หัวข้อ "สรุปวันฝน — รายได้ที่สูญเสียไป" → **"สรุปวันฝน"** (manager ไม่เห็นส่วนรายได้)<br>• **ใช้ CSS `body.no-rev .col-rev` ของเดิม ไม่เพิ่มกลไกใหม่** |
| LAY-01 ☁️ | ผัง 3D ที่แก้แล้วอยู่ใน `localStorage` ของเครื่องตัวเอง — ให้คนอื่นเห็นต้องส่งออกไฟล์ส่งไลน์ หรือแก้ `BASE_LAYOUT` ในไฟล์แล้ว deploy ใหม่ | **Market3D-SR/index.html:**<br>• **ด่าน auth (module):** เพิ่ม `setDoc` ใน import · อ่าน `layouts/{branch}` **ก่อนเรียก `boot()`** (สคริปต์หลักถูกฉีดใน `boot()` ดังนั้น `restore()` ยังไม่ทำงาน — จังหวะนี้จึงทันพอดี) เก็บลง `window.__m3d.central` · สร้างสะพาน `window.__m3dCentral.get/save`<br>• **ลำดับผังใหม่:** ลิงก์แชร์ `#s=` → localStorage → **ผังกลาง** → BASE ในไฟล์ · เก็บที่มาไว้ใน `RESTORE_SRC` เพื่อโชว์ในกล่องตั้งค่า<br>• **สำรอง 3 เวอร์ชันในเอกสารเดียวกัน** (`history[]`) — จำนวนเอกสารคงที่ 3 ตัว (สาขาละ 1) ไม่โตตามเวลา<br>• guard ขนาด **700 KB** ก่อนเขียน (Firestore จำกัด 1 MB/เอกสาร) — แนวเดียวกับที่ Apps Script เตือนเรื่อง CSV<br>• UI แท็บตั้งค่า กล่อง "ผังกลาง" (`#centralBox`): ทุกคนเห็นสถานะ + ปุ่ม **↩ กลับไปใช้ผังกลาง** · admin เห็นเพิ่ม **☁ บันทึกเป็นผังกลาง** + ปุ่มย้อนกลับรายเวอร์ชัน<br>• **`BASE_LAYOUT` ในไฟล์ไม่ลบ** — Firestore ล่ม/ยังไม่มีผังกลาง = ตกไปใช้ของเดิม ไม่มีจอว่าง |
| LAY-02 🔒 | — | **Rules (ตัวเต็มใน `6_project-setup.md` ข้อ 2.5):** เพิ่ม `match /layouts/{branch}` — `get` เฉพาะสาขาที่มีสิทธิ์ · `create/update` เฉพาะ admin · `list`/`delete` ปิด · **ไม่แตะบล็อก `users/` และ `data/` เลย** |

> **ผลข้างเคียงที่ตั้งใจ:** manager ที่เคยเห็นแท็บวันฝนไม่ได้ ตอนนี้เปิดได้ถ้า admin ติ๊ก `showFreeday` ให้ แต่จะเห็นเฉพาะจำนวนล็อก/วันฝน ไม่เห็นเงิน
> **ยังทำไม่ได้ (ข้อจำกัดของเดิม ไม่ใช่ของรอบนี้):** ผัง 3D รองรับแค่สาขา **SS** (`BRANCH = { SS: {} }` · `BRANCH_READY`) — BG/BN ยังไม่มีผัง ต้องถอดพิกัดจาก CAD ก่อน · ลิงก์ sidebar จึงยังชี้ SS ตามเดิม **ถูกต้องแล้ว** · `showMap3D` ใช้คุมว่าใครเข้าดูผัง SS ได้
> **ทดสอบแล้ว:** `node --check` ผ่านทุกบล็อก (เว็บหลัก 2 บล็อก · ผัง 3D 4 บล็อก รวม `m3dMain` 291K) · id ที่ JS อ้างถึงครบ ไม่มีตัวไหนหาย (ที่ขึ้นเตือนเป็น prefix ของ id ที่ต่อสตริง เช่น `edP_`+uid) · จำลอง logic สิทธิ์ 5 เคส (admin / manager เดิมไม่มี field / ปิด benchmark / 1 สาขา / ปิด map3d) ถูกทุกเคส · จำลอง Rules `layouts/` 8 เคส ผ่านครบ (manager เขียน = denied · ข้ามสาขาอ่าน = denied · delete = denied)
> ✅ **ทดสอบจริงครบแล้ว (6 ส.ค. 69):** Rules publish 20:44 · deploy ครบ 2 repo · ปุ่มบันทึกผังกลางรันจริงสำเร็จ (ผังกลางแรก 21:11 โดย `anansit@saveone.go`) · manager ไม่เห็นปุ่มบันทึก · pentest แบบ SEC-06 ผ่าน — manager เขียน `layouts/SS` ได้ `permission-denied` **ไม่มีรายการค้าง**
> **หมายเหตุที่เจอระหว่างทาง (ของเดิม ไม่ได้แก้):** ผัง 3D มี `getElementById` อ้าง `btnLoad` / `btnSnap` / `snapSum` ที่ไม่มี id ตรงกันใน HTML — มีมาก่อน v2.9.0 แล้ว (ตรวจเทียบกับ `_archive/Market3D-SR-pre-layouts` แล้วเหมือนกัน) ยังไม่แตะ
> **✅ backup ครบก่อนแก้รอบนี้:** `_archive/SaveOneGo-v2.8.0-pre-perm` + `_archive/Market3D-SR-pre-layouts`

### v2.8.0 (6 ส.ค. 2569) — 🏗️ ARCH-01 เปลี่ยนสถาปัตยกรรม เอา Apps Script ออกจากเส้นทางที่ผู้ใช้ต้องรอ

**ที่มา:** หลัง deploy v2.7.1 แอดมินยังโหลดนาน — console วัดได้ **108.7 วิ พลาด 5 จาก 6 ท่อ** (เทียบกับ 9.3 วิ ที่วัดไว้ 28 ก.ค. = แย่ลงเรื่อยๆ) · error เป็น `404 script.googleusercontent.com/macros/echo?user_content_key=…` คือ **request ไปไม่ถึงตัว script ด้วยซ้ำ**

**ทำไมไม่แก้แบบเดิมอีก:** ทุกวิธีที่ผ่านมา (retry / timeout / คุม concurrency / cache / กรองตามสิทธิ์) คือการ**จัดการกับการรอ** ไม่ใช่การเอาการรอออก · บันไดไล่ปัญหาที่จดไว้เองใน v2.6.2 ข้อ 4 เขียนตรงตัวว่า *"request ไปไม่ถึง = dispatch ฝั่ง Google **แก้ที่โค้ดไม่ได้ ต้องเปลี่ยนสถาปัตยกรรม**"* · และ v2.6.3 ก็จดทางออกไว้แล้วว่า *"งานถัดไปที่แก้รากคือย้ายไป Firestore (ให้ Apps Script ทำงานตาม time trigger)"* — รอบนี้คือทำตามนั้น

| Fix | ปัญหา | แก้อะไร |
|---|---|---|
| ARCH-01 🏗️ | Apps Script อยู่ในเส้นทางที่ผู้ใช้ต้องรอ · สุ่มพัง 404/timeout แก้ที่โค้ดไม่ได้ | **Apps Script (ทั้ง 3 สาขา — โค้ดเต็มอยู่ใน `3_apps-script-code.md`):** เพิ่ม `pushToFirestore()` รันตาม **time trigger ทุก 10 นาที** อ่านชีต → เขียน CSV ลง Firestore `data/{BRANCH_SHEET}` ผ่าน REST ด้วย `ScriptApp.getOAuthToken()` (ไม่ต้องมี service account) · มี guard ไม่เขียนทับของดีถ้าอ่านได้ < 2 แถว · เตือนถ้า CSV ใกล้ลิมิต 1 MB ของ Firestore · **`doPost`/`verifyToken` เดิมไม่ลบ** เก็บเป็นทางถอย<br>**index.html:** เพิ่ม `window.fetchBundleFromFirestore()` ในบล็อก module (อ่าน `getDoc` ขนานทุกเอกสารพร้อมกัน — Firestore ไม่มีปัญหา concurrency แบบ Apps Script) · `loadAll()` เลิกเรียก `fetchPool`/`PROXIES` เปลี่ยนเป็นอ่าน Firestore ตาม `myPipeKeys()` · `refreshBranch()` เปลี่ยนเป็นอ่าน Firestore 2 เอกสาร (คง guard "ว่างห้ามทับของเดิม" ไว้) · `applyCsvBundle(csv, quiet)` รับ quiet เข้ามาเพื่อไม่ให้ log ขึ้นว่า "จาก cache" ตอนโหลดสด · **ป้ายเวลาแสดงเวลาที่ Apps Script เขียนขึ้น Firestore ไม่ใช่เวลาที่เพิ่งอ่าน** (ใช้ค่าเก่าสุดของชุด — สาขาไหน trigger พังจะเห็นจากป้ายทันที)<br>**Rules (ตัวเต็มอยู่ใน `6_project-setup.md` ข้อ 2.5):** เพิ่ม `match /data/{docId}` — อ่านได้เฉพาะสาขาที่ตัวเองมีสิทธิ์ (ตัด 2 ตัวหน้าของ docId เทียบกับ `branches`) · `list` ปิด · `write` ปิดทุกกรณี (Apps Script เขียนผ่าน OAuth ของ Google ซึ่งไม่ผ่าน Security Rules อยู่แล้ว) |

> **ผลที่คาดหวัง:** เปิดเว็บ **< 1 วิ** · Apps Script ค้าง = ข้อมูลเก่าลง แต่เว็บยังใช้งานได้ปกติ (เดิม = เปิดเว็บไม่ได้)
> **แลกอะไรไป:** ข้อมูลช้าได้ถึง 10 นาที — ยอมรับได้ ข้อมูลจองไม่ได้เปลี่ยนทุกวินาที (ลด trigger เป็น 5 นาทีได้ โควตาเหลือเฟือ)
> **โควตา Spark (ฟรี):** เขียน 864/วัน จาก 20,000 · อ่าน ~2,600/วัน จาก 50,000 · Apps Script trigger ใช้ ~7 นาที/วัน จาก 90 นาที
> **PERM-01 ของ v2.7.1 ยังทำงานอยู่** — ยังอ่านเฉพาะเอกสารของสาขาที่มีสิทธิ์ และตอนนี้ **Rules บังคับซ้ำที่ระดับฐานข้อมูล** ไม่ใช่แค่ฝั่ง client
> **โค้ดที่ตายแล้วแต่ไม่ลบ:** `fetchCSV` / `fetchPool` / `logLoadTimes` / `PROXIES` / `refreshToken` — เก็บไว้ให้ถอยกลับได้เร็ว ถ้า ARCH-01 อยู่ตัวแล้วค่อยลบรอบหน้า
> **ทดสอบแล้ว:** `node --check` ผ่านทั้ง 2 บล็อก · id ครบ 117 ตัว · ตรวจว่า `loadAll`/`refreshBranch` ไม่มี `PROXIES`/`fetchPool` เหลือแล้ว · จำลอง logic ด้วย node 3 เคส (admin 6 เอกสาร · manager SS 2 เอกสาร · manager BN ที่ BN_CAR หาย → ขึ้นเตือนถูก ไม่บันทึก cache) · จำลอง rules 3 เคส (BN อ่าน SS_ST/BG_CAR = denied · BN_ST = ผ่าน)
> ✅ **ทดสอบจริงแล้ว 6 ส.ค. — ฝั่ง backend ผ่านหมด:** Apps Script v3 รันจริงครบ 3 สาขา เขียน Firestore สำเร็จ 6/6 เอกสาร ไม่มีพลาดเลยสักท่อ · เวลารวมต่อสาขา 4–11 วิ (เทียบกับที่ผู้ใช้เคยรอ 108.7 วิ) · trigger ทุก 10 นาทีตั้งครบ 3 สาขา · Rules v2.8.0 publish แล้ว
> ~~🔴 ยังไม่ได้ทดสอบฝั่งเว็บ — `index.html` v2.8.0 ยังไม่ได้อัปขึ้น GitHub~~
> ✅ **ปิดแล้ว (6 ส.ค. 69):** deploy + ใช้งานจริงผ่าน เจ้าของยืนยัน *"โหลดเร็วมาก"* · ARCH-01 ทำงานครบวงจร
> ⚠️ **ผิดกฎตัวเอง:** ไม่ได้ copy v2.7.1 เข้า `_archive/` ก่อนเริ่มแก้ — กู้จาก GitHub ได้ (deploy ไปแล้ว) วิธีอยู่ใน `_archive/SaveOneGo-v2.7.1-pre-arch/README-อ่านก่อน.md`

### v2.7.1 (6 ส.ค. 2569) — ⚡ PERM-01 โหลดเฉพาะสาขาที่มีสิทธิ์

**อาการ (เจอตอนเจ้าของ login เป็น manager จริงครั้งแรก):** `testsg@saveone.go` สิทธิ์แค่ BN — เปิดเว็บรอนานมาก แล้วขึ้น "ข้อมูลไม่ครบ — โหลดไม่ได้: BG, BN" พร้อมเลข **0 ทั้งหน้า** · console: SS โหลดผ่าน 492/370 แถว แต่ BG/BN พังหมด (404 + timeout 12 วิ × 3 ครั้ง)

**ต้นเหตุ:** `loadAll()` ยิงครบ 6 ท่อทุกคน ไม่สนสิทธิ์ — manager ที่มีสาขาเดียวต้องรอท่อของอีก 2 สาขาที่ตัวเองไม่มีวันได้เห็น · แถม `saveCache()` บังคับต้องได้ครบทั้ง 6 ท่อถึงจะบันทึก → **ท่อสาขาอื่นพังเมื่อไหร่ ก็ไม่มีวันได้ cache เลย = ช้าทุกครั้งที่เปิด ตลอดไป** (ประโยชน์ CACHE-01 หายหมดสำหรับ user กลุ่มนี้) · admin ไม่เจอเพราะมีครบ 3 สาขา ทุกท่อสำเร็จ cache เลยบันทึกได้

| Fix | ปัญหา | แก้อะไร |
|---|---|---|
| PERM-01 ⚡ | ยิง 6 ท่อทุกคน + cache บังคับครบ 6 → manager สาขาเดียวช้าตลอดไปและเห็นเลข 0 | **index.html:** เพิ่ม `myBranches()` / `myPipeKeys()` / `brSig()` อ่านจาก `window.userBranches` (ไม่รู้ค่า = ถือว่าครบ 3 สาขา = พฤติกรรมเดิม)<br>• `loadAll()` filter รายการท่อตามสิทธิ์ — BN-only เหลือ **2 ท่อจาก 6** · คงลำดับสลับสาขาเดิมไว้<br>• สาขาเดียว = 2 ท่อเข้า script ตัวเดียวกัน → บังคับ `limit=1` เหมือน `refreshBranch` (ไม่งั้นซ้ำรอย PERF-03)<br>• `saveCache()` เช็คเฉพาะท่อที่ควรได้ + เก็บ `br` (ลายเซ็นชุดสาขา) ลงไปด้วย<br>• `loadCache()` ทิ้ง cache ที่คนละชุดสาขา — กัน cache ของ admin ไปโผล่ให้ manager และกลับกัน (cache เก่าก่อน v2.7.1 ไม่มี `br` → ทิ้ง โหลดใหม่รอบเดียว)<br>• `renderDataHealth()` เตือนเฉพาะสาขาที่มีสิทธิ์ (เดิมเตือนสาขาที่ไม่ได้โหลดเพราะไม่มีสิทธิ์ด้วย)<br>• `logLoadTimes()` รับจำนวน worker จริงเข้ามา ไม่ fix ที่ `FETCH_CONCURRENCY` — กันเกณฑ์เตือนเพี้ยนซ้ำรอย PERF-05 |
| PERM-02 🔒 | **(เจอระหว่างทาง)** แท็บ Benchmark ไม่ได้ gate — manager ที่มีสาขาเดียวเห็นตัวเลขของสาขาที่ไม่มีสิทธิ์ได้ | **index.html:** ใส่ `id="tab-benchmark"` แล้วซ่อนเมื่อ `branches.length <= 1` · ซ่อนปุ่มเลือกสาขาใน `#bmBranchPills` ที่ผู้ใช้ไม่มีสิทธิ์ด้วย |

> **ทั้ง 2 อย่างเป็นของเดิม ไม่ได้เกิดจาก v2.7.0** — v2.7.0 ไม่ได้แตะ `loadAll`/`fetchCSV`/cache เลย แค่เพิ่งมาเห็นตอนลอง login เป็น manager จริงครั้งแรก
> **ผลข้างเคียงที่ตั้งใจ:** manager สาขาเดียวจะไม่เห็นแท็บ Benchmark อีกต่อไป (เดิมเห็น — ซึ่งเป็นการรั่วข้อมูลข้ามสาขา)
> **ทดสอบแล้ว:** `node --check` ผ่านทั้ง 2 บล็อก · id ครบ 117 ตัว · จำลอง logic ด้วย node 6 เคส (admin 3 สาขา = 6 ท่อ limit 2 · SS-only = 2 ท่อ limit 1 · BN-only = 2 ท่อ limit 1 · 2 สาขา = 4 ท่อ · `undefined` = ครบ 6 เหมือนเดิม · ค่าเพี้ยน = fallback ครบ 6) · เทส cache ข้ามสิทธิ์ 3 เคส (admin→manager ใช้ไม่ได้ ✓ · ของตัวเองใช้ได้ ✓ · cache เก่าไม่มี `br` ทิ้ง ✓) · เทส `saveCache` เคส BN-only ได้ครบ 2 ท่อ → บันทึกได้ (ของเดิม false ตลอด)
> ~~**ยังไม่ได้ทดสอบ:** ต้องดูว่า BN โหลดผ่านไหมเมื่อไม่ต้องแย่งคิวกับท่อสาขาอื่น~~
> ✅ **ปิดแล้ว (6 ส.ค. 69) — คำถามนี้ตกไปเอง:** v2.8.0 (ARCH-01) เลิกยิง Apps Script ตอนผู้ใช้เปิดเว็บทั้งหมด เปลี่ยนเป็นอ่าน Firestore แทน จึงไม่มีการแย่งคิวให้ต้องวัดอีกแล้ว · PERM-01 (กรองท่อตามสิทธิ์) ยังทำงานอยู่ในรูปของ `myPipeKeys()` ที่ใช้เลือกเอกสาร Firestore

### v2.7.0 (6 ส.ค. 2569) — 👥 หน้าจัดการผู้ใช้ + ปรับหน้าตา

**ที่มา:** เพิ่ม/แก้สิทธิ์ผู้ใช้ต้องเข้า Firebase Console ทุกครั้ง (Auth → copy uid → Firestore → เพิ่ม field ทีละตัว) ช้าและพลาดง่าย · อยากได้หน้าทำในเว็บเลย

| Fix | ปัญหา | แก้อะไร |
|---|---|---|
| USR-01 👥 | จัดการผู้ใช้ต้องทำใน Console เท่านั้น | **index.html:** แท็บใหม่ "👥 จัดการผู้ใช้" (`#tab-users` ซ่อนไว้ default → `onAuthStateChanged` เปิดให้เฉพาะ `role==='admin'`)<br>• ตารางรายชื่อ + แก้ไขในแถว (role / สาขา / สิทธิ์ 6 ตัว) → `setDoc(...,{merge:true})`<br>• ปุ่มลบ → `deleteDoc` (ลบเฉพาะสิทธิ์ บัญชี login ยังอยู่ · มี confirm + ลบตัวเองไม่ได้)<br>• กล่องเพิ่มผู้ใช้ → `createUserWithEmailAndPassword` ผ่าน **secondary app instance** แล้ว `signOut` ทิ้ง — ไม่งั้น Firebase จะสลับ session ทำให้ admin หลุดออกทันทีที่สร้างคนใหม่<br>• เลือก role = admin → บังคับติ๊กสิทธิ์ครบทุกช่องแล้วล็อก (ให้ตรงกับ logic ตอน login ที่ admin เห็นทุกอย่างอยู่แล้ว)<br>• ทุกฟังก์ชันเช็ค `isAdmin()` ซ้ำอีกชั้น (6 จุด) · แปลง error code เป็นข้อความไทยที่บอกวิธีแก้ |
| UI-05 🎨 | หน้าตาเรียบไป | **index.html (CSS 10 บรรทัด):** เพิ่ม `box-shadow` ให้การ์ด/panel/sidebar/header + แถบสีซ้าย 3px บน `.kpi` (แยกสีตามชนิด) และบน `.branch-item.active` / `.pill.active` — **คงสีทองเดิม ไม่เอาสีน้ำเงินของ SB Admin 2 มา** |

> ✅ **Firestore Rules ใหม่ publish แล้ว** (6 ส.ค. 69 16:26) — ถูกแทนที่ด้วยรุ่น v2.8.0 ตอน 17:40 · ตัวที่ใช้จริงดู `6_project-setup.md` ข้อ 2.5
> **นี่คือการเปิดรูที่ SEC-06 (v2.5.2) ตั้งใจปิดไว้** (`allow write: if false`) — กฎใหม่เปิดให้เฉพาะ admin เขียน + **ห้ามแก้เอกสารตัวเอง** (`request.auth.uid != userId`) กันถอด role ตัวเองจนไม่มี admin เหลือ และกันบัญชีที่ถูกยึดใช้ยกระดับสิทธิ์ต่อ · `list` เปิดให้ admin เท่านั้น (manager อ่านรายชื่อคนอื่นไม่ได้)
> **ผลข้างเคียงที่ตั้งใจ:** แก้สิทธิ์ของ admin (รวมตัวเอง) ยังต้องทำใน Console — เป็นราคาของกฎกันล็อกตัวเอง
> **ทดสอบแล้ว:** `node --check` ผ่านทั้ง 2 บล็อก (module 21K + main 89.8K) · id ที่ JS อ้างครบ 111 ตัวไม่มีหาย · id ใหม่ 21 ตัวมีจริงครบ · ฟังก์ชันแกนเดิม 10 ตัวยังอยู่ (`loadAll`/`fetchCSV`/`applyAll`/`fetchPool`/`saveCache`/`makeChart` ฯลฯ) · ระบบสิทธิ์เดิมไม่ถูกแตะ · diff แทนที่ของเดิมแค่ 16 บรรทัด ที่เหลือเป็นของเพิ่มล้วน · เปิดผ่าน http:// ได้ 200
> ~~**ยังไม่ได้ทดสอบ:** ใช้งานจริงหลัง login + หลังวาง Rules — โดยเฉพาะ **การสร้างผู้ใช้ใหม่**~~
> ✅ **ปิดแล้ว (6 ส.ค. 69):** หน้าจัดการผู้ใช้ใช้งานจริงแล้ว (มีบัญชี `testsg@saveone.go` ที่ใช้ทดสอบ manager) · v2.9.0 เพิ่มช่องสิทธิ์เป็น 12 ตัวในหน้านี้ ทดสอบซ้ำผ่าน

### v2.6.5 (4 ส.ค. 2569) — 🗺️ ต่อผังตลาด 3D เข้ากับเว็บหลัก

**ที่มา:** ผังตลาด 3D (repo `3D-Market-SR`) ทำเสร็จถึง v0.8.1 แต่ยังเป็นไฟล์ลอย ไม่มีทางเข้าจากเว็บหลัก และสิทธิ์แอดมินในผังเปิดด้วย `?admin=1` ต่อท้าย URL ใครก็ทำได้

**วิธีที่เลือก: แยกไฟล์ + ลิงก์** (ไม่ฝังเป็นแท็บ ไม่ใช้ iframe) — `index.html` หลักไม่ต้องบวมอีก ~400 KB · คนที่มาดูแค่ยอดไม่ต้องโหลด Three.js · ตัวแปร 2 ไฟล์ไม่ชนกัน · แก้ผังไม่กระทบเว็บที่ใช้งานจริง (คนละ repo deploy คนละที่)

| Fix | ปัญหา | แก้อะไร |
|---|---|---|
| LINK-01 🗺️ | ไม่มีทางเข้าผัง 3D จากเว็บหลัก | **index.html (1 จุด · HTML ล้วน ไม่แตะ JS):** เพิ่ม `<a class="branch-item" data-branch="SS" href="/3D-Market-SR/?branch=SS" target="_blank" rel="noopener">` ต่อท้ายรายการสาขาใน sidebar<br>• `data-branch="SS"` ทำให้ลูปซ่อนสาขาใน `onAuthStateChanged` (บรรทัด 583) **ซ่อนปุ่มนี้ให้เอง**ถ้าผู้ใช้ไม่มีสิทธิ์ SS — ไม่ต้องเขียนเช็คสิทธิ์ซ้ำ<br>• วางไว้**หลัง**สาขาทั้ง 3 เพราะบรรทัด 591 ใช้ `querySelector('.branch-item[data-branch="…"]')` เอาตัวแรก — ถ้าวางก่อนจะแย่งตัวจริง (ตรวจแล้ว: ลำดับเป็น `DIV:SS, DIV:BG, DIV:BN, A:SS` ✓)<br>• path เต็มจากราก — relative จะวิ่งไปหาไฟล์ใน `saveone-go-admin/` แล้ว 404 |

> **แตะไฟล์เว็บหลักกี่จุด: 2** — (1) บล็อกลิงก์ข้างบน (2) ป้ายเวอร์ชัน `v2.6.4`→`v2.6.5` (แสดงผลอย่างเดียว)
> **ไม่แตะเลย:** `loadAll` · `fetchCSV` · `applyAll` · `onAuthStateChanged` · ระบบ cache · role gate
> **⚠️ Netlify สำรองใช้ผัง 3D ไม่ได้** — `saveonego-rp.netlify.app` เป็นคนละ origin ลิงก์จะ 404 (ถ้าชี้ไป github.io แทนก็จะกลายเป็นคนละ session ต้อง login ใหม่) · Netlify ยังใช้พรีวิวเว็บหลักได้ตามเดิม
> **ทดสอบแล้ว:** `node --check` ผ่านทั้ง 2 inline script · เปิดจริงผ่าน http:// ไม่มี error ใน console · ตรวจลำดับ `.branch-item` + สมดุลแท็กในบล็อกที่แก้
> ~~**ยังไม่ได้ทดสอบ:** เจ้าของต้องเช็คเองว่าปุ่มผัง 3D โผล่/ซ่อนถูกตาม `branches`~~
> ✅ **ปิดแล้ว (6 ส.ค. 69):** ใช้งานจริงผ่าน · v2.9.0 เพิ่ม `showMap3D` คุมอีกชั้นนอกเหนือจากการซ่อนตามสาขา

### v2.6.4 (28 ก.ค. 2569) — 🧱 จัดหน้าภาพรวมใหม่ กราฟไม่ตกจอ

**อาการ:** เปิดหน้าภาพรวมแล้วเห็นแต่ KPI กับบล็อกเป้า — หัวข้อกราฟรายวันไปอยู่ขอบล่างจอพอดี ต้องเลื่อนทุกครั้ง · ย่อหน้าต่างให้แคบลงเพื่อดูภาพรวมยิ่งหนัก เพราะการ์ด KPI ตกบรรทัดเพิ่มอีก

| Fix | ปัญหา | แก้อะไร |
|---|---|---|
| UI-04 🧱 | บล็อกเป้าของ ENH-05 มี **3 ชั้นซ้อนกัน** (`#targetAlert` แบนเนอร์เตือน ~55px + `#targetMonthBox` เป้าเดือน/แถบเพซ/โน้ต/5 วันล่าสุด ~275px + แถบสรุปเป้า ~45px = **~375px**) และถูกวาง **คั่นกลางระหว่าง `revSection` กับ `statsSection`** → (1) การ์ด KPI 2 ชุดขาดจากกัน อ่านเป็นคนละเรื่อง (2) กราฟหลักถูกดันพ้นจอ | **index.html:** (1) ย้ายทั้งบล็อกจากบรรทัดเดิม (883–912) ลงไปไว้ **หลัง `</div><!-- /statsSection -->`** ก่อน `.main-chart-card` — KPI 2 ชุดต่อกันแล้ว (2) ห่อด้วย `<details id="targetPanel">` + `<summary id="targetSummary">` พับเก็บได้ ค่าเริ่มต้น = พับ เหลือความสูง ~40px (3) `renderTargetStatus()` เลิกเขียน `#targetAlert` (ลบทิ้ง) เปลี่ยนเป็นเขียนสรุป 1 บรรทัดลง `#targetSummaryText`: `streak + ชื่อสาขา(โซน) + เฉลี่ย/เป้า + % ต่ำกว่าเป้า + % เป้าเดือนนี้` — **ใช้ค่าที่ฟังก์ชันคำนวณอยู่แล้วทั้งหมด ไม่เพิ่มลอจิกคำนวณใหม่** (4) CSS ซ่อนสามเหลี่ยม `::marker`/`::-webkit-details-marker` + สลับป้าย `รายละเอียด ▾` / `ย่อ ▴` ด้วย `[open]` |

> **คงตามเดิม:** เกณฑ์สี 🔴 หลุด ≥ `TGT_MISS_STREAK` (3) วันติด / 🟠 1–2 วัน / 🟢 วันล่าสุดถึงเป้า · ซ่อนทั้งก้อนเมื่อ `group !== 'all'` (ข้อจำกัดที่ตั้งใจของ ENH-05) · `currentTarget()` / `applyAll()` / `targetLock` ไม่แตะเลย
> **ระวังตอนแก้ต่อ:** `#targetAlert` **ไม่มีแล้ว** — ถ้าโค้ดที่ไหนยังอ้างถึงจะได้ `null` (ตรวจแล้วไม่มีจุดอื่นอ้างถึง) · เนื้อใน `<details>` ยังอยู่ใน DOM ตอนพับ แต่ไม่มี canvas อยู่ข้างใน จึงไม่กระทบ guard `isConnected` ของ `makeChart`
> **ทดสอบแล้ว:** `node --check` ผ่านทั้ง 2 inline script · ตรวจ id ที่ JS อ้างถึงครบ 19 ตัว · ตรวจลำดับบล็อก รายรับ → สถิติการจอง → บล็อกเป้า → กราฟ · **เจ้าของเปิดเบราว์เซอร์ทดสอบแล้ว = ผ่าน (28 ก.ค. 21:00)**

### v2.6.3 (28 ก.ค. 2569) — ⚡ เปิดเว็บเห็นทันที (เอา Apps Script ออกจากการรอ)

**ที่มา:** v2.6.2 ทำให้โหลดเหลือ 9.3 วิ แต่ยัง**สุ่มเจอท่อค้าง** เป็นระยะ (20-45 วิ) — เพราะทุกวิธีที่ผ่านมา (retry / timeout / คุมจำนวนท่อ) **ยังให้ผู้ใช้นั่งรอ Apps Script อยู่** ต่างกันแค่รอยังไง · Apps Script Web App เป็นบริการฟรีที่สุ่มค้างเป็นปกติ ปรับพารามิเตอร์ไม่เปลี่ยนข้อนี้

| Fix | ปัญหา | แก้อะไร |
|---|---|---|
| CACHE-01 ⚡ | ผู้ใช้ต้องรอผลจาก Apps Script ทุกครั้งที่เปิดเว็บ — ถ้าท่อไหนค้างก็ต้องนั่งรอ และถ้าพังก็เห็นเลข 0 หรือหน้าโหลดค้าง | **index.html:** เก็บ **CSV ดิบ 6 ก้อน** ใน `localStorage` (`saveone_data_v1`, ~300 KB = 6% ของโควตา 5 MB) — เก็บ CSV ไม่ใช่ object ที่ parse แล้ว เพราะเล็กกว่ามากและไม่มีปัญหา `Date`<br>• `loadAll()` ขั้น 1: มี cache → `applyCsvBundle()` + render + `hideSplash()` **ทันที** ป้ายเป็น `ข้อมูล HH:MM · กำลังอัปเดต…`<br>• ขั้น 2: ดึงของใหม่เบื้องหลัง (ไม่บังหน้าจอ) → สำเร็จ: `saveCache()` + ป้าย `อัปเดต HH:MM` · ล้มเหลว: ใช้ของเก่าต่อ ป้าย `· อัปเดตไม่สำเร็จ` สีแดง<br>• `saveCache()` บันทึก**เฉพาะตอนได้ครบทั้ง 6 ท่อ** — ไม่ cache ข้อมูลไม่ครบไว้หลอกตัวเองรอบหน้า<br>• cache เก่าเกิน **7 วัน** ไม่ใช้<br>• ถ้าโชว์ cache อยู่แล้ว `catch` จะ**ไม่เอาหน้า "โหลดไม่ได้" ไปทับ** — ทำงานต่อได้ปกติ<br>• แยก `applyRowsBundle()` ให้ "โหลดสด" กับ "อ่าน cache" ใช้เส้นทางเดียวกัน ลดโอกาสสองทางไม่ตรงกัน<br>• `setDataAge(ts, state)` ป้ายบอกอายุข้อมูลเสมอ (ข้ามวันแสดงวันที่ด้วย) |

> **⚠️ ยังไม่ได้แก้ที่ราก:** Apps Script ยังสุ่มค้างเหมือนเดิม — แค่**ไม่มีใครต้องรอมันแล้ว** · งานถัดไปที่แก้รากคือย้ายไป Firestore (ให้ Apps Script ทำงานตาม time trigger แทนที่จะทำตอนผู้ใช้เปิดเว็บ) ดูข้อ 6
> **ทดสอบ:** `node --check` ผ่าน · จำลอง logic cache 5 เคส (ข้อมูลไม่ครบ→ไม่บันทึก · ครบ→บันทึก · อ่านปกติ · เก่า 8 วัน→ไม่ใช้ · ไม่มี cache→โหลดสด) ผ่านทุกเคส · คำนวณขนาด 304 KB = 5.9% ของลิมิต

### v2.6.2 (28 ก.ค. 2569) — 🚦 ความทนทาน + ความเร็วตอนโหลด

> รวมงานทั้งวันของ 28 ก.ค. ไว้เป็นเวอร์ชันเดียว (ระหว่างทางเคยแยกเป็น 2.6.1/2.6.2/2.6.3)

**อาการตั้งต้น:** เปิดเว็บรอนาน 1-2 นาที · บางสาขาได้ **0 แถว** แต่หน้าจอไม่บอกอะไร โชว์รายรับ ST = 0 กับ "หลุดเป้า 89 วันติด" เหมือนเป็นยอดจริง

**ผลลัพธ์:** โหลด **71.7 วิ → 9.3 วิ** ไม่มี error ไม่มี retry (งานจริง 18.4 วิ ÷ 2 ท่อพร้อมกัน = 9.2 วิ → ขนานได้เต็มประสิทธิภาพ)

| Fix | ปัญหา | แก้อะไร |
|---|---|---|
| PERF-03 🚦 | **ต้นเหตุจริง** — งานจริงเบามาก (2-3 วิ/ท่อ) แต่รอ 71.7 วิ: ยิง 6 ท่อพร้อมกันเกินลิมิต concurrency ฝั่ง Google → คลื่นแรกพังเกือบหมด (404 / Unauthorized / timeout) หลังรอ 18-45 วิ พอ retry มาทีหลังกระจายตัวแล้วผ่านหมดใน 2-3 วิ · เวลาเกือบทั้งหมดคือ retry + backoff · **"ใครพัง" เป็นเรื่องสุ่ม** ไม่ใช่ปัญหาสาขาใดสาขาหนึ่ง | **index.html:** เพิ่ม `fetchPool(tasks, limit)` คิวงานยิงพร้อมกันไม่เกิน `FETCH_CONCURRENCY` (**=2** ปรับที่เดียว) ท่อไหนเสร็จดึงงานถัดไปทำต่อ · `loadAll()` เรียงงาน **สลับสาขา** (ST ทั้ง 3 สาขาก่อน แล้วค่อย NON/CAR) ให้ 2 ท่อที่วิ่งพร้อมกันเป็นคนละ Apps Script เสมอ · `refreshBranch()` (2 ท่อเข้า script เดียวกัน) บังคับ `limit=1` |
| PERF-04 ⏱ | `FETCH_TIMEOUT=45` วิ สูงเกินไป — ท่อที่ค้างจะลากเวลารวมทั้งรอบ (เจอจริง: BG_ST ค้าง **34.3 วิ** แล้วตอบ 404 ระหว่างนั้น worker อีกตัววิ่งจบงานแล้วนั่งรอ) | **index.html:** `FETCH_TIMEOUT` 45000 → **12000** · `FETCH_BACKOFF` 2000 → **1000** · เหตุผล: ท่อที่สำเร็จใช้ **2.2-7.5 วิ ไม่มีเกิน 8 วิ** → เกิน 12 วิแทบไม่มีทางสำเร็จ ตัดแล้ว retry เร็วๆ คุ้มกว่ารอ (ท่อค้าง ~48 วิ → **~15 วิ**) |
| PERF-01 📊 | ไล่หาสาเหตุ "โหลดนาน" มา 4 รอบโดย**ไม่มีตัวเลขสักตัว** — เดาผิดทุกครั้ง (concurrency → คอลัมน์บวม → ขนาด output → cold start) | **index.html:** `fetchCSV` จับเวลาต่อท่อ log `⏱ X.X วิ ?sheet=... rows: N` (เคส error บอกว่าผ่านไปกี่วิ) เก็บลง `fetchTimes[]` · `logLoadTimes()` สรุปท้ายรอบ: เวลาอุ่น token, ทุกท่อเรียงช้า→เร็ว, จำนวน retry, เวลารวม, ท่อช้าสุด |
| PERF-05 🐛 | เกณฑ์เตือนของ PERF-01 เทียบกับ "ท่อช้าสุด × 1.8" ซึ่งใช้ได้เฉพาะตอนยิงขนานทั้งหมด — พอใช้ pool แล้วเวลารวม *ควรจะ* สูงกว่าท่อเดียวอยู่แล้ว → **เตือนผิดทุกครั้ง** (เตือนหมาป่า) | **index.html:** เปลี่ยนเป็นเทียบกับ `ผลรวมทุกท่อ ÷ FETCH_CONCURRENCY` = เวลาที่ควรได้จริง · log บอกค่าที่ควรได้ด้วย · ถ้ามี retry แจ้งแยกอีกข้อความ ไม่ปนกัน |
| NET-01 🐛 | `fetchCSV` ใช้ `fetch()` เปล่าๆ **ไม่มี timeout** — Apps Script ค้างเมื่อไหร่ `Promise.all` ไม่ settle ตลอดกาล splash ค้างไม่มีวันหาย; retry เดิม 1 ครั้งหน่วง 600ms เร็วเกินไป | **index.html:** `AbortController` + `finally clearTimeout` · `FETCH_RETRY=2` backoff คูณ 2 ทุกครั้ง · แยกข้อความ `AbortError` เป็น "timeout N วิ" · เก็บท่อที่พลาดลง `loadErrors[]` |
| NET-03 🚩 | ข้อมูลโหลดไม่ครบแล้ว**หน้าเว็บเงียบสนิท** — เขียนแค่ `console.warn` (ตั้งใจตั้งแต่ v2.5.1 กัน alert เด้งตอน auto-refresh) ผลคือเห็นเลข 0 แล้วนึกว่าไม่มีลูกค้าจริง **อันตรายกว่าเว็บพัง** | **index.html:** `<div id="dataHealth">` บนสุดของ `#mainContent` + `renderDataHealth()` — แบนเนอร์แดงบอกว่าสาขาไหนโหลดไม่ได้ + "ตัวเลขบนหน้านี้ยังไม่ใช่ยอดจริง อย่าเพิ่งเอาไปใช้ตัดสินใจ" + รายละเอียดจาก `loadErrors` · เรียกทั้งใน `loadAll` และ `refreshBranch` (รวมเคส return ก่อนกำหนด) |
| NET-04 🐛 | `catch` ใน `loadAll()` เขียน error ลง `#loadingState` แต่**ไม่ซ่อน splash** — splash `z-index:9999` ทับหมด → error มองไม่เห็น เห็นเป็นหน้าโหลดค้าง แยกไม่ออกว่า "กำลังโหลด" หรือ "พังไปแล้ว" | **index.html:** แยก `hideSplash()` เรียกทั้งตอนสำเร็จและใน `catch` + `catch` ตั้ง `loadingState.display='flex'` ให้เห็นปุ่ม "ลองใหม่" |
| CHT-01 🐛 | `Uncaught TypeError: Cannot read properties of null (reading 'getContext')` ที่ `chart.umd.min.js` ตอนสาขาว่าง 0 แถว — guard ของ FIX-016 เช็คแค่ `if(!el)` ยังไม่พอ และกราฟตัวเดียวพังทำให้ทั้งหน้าหยุด render | **index.html:** `makeChart` เพิ่มเช็ค `typeof el.getContext!=='function' \|\| !el.isConnected` + ห่อ `new Chart` ด้วย try/catch · `dChart` ห่อ `destroy()` ด้วย try/catch |
| SEC-07 ✅ | **[Apps Script ทั้ง 3 สาขา — deploy แล้ว]** `verifyToken` ยิง Firebase ครั้งเดียว ถ้าตอบ ≠ 200 (throttle/ชั่วคราว) คืน `null` ทันที → หน้าเว็บได้ `Unauthorized` ทั้งที่ token ดี; cache 300 วิ ชนกับ auto-refresh ทุก 5 นาทีพอดี เลย miss แทบทุกรอบ | **Apps Script:** `verifyToken` retry 2 ครั้ง หน่วง 700ms (`Utilities.sleep`) + try/catch กัน network error · `cache.put(..., 300)` → **`3000`** (50 นาที ยังสั้นกว่าอายุ token 60 นาที) · โค้ดเต็ม + วิธี deploy อยู่หัวไฟล์ `3_apps-script-code.md` |

> **เคยลองแล้วถอนออก — NET-02:** ยิงทีละท่อในสาขาเดียวกัน เพราะเชื่อว่า ST+NON ชนกันคือต้นเหตุ → **พิสูจน์แล้วว่าไม่ใช่** (แก้เสร็จ ST ยังพลาดตั้งแต่ท่อแรกที่ยิงเดี่ยวๆ) และผลข้างเคียงคือ**เวลาโหลดคูณสอง** จึงถอนออก แทนด้วย PERF-03 ที่คุม concurrency ตรงๆ
> **ทดสอบแล้ว:** `node --check` ผ่านทั้ง 2 inline script · จำลอง `fetchPool` ด้วย node (ยิงพร้อมกันสูงสุด 2 ตามที่ตั้ง, ได้ครบ 6 key, `limit=1` ได้ 1) · จำลองเกณฑ์เตือน PERF-05 ครบ 3 เคส (ปกติ / มี retry / ขนานพัง) · deploy จริงได้ **9.3 วิ** ข้อมูลครบ SS 484/362 · BG 483/483 · BN 179/179

#### 🔍 บันทึกการสืบสวน "โหลดช้า/ข้อมูลหาย" — อ่านก่อนถ้าอาการกลับมา

> ### ✅ ปิดเคสแล้ว — สาเหตุจริงคือ **ยิง 6 ท่อพร้อมกันเกินลิมิต Google**
> ท่อที่สำเร็จใช้แค่ **2-3 วิ** แต่คลื่นแรกพังเกือบหมดหลังรอ 18-45 วิ แล้ว retry ทีหลังผ่านหมดใน 2-3 วิ → **71.7 วิ โดยเกือบทั้งหมดเป็น retry ไม่ใช่งานจริง**
> **"ใครพัง" เป็นเรื่องสุ่ม** — รอบที่วัด SS ผ่านฉลุย 2.7 วิ แต่ BG/BN พังแทน จึงไม่ใช่ปัญหาของ SS หรือชีตใดชีตหนึ่ง
> **ต้นตอ: v2.5.2 (SEC-05)** ที่เปลี่ยนจากยิงทีละท่อเป็น `Promise.all` 6 ท่อ — Fix Log เขียนเองว่าตอนนั้นต้องใส่ retry "กัน SS=0 ตอน cold start" คือหลักฐานว่าอาการเริ่มตั้งแต่นั้น · พอข้อมูลโตขึ้น execution นานขึ้น การทับซ้อนก็หนักขึ้นจนพังบ่อย · **v2.6.0 ไม่ได้แตะโค้ดเครือข่ายเลย** แค่อาการหนักพอจนสังเกตเห็น

**อาการเดิมที่บันทึกไว้ (ก่อนรู้สาเหตุ):** ดูเหมือนเป็นเฉพาะ `SS ?sheet=ST` — `Unauthorized` → HTTP 404 (`macros/echo`) → `timeout 45 วิ` · BG/BN ปกติ · หายเองแล้วกลับมาแบบสุ่ม
⚠️ ที่ดูเหมือนเป็นเฉพาะ SS เพราะบังเอิญ SS แพ้บ่อยกว่าในช่วงนั้น — พอวัดจริงแล้วสลับกันได้ทุกสาขา

**ทฤษฎีที่ทดสอบแล้วว่า "ผิด" — อย่าเสียเวลาไล่ซ้ำ:**

| ทฤษฎี | หลักฐานที่หักล้าง |
|---|---|
| ❌ ท่อ ST+NON ยิงชนกันในสาขาเดียว | แก้เป็นยิงทีละท่อ (NET-02) แล้ว ST ยังพังตั้งแต่ครั้งแรกที่ยิงเดี่ยวๆ |
| ❌ deployment ของ SS พัง | เปิด `/exec` ตรงๆ ได้ `Unauthorized` = ปกติดี |
| ❌ ชีตบวม / คอลัมน์ขยะ / response ใหญ่เกิน | วัดด้วย `timeST()`: range จริง **485 × 17**, CSV 44 KB, `buildCSV` ใช้ **1.0 วินาที** (วิธีวัดอยู่ใน `3_apps-script-code.md`) |
| ❌ token หมดอายุ | ท่อ NON บน script เดียวกัน token เดียวกัน เวลาเดียวกัน **ผ่าน** |
| ❌ เป็นปัญหาเฉพาะสาขา SS | รอบที่วัดด้วย PERF-01: **SS ผ่านครั้งเดียว 2.7 วิ** ส่วน BG_CAR/BG_ST/BN_CAR พังแทน |

**ถ้าอาการกลับมาอีก ให้ทำตามลำดับนี้:**

1. ดูบล็อก **"⏱ สรุปเวลาโหลด"** ใน console ก่อนเสมอ — บอกทุกอย่างที่ต้องรู้ (มันเทียบให้เองว่าเวลาที่ได้ห่างจากที่ควรได้แค่ไหน)
2. **ท่อสำเร็จเร็ว (2-3 วิ) แต่เวลารวมสูง + มี retry เยอะ** → concurrency ยังสูงไป ลด `FETCH_CONCURRENCY` เป็น 1
3. **ท่อเดี่ยวๆ ช้าเอง (>10 วิ)** → ไปฝั่ง Apps Script: รัน `timeST()` วัด `buildCSV` + เปิด **การดำเนินการ (Executions)** ดู duration จริง
4. **ไม่มีแถวใน Executions เลยตอนที่หน้าเว็บ timeout** → request ไปไม่ถึง = dispatch ฝั่ง Google แก้ที่โค้ดไม่ได้ ต้องเปลี่ยนสถาปัตยกรรม

**เลิกทำแล้ว:** ~~NET-05 หน่วงเริ่มแต่ละสาขา 500ms~~ · ~~NET-06 ขยาย timeout 45→90 วิ~~ — PERF-03 + PERF-04 แก้ที่เหตุแล้ว ไม่จำเป็น

### v2.6.0 (28 ก.ค. 2569) — 🎯 ENH-05 เทียบเป้า vs ยอดจริง + เตือนหลุดเป้า
| Fix | ปัญหา | แก้อะไร |
|---|---|---|
| ENH-05 🎯 | เป้ามีช่องเดียว (`targetLock=400`) ใช้ร่วมทุกสาขา/ทุกโซน + ตั้งใหม่ทุกครั้งที่เปิดเว็บ + ไม่มีอะไรเตือนเมื่อยอดตก | **index.html:** (1) `TARGETS` เป้าแยกสาขา×โซน (ST / Car-Non) เก็บ localStorage key `saveone_targets_v1` — `loadTargets()`/`saveTargets()`/`currentTarget()`/`syncTargetUI()`/`setTargetZone()`; sidebar เป็น 2 ช่อง เปลี่ยนตามสาขาที่เลือก (2) `renderTargetStatus()` แบนเนอร์เตือนหน้าภาพรวม: หลุดเป้าติดกันครบ 3 วัน (`TGT_MISS_STREAK`) = แดง / 1-2 วัน = ส้ม / วันล่าสุดถึงเป้า = เขียว (3) กล่อง "เป้าเดือนนี้" แถบความคืบหน้า + เส้นเพซที่ควรเป็น + คาดการณ์ยอดสิ้นเดือน + ต้องทำกี่ล็อก/วันในวันที่เหลือ (4) รายการ 5 วันล่าสุดเทียบเป้า (5) `applyAll()` ตั้ง `targetLock` จาก `currentTarget()` ทุกรอบ ให้กราฟ/ข้อมูลดิบใช้เป้าเดียวกัน |
| CMP-06 🐛 | (เจอระหว่างทำ ENH-05) หน้าเปรียบเทียบเดือนอ่านเป้าจาก `document.getElementById('targetInput')` ซึ่งถูกแทนที่แล้ว → จะ fallback เป็น 400 ตลอด | **index.html:** เปลี่ยนเป็น `currentTarget()` |

> **ข้อจำกัดที่ตั้งใจ (ENH-05):** (1) แบนเนอร์/กล่องเป้าเดือนโชว์เฉพาะตอน group = "ทุกกลุ่ม" — ถ้ากรอง online/walkin เป้าจะเทียบไม่แฟร์จึงซ่อน (2) เพซคิดจาก "จำนวนวันที่มีข้อมูลในเดือน / วันทั้งเดือน" ไม่ใช่วันที่ปฏิทินจริง ถ้าตลาดปิดบางวัน เพซจะดูช้ากว่าจริงเล็กน้อย (3) เป้าเก็บในเครื่อง (localStorage) แยกตามเครื่อง/เบราว์เซอร์ ไม่ sync ข้ามผู้ใช้

### v2.5.8 (21 ก.ค. 2569) — 📈 Benchmark: เทรนด์ + ป้ายเตือนสาขาไหลลง
| Fix | ปัญหา | แก้อะไร |
|---|---|---|
| BM-01 📈 | แท็บ Benchmark กราฟ "ล็อกเฉลี่ย/วัน แต่ละสาขา" เป็น**กราฟแท่ง** อ่านเป็นการเทียบ ไม่เห็นทิศทางว่าสาขาไหนกำลังขึ้น/ลง; ค่าเฉลี่ยยังนับวันที่ล็อก = 0 (วันปิด/ไม่มีข้อมูล) เป็นวันหนึ่ง → เฉลี่ยต่ำกว่าจริง | **index.html:** เพิ่ม `bmMonthlyAvg(b)` คืน `{avg,days}` รายเดือนโดย **ตัดวันที่ `getLock(r,'all')` = 0 ออกก่อนเฉลี่ย**; เปลี่ยน `cBmAvg` จาก `type:'bar'` → `type:'line'` (borderWidth 2.5, tension .3, spanGaps) ใช้สี `BM_COLORS`; เดือนที่มีข้อมูล < `BM_MIN_DAYS` (7) วาด **จุดกลวง** = ยังไม่ครบเดือน; หัวกราฟเป็น "เทรนด์ล็อกเฉลี่ย/วัน รายเดือน" + โน้ตใต้กราฟอธิบายว่าใช้เฉลี่ย/วันและตัดวัน 0 |
| BM-02 🚩 | ไม่มีสัญญาณเตือนว่าสาขาไหนกำลังไหลลง ต้องนั่งอ่านกราฟเอง | **index.html:** เพิ่ม `<div id="bmAlert">` เหนือกราฟ + `bmTrend(b)` นับเดือนที่ตกต่อเนื่องนับถอยหลังจากเดือนล่าสุด (คิดเฉพาะเดือนที่มีข้อมูล ≥ 7 วัน) → `>= BM_ALERT_N` (3) = 🔴 **"ไหลลงไม่หยุด"**, 2 เดือน = 🟡 เฝ้าระวัง, 1 เดือน = 🟡 ยังไม่นับเป็นเทรนด์, 0 = 🟢 ไม่ไหลลง; ป้ายบอกค่าเฉลี่ยเดือนล่าสุด + % ต่ำกว่าจุดพีคด้วย |
| BM-03 🐛 | **เจอตอนเทสต์:** เทียบเดือนด้วย `<` เฉยๆ → BN บางนา (1699→1200→1050→1010→1005→1000 = เจอพื้นแล้วราบ) ได้ streak = 5 ขึ้น 🔴 ทั้งที่สุขภาพดี | **index.html:** ใส่ deadband `BM_DROP_PCT=3` — นับเป็น "ตก" เฉพาะเดือนที่ลด **เกิน 3%** จากเดือนก่อน. ผลหลังแก้ตรงกับบทวิเคราะห์: SS 🔴 streak 6 (−83% จากพีค) · BG 🟢 · BN 🟢 (−41% จากพีคแต่ราบแล้ว) |

> **คงตามดีไซน์เดิม:** Benchmark ไม่ใส่ filter zone/group · รายรับใน KPI card ยัง gate ด้วย `userPerms.showRevenue` · สีกราฟใช้ `cOpts()`/`chartClr()` ตามธีม ไม่ hardcode
> **ตัวเลขปรับได้ที่หัวฟังก์ชัน:** `BM_MIN_DAYS=7` (เดือนสั้นไม่เอามาคิด) · `BM_ALERT_N=3` (กี่เดือนติด = ธงแดง) · `BM_DROP_PCT=3` (ลดกี่ % ถึงนับว่าตก)

### v2.5.7 (21 ก.ค. 2569) — 🔮 หน้าพยากรณ์อิงเดือนปฏิทิน (ตัด "30 วันข้างหน้า")
| Fix | ปัญหา | แก้อะไร |
|---|---|---|
| FCST-02 🎯 | "พยากรณ์ 30 วันข้างหน้า" (rolling) ซ้ำซ้อนกับ "คาดทั้งเดือน" → คนดูงงว่าตัวไหนตัวจริง; 30 วัน rolling คร่อม 2 เดือน เทียบยอดจริงรายเดือนไม่ได้ | **index.html:** `renderForecast()` คำนวณเดือนหน้าจาก **เดือนปฏิทินจริง** — `_nextY`/`_nextM`/`_daysInNext` (จัดการข้ามปี ธ.ค.→ม.ค. + ปีอธิกสุรทิน ก.พ. 29 วัน) แล้ว `fcLock=avgLock×_daysInNext`, `fcRev=avgRev×_daysInNext`; หัวข้อเปลี่ยนเป็น "🔮 เดือนหน้า — {เดือน} (เต็มเดือนปฏิทิน N วัน)"; label จุดพยากรณ์ในกราฟ `cFC` เปลี่ยนจาก "พยากรณ์" → "{เดือน} (คาด)"; `fcNote` อธิบายฐานใหม่. **What If เก็บไว้ตามเดิม** |
| FCST-03 🐛 | บล็อก "📅 เดือนนี้" ผูกกับ `_curIncomplete` → วันสุดท้ายของเดือนบล็อกหายทั้งอัน การ์ดคู่เหลือข้างเดียว | **index.html:** เปลี่ยนเงื่อนไขเป็น `_hasCur = !!mon[_curMK]` — มีข้อมูลเดือนปัจจุบันก็แสดงเสมอ; แยก `_warnBar`/`_progBar`/`_projCard` ให้โผล่เฉพาะตอนเดือนยังไม่จบ, เดือนจบแล้วโชว์ "ยอดจริง" (หัวข้อ "(จบแล้ว · ยอดจริง)"), ไม่มีข้อมูลเดือนปัจจุบันเลย → ซ่อนบล็อกเหมือนเดิม |
| FCST-04 ✅ | การ์ดคู่ไม่สมมาตร — ฝั่ง "เดือนหน้า" มีรายรับ ฝั่ง "เดือนนี้" มีแต่ล็อก | **index.html:** เพิ่ม `_curRevCards` (รายรับเฉลี่ย/วัน + รายรับคาดทั้งเดือน / รายรับรวมทั้งเดือนถ้าจบแล้ว) gate ด้วย `_fcRev` (`userPerms.showRevenue`) เหมือนจุดอื่น — manager ไม่เห็น |

> **ทดสอบ:** syntax `node --check` ผ่านทั้ง 2 inline script · dry-run 4 เคส (เดือนยังไม่จบ / วันสุดท้ายของเดือน / role ไม่มีสิทธิ์รายรับ / ไม่มีข้อมูลเดือนปัจจุบัน) · date math ข้ามปี+อธิกสุรทิน ถูกต้อง

### v2.5.6 (ก.ค. 2569) — 🐛 แก้บั๊ก 4 จุด + จัดหน้าพยากรณ์ใหม่
| Fix | ปัญหา | แก้อะไร |
|---|---|---|
| DATE-01 🐛 | หน้า "กำหนดเอง" เลือกวันที่ 1 แต่ข้อมูลเริ่มวันที่ 2 (เลื่อน +1 วัน) | **index.html:** `applyCustomDate()` parse เป็นเวลา local (`new Date(f+'T00:00:00')` / `+'T23:59:59'`) แทน `new Date(f)` ที่เป็น UTC ทำให้ record วันแรก (local midnight) หลุด filter |
| CMP-05 🐛 | หน้าเปรียบเทียบ กล่อง "สรุปตัวเลขสำคัญ" แถวเฉลี่ยรายรับไม่มีลูกน้ำ | **index.html:** เปลี่ยน `.toFixed(0)` → `fmtN(...)` ให้ใส่ comma เหมือนแถวอื่น |
| HEAT-01 🎨 | ปฏิทินล็อก heatmap เรียงแนวตั้ง เหลือพื้นที่ว่างด้านขวาเยอะ | **index.html:** ห่อแต่ละเดือนด้วย `.hm-month` + `#hmWrap` เป็น `flex-wrap` เรียงแนวนอนไหลไปทางขวาแล้ววนลงบรรทัดใหม่; `.hm-grid` ใช้ `width:340px` คงที่ cell ขนาดเท่าเดิม |
| FCST-01 🎯 | หน้าพยากรณ์เอาเดือนปัจจุบันที่ยังไม่จบมาคิดค่าเฉลี่ยด้วย ทำให้ตัวเลขเพี้ยน + อ่านแล้วงง แยกอดีต/อนาคตไม่ออก | **index.html:** `renderForecast()` แยกเดือนปัจจุบันที่ยังไม่จบออกจากฐานเฉลี่ย 3 เดือน + จัดหน้าเป็น 2 กลุ่ม "📅 เดือนนี้ (กำลังดำเนินอยู่)" (ล็อกสะสม/เพซเฉลี่ยตอนนี้ + ลูกศร ▲▼ เทียบเฉลี่ย/คาดทั้งเดือน + แถบความคืบหน้า + ป้ายเตือน) กับ "🔮 พยากรณ์เดือนหน้า" |

### v2.5.5 (ก.ค. 2569) — 🔘 ปุ่ม "ทั้งหมด" เลือกทุกเดือน
| Fix | ปัญหา | แก้อะไร |
|---|---|---|
| CMP-04 🔘 | หน้าเปรียบเทียบเดือนเลือกเดือนได้ทีละปุ่ม อยากดูทุกเดือนต้องกดทีละอัน | **index.html:** เพิ่มปุ่ม "ทั้งหมด" (ขอบทอง) หน้าปุ่มเดือน — กด = เลือกครบทุกเดือน, กดซ้ำ = ล้างทั้งหมด; ปุ่มอัปเดตสถานะเองเมื่อเลือก/ยกเลิกเดือนทีละอัน (`toggleAllMonths()`, `_syncAllMonthBtn()`, ปุ่มเดือนมี `data-mk`) |

### v2.5.4 (ก.ค. 2569) — 🎨 หน้าเปรียบเทียบเดือน + สีกราฟโหมดสว่าง
| Fix | ปัญหา | แก้อะไร |
|---|---|---|
| UI-03 🎨 | โหมดสว่าง: สีตัวหนังสือ/legend/เส้นตารางหลังกราฟ hardcode เป็นโทนมืด (`#5c6480`,`#9aa3bc`,`rgba(255,255,255,.04)`) → จมหายบนพื้นขาว **ทุกหน้า** | **index.html:** เพิ่ม `chartClr()` คืนสี tick/legend/grid ตามธีม แล้วแทนค่าสี hardcode ทุกจุด (กราฟหลัก/pie/cOpts/compare/benchmark) — กดสลับ Dark/Light รีวาดเห็นผลทันที |
| CMP-01 📊 | หน้าเปรียบเทียบดูยาก กราฟ/กล่องเล็ก ผู้ใช้บางคนหาไม่เจอ | **index.html:** จัดเลย์เอาต์ใหม่ — กราฟแท่ง "ล็อกรวมรายเดือน" เป็นกราฟหลักบนสุด (สูง 300px) + ตัวเลขบนแท่ง (inline plugin) + ป้าย "(ยังไม่จบเดือน)" บนเดือนปัจจุบัน; ย้ายกราฟเส้นรายวันลงล่างสุด (สลับตำแหน่งกัน) |
| CMP-02 🔢 | การ์ด MoM เล็ก มองไม่ชัด | **index.html (CSS):** `.mom-val` 18→26px, `.mom-month` 11→13px หนาขึ้น, การ์ดกว้าง/หนาขึ้น (`minmax` 160→200px + padding) |
| CMP-03 🧮 | ข้อมูลยังไม่ครบวงจร | **index.html:** เพิ่มกล่อง "สรุปตัวเลขสำคัญ" (รวมล็อก, เฉลี่ย/วัน, % เทียบเป้า, เดือนสูง/ต่ำสุด, รวมรายรับ + เฉลี่ยรายรับ/วัน) |
| UX-04 🗂 | แท็บ "เปรียบเทียบเดือน" อยู่กลางแถบ หายาก | **index.html:** ย้ายแท็บมาเป็นอันที่ 2 ติด "ภาพรวม" |

### v2.5.3 (ก.ค. 2569) — 🎨 แก้โลโก้/หน้าโหลด (splash) โหมดสว่าง
| Fix | ปัญหา | แก้อะไร |
|---|---|---|
| UI-01 🎨 | โลโก้รถบนหน้าโหลด (splash) เป็นรูปสีขาว พอเปิด **light mode** พื้น splash กลายเป็นสีสว่าง → รถขาวจมหาย เห็นเป็นสีขาว | **index.html:** ล็อก `.splash` ให้พื้นมืดเสมอใน light mode (re-declare ตัวแปรสีมืด + `background:#0a0a0a`) เอา light-mode override ของ `.splash-logo-savewan`/`.splash-company`/`#loginPage` ออก, คืน `.login-logo-text` ให้เข้ม (หน้า login ยังตามโหมดปกติ) |
| UI-02 🐛 | deploy แล้วรูปรถไม่ขึ้น โชว์ไอคอนรูปแตกเล็ก ๆ | สาเหตุ: `src="logo.png"` เป็น path สัมพัทธ์ ถ้า `logo.png` ไม่อยู่โฟลเดอร์เดียวกับ index.html หรือชื่อ/ตัวพิมพ์ไม่ตรง (GitHub Pages เซิร์ฟเวอร์ Linux แยกพิมพ์เล็ก-ใหญ่) → 404. วิธี: ตอน deploy วาง `logo.png` คู่ index.html เสมอ ชื่อพิมพ์เล็กตรงกัน |

> **โน้ต deploy:** GitHub Pages ต้อง rebuild + กระจาย CDN ทุกครั้งที่ push ปกติ 1–10 นาที ไม่ใช่ที่ไฟล์. อยากเห็นผลเร็ว: พรีวิวที่ Netlify (`saveonego-rp.netlify.app`, ลากไฟล์วาง) ก่อน แล้ว Ctrl+F5 เคลียร์ cache เบราว์เซอร์.

### v2.5.2 (มิ.ย. 2569) — ⚡ ความเร็ว + 🔒 ปิดช่องยกระดับสิทธิ์
| Fix | ปัญหา | แก้อะไร |
|---|---|---|
| SEC-05 ✅ | โหลดช้า/รวน: `loadAll()` ยิง 6 ท่อทีละท่อ + `getIdToken(true)` บังคับ refresh ทุก fetch + `verifyToken` ฝั่ง Apps Script ยิงถาม Firebase ทุก request | **index.html:** (1) ดึง token ครั้งเดียวใช้ซ้ำ (`refreshToken()`/`cachedToken`) (2) `loadAll` ยิง 6 ท่อพร้อมกัน `Promise.all` (3) `fetchCSV` retry 1 ครั้งถ้าว่าง/error กัน SS=0 ตอน cold start (4) `getIdToken(true)`→`getIdToken()` token นิ่ง ~55 นาที. **Apps Script (3 สาขา):** `verifyToken` cache ผลด้วย `CacheService` (hash token เป็น key, เก็บ 5 นาที) |
| FIX-016 🐛 | error `Cannot read properties of null (reading 'getContext')` หลังโหลดเร็วขึ้น (timing เปลี่ยน กราฟถูกสั่งวาดตอน canvas ยังไม่ขึ้น DOM) | **index.html:** `makeChart` ใส่ guard เช็ก canvas ก่อน — ถ้าไม่เจอ warn id + ข้ามการวาด ไม่ไป `new Chart` กับ null |
| SEC-06 ✅ | **ช่องโหว่ยกระดับสิทธิ์:** Firestore Rules หลวม — user ที่ login แล้ว (เช่น manager) เขียน field ของ doc ตัวเองได้ → ตั้ง `role:'admin'`, `branches`, perms เองผ่าน Console (F12) ได้ → เห็นข้อมูลเงินทุกสาขา | **Firebase Console → Firestore Rules:** `users/{userId}` อ่านได้เฉพาะเจ้าของ (`request.auth.uid == userId`), `allow write: if false` ห้าม client เขียนทุกกรณี (แก้ role/perms ผ่าน Console/admin เท่านั้น), collection อื่นปิดหมด. ทดสอบ pentest: ก่อนอุด 🔴 เขียนได้ → หลัง Publish 🟢 permission-denied |
| FIX-017 🐛 | field `role` ใน Firestore สะกดไม่ตรง — บาง doc เป็น `Role` (R ใหญ่) แต่โค้ดอ่าน `d.role` (เล็ก) → อ่านไม่เจอ fallback เป็น `manager` เสมอ (สิทธิ์ที่ตั้งไว้ไม่ทำงาน) | แก้ field ทุก doc ให้เป็น `role` ตัวเล็กตรงกับโค้ด + ลบ field ขยะ `Role` ทิ้ง |

> **SEC-06 — โน้ตเพิ่ม:** ช่องโหว่นี้คนนอกใช้ไม่ได้ ต้องมีบัญชี login จริงก่อน แล้วแก้ได้แค่ doc **ตัวเอง** (rule เดิมยังกัน doc คนอื่น) แต่อันตรายเพราะ `branches` คุมสิทธิ์ดูสาขาตรง ๆ. การคืนค่า user ที่โดนแก้ต้องทำที่ Console เอามือ (rule ปิด write แล้ว). สคริปต์ทดสอบ pentest อยู่ใน 4_AppsScript_Code.md.

### v2.5.1 (มิ.ย. 2569) — 🐛 แก้ข้อมูลหายเอง (Data Loss)
อาการ: ข้อมูลสาขา (เจอชัดที่ BG zone all/ST) ค่อยๆ หายเองระหว่างเปิดเว็บค้างไว้ ต้องรีทั้งเว็บข้อมูลถึงกลับ
| Fix | ปัญหา | แก้อะไร |
|---|---|---|
| FIX-015 🔴 | **ต้นเหตุ:** Firebase ID token อายุ 1 ชม. พอเปิดเว็บค้าง auto-refresh (ทุก 5 นาที) ยิงด้วย token หมดอายุ → Apps Script ตอบ `"Unauthorized"` เป็น **HTTP 200** → `fetchCSV` เช็คแค่ `r.ok` (200=ผ่าน) เลยเอา "Unauthorized" ไป parse เป็น CSV ว่าง → `refreshBranch` ทับ merged ด้วย `[]` = ข้อมูลหาย | **index.html (3 จุด):** (1) `getIdToken()` ใส่ `getIdToken(true)` บังคับ refresh token กันหมดอายุ (2) `fetchCSV` จับ "Unauthorized"/"Sheet not found" ที่เป็น HTTP 200 → throw error ไม่ parse (3) guard ใน `refreshBranch`+`loadAll`: ถ้า fetch คืนว่างทั้ง ST+Non/Car → ข้ามการ merge คงข้อมูลเดิม + เปลี่ยน `alert` เป็น `console` กัน popup เด้งตอน auto-refresh |

> **ไม่แตะ Apps Script** — แก้ฝั่ง index.html อย่างเดียว
> **ยังค้าง (perf ไม่ใช่ data loss):** SEC-05 — `verifyToken` ยิงถาม Firebase ทุก request ทำ Apps Script ช้า/timeout อาจ cache token ฝั่ง Apps Script ลดอาการได้

### v2.5.0 (มิ.ย. 2569) — 🔒 Security Hardening
อุดช่องโหว่ใหญ่: เดิม login เป็นแค่ฉากบังตา — ข้อมูลจริงดึงตรงจาก Apps Script ได้โดยไม่ต้อง login
| Fix | ปัญหา | แก้อะไร |
|---|---|---|
| SEC-01 🔴 | Apps Script เปิดให้ดึงข้อมูลตรงโดยไม่ตรวจสิทธิ์ ใครรู้ URL ก็ได้ข้อมูลทั้งหมด (รวมรายรับ/ค่าไฟ) permission ฝั่งหน้าจอแค่ `display:none` ข้อมูลโหลดมาเต็มใน memory | **Apps Script ทั้ง 3 สาขา:** เพิ่ม `verifyToken()` ตรวจ Firebase ID token ก่อนส่ง ไม่ผ่าน→"Unauthorized"; เปลี่ยนเป็น `doPost` (อ่าน token จาก body), `doGet` ตอบ Unauthorized |
| SEC-02 🔴 | Google Sheets เปิด publish-to-web (/pub URL) = ทางเข้าที่ 2 ที่ไม่มียาม | เลิกใช้ pub URL → อ่าน sheet ตรงด้วย `SpreadsheetApp.openById()` |
| SEC-03 🟡 | token หายตอน Apps Script redirect (302) ถ้าแนบใน query string | **index.html:** `fetchCSV()` เปลี่ยนเป็น POST แนบ token ใน body + เพิ่ม `window.getIdToken()`; ใช้ `x-www-form-urlencoded` เลี่ยง CORS preflight |
| SEC-04 🟡 | `SpreadsheetApp.getValues()` คืนวันที่เป็น Date object → CSV เพี้ยน parseD อ่านไม่ออก ข้อมูลเหลือ 0 แถว | **Apps Script:** แปลงวันที่เป็น dd/MM/yyyy ด้วย `Utilities.formatDate` ก่อนส่ง |

> **หมายเหตุ:** Firebase apiKey ในโค้ดเป็นค่าสาธารณะปกติ ไม่ใช่ความลับ — ตัวจริงที่กันคือ Auth + Firestore rules
> **ยังค้าง (ดูข้อ 6):** (1) ปรับความเร็ว loadAll (2) ตรวจ Firestore Security Rules

### v2.4.3 (มิ.ย. 2569)
แก้ราคา Walk-in/ล็อกเสริม ของ Car ที่คิดขาด +50 (ครอบ BG/BN ทุก role; SS ไม่กระทบ เพราะ Non คงที่ 30฿)
| Fix | ปัญหา | แก้อะไร |
|---|---|---|
| FIX-013 🔴 | รายรับ Car คิดราคา Walk-in + ล็อกเสริม ขาด +50 → รายงานรายรับต่ำกว่าจริงทุกวันที่มี Walk-in รถ (พิสูจน์ 17/06: 128,000 ควรเป็น 128,650 = 13 ล็อก×50) | `revNon`/`revNonNormal`/`calcDiscount` (BG/BN) เปลี่ยนจาก `(online+walkin+extra)×priceCar` เป็น `online×priceCar + (walkin+extra)×(priceCar+50)`; วันฝน ×0.5 หลังคิดราคาถูก. **ไม่กระทบเงินลูกค้า** (แอปจองเก็บ +50 ถูกแล้ว — แก้รายงานให้ตรงจริง) |
| FIX-014 🟡 | คอลัมน์ราคา/ล็อก หน้าข้อมูลดิบ โชว์ราคา ST ในโซน Car (ฮาร์ดโค้ด 100/130) | `renderRaw` ตั้ง po/pw ตาม zone: zone='non' ใช้ `getNonPrice` (online) + `+50` (walkin, SS ไม่บวก); st/all คงราคา ST เดิม |

### v2.4.2 (มิ.ย. 2569)
แก้ 3 bug กลุ่มข้อมูล "ราย" ของล็อกเสริม/ยกเลิก-ลา และการกรองโซน (ครอบทุกสาขา SS/BG/BN — ใช้โค้ดร่วมกัน)
| Fix | ปัญหา | แก้อะไร |
|---|---|---|
| FIX-010 🔴 | กราฟแท่ง "จำนวนราย" ของกลุ่มล็อกเสริม + ยกเลิก/ลา เป็น 0 (มีแต่กราฟเส้นล็อก) | root cause: `mapST`/`mapNon` ไม่ได้อ่านคอลัมน์ `ล็อกเสริม_Rai`/`Cancel_Rai`/`Absent_Rai` จากชีต → เติม mapping + ส่งผ่าน `mergeBranch` (extraRai/cancelRai/absentRai + non*) + แก้ `getRai` ให้คืนค่าจริงสำหรับ group extra/cancel |
| FIX-011 🟡 | ตารางข้อมูลดิบ คอลัมน์ยกเลิก/ลา ไม่โชว์ข้อมูลโซน Car | `renderRaw` ฮาร์ดโค้ดใช้ `cancelLock`/`absentLock` (ST) ตายตัว → เปลี่ยนเป็นสลับ/รวมตาม zone (st=ST, non=Car, all=รวม) เหมือน KPI |
| FIX-012 🔴 | โซน ST โชว์ "ราย" ของ Car ปน (เช่น 20/06 ราย 322 ทั้งที่ล็อก ST=0) | `applyZone` zone='st' ล้างค่า Car ไม่ครบ ลืม `nonOnlineRai`/`nonWalkInRai` (+ราย non ที่เพิ่มใหม่) → getRai บวกรายของ Car เข้ามา → เติมให้ zero ครบทุกฟิลด์ราย |

### v2.4.1 (มิ.ย. 2569)
| Fix | ปัญหา | แก้อะไร |
|---|---|---|
| FIX-009 🔴 | การ์ด Month-over-Month โชว์เลขเท่ากันทุกสาขา ไม่เปลี่ยนตามสาขา | MoM ใช้ตัวแปร `merged` (ค้างที่ SS) → เปลี่ยนเป็น `applyZone(getActiveMerged())` ดึงตามสาขา active + กรอง zone |

### v2.4 (มิ.ย. 2569)
เพิ่ม 2 ENH + แก้ 2 bug

| Fix | ปัญหา | แก้อะไร |
|---|---|---|
| ENH-01 ✅ | เลือกช่วงวันเองไม่ได้ มีแค่ปุ่มตายตัว | เพิ่มปุ่ม "กำหนดเอง" + input วันเริ่ม-สิ้นสุด ใน sidebar → filter ตามช่วง (customFrom/customTo) |
| ENH-02 ✅ | Heatmap cell 32px fixed ล้นจอมือถือ | cell เป็น aspect-ratio responsive + dropdown เลือก 3/6/12/ทั้งหมด เดือน |
| FIX-007 🔴 | มือถือแนวตั้งจอดำ ต้องหมุนแนวนอน | body/layout ล็อก height:100vh+overflow:hidden ตัด content ทิ้ง → @media มือถือปลด overflow ให้ scroll ได้ + เพิ่มความสูงกราฟ |
| FIX-008 🟡 | หน้าเปรียบเทียบเดือน filter zone ไม่ทำงาน | แยก zone filter เป็น applyZone() ใช้ร่วมกัน → compare กรอง zone ได้ทั้ง 2 จุด (group ทำงานอยู่แล้ว) |

> **หมายเหตุ:** Benchmark ตั้งใจไม่ใช้ zone/group (ดูภาพรวมเทียบสาขา) — ปล่อยไว้ตามดีไซน์

### v2.3 (มิ.ย. 2569)
แก้ 6 bug, ทดสอบผ่านทุกตัว, ค่าคำนวณไม่แตะ (ถูกอยู่แล้ว)

| Fix | ปัญหา | แก้อะไร |
|---|---|---|
| 001 🟡 | ราคา/ล็อก Rawdata วันฝน BG/BN โชว์วอคเต็ม | แสดง pw/2 สำหรับ BG/BN (SS คงเดิม) |
| 002+005 🔴 | รายรับรั่วถึง manager 3 จุด | gate showRevenue: กราฟเปรียบเทียบเดือน + คอลัมน์ข้อมูลดิบ + การ์ดพยากรณ์ |
| 004 🟡 | Splash โหมดสว่างมองไม่เห็น | เพิ่ม CSS light-mode .splash-company |
| 006 🟡 | กราฟ filter WalkIn แท่งเตี้ย | filter กลุ่มย่อยไม่ clamp แกน Y ด้วย target + ซ่อนเส้นเป้า |

**ยกเลิก:** BUG-003 (Cancel/Absent วันฝน) — ไม่ใช่ bug เพราะเข้าใจนโยบายผิด จริงๆ cancel/absent อยู่ในกลุ่ม Online ได้ลด 0.5 ถูกแล้ว

### v3 (ก่อนหน้า) — bug ที่แก้ไปแล้ว
Non/Car ไม่ขึ้น / กราฟไม่อัปเดตตอนเปลี่ยน filter / manager เห็นรายรับ (statsSection แยก) / Date Range แสดง — / revNormal ผิด (key ซ้ำ) / Zone label ไม่เปลี่ยน / KPI ลา-ไม่มา ไม่นับ Non / Benchmark รายรับโชว์ทุก role / scroll ไม่ได้ / dark mode / splash หาย / group filter ไม่เปลี่ยน KPI / zone=non ใช้ราคา ST ผิด / forecast ไม่สน filter / getLock walkin นับ nonLock รวม / getRai ไม่นับ Non / heatmap cell ใหญ่ / splash โชว์ก่อน login / calcDiscount BG-BN ใช้ po แทน pw / What If note ไม่บอก zone

---

## 6. งานที่รออนาคต (v3.0)
| # | สิ่งที่อยากได้ | สถานะ |
|---|---|---|
| ENH-01 | เลือกช่วงวันที่เอง (date picker) | ✅ เสร็จ v2.4 |
| ENH-02 | Heatmap เลือกจำนวนเดือน + cell พอดีจอ | ✅ เสร็จ v2.4 |
| ARCH-01 🎯 | **ย้ายข้อมูลไป Firestore** — Apps Script ทำงานตาม time trigger (ทุก 5-10 นาที) เขียนข้อมูลลง Firestore แทนที่จะทำตอนผู้ใช้เปิดเว็บ · หน้าเว็บอ่าน Firestore ตรงๆ (SDK/Auth/Rules มีอยู่แล้ว) · **แก้ที่รากจริง** — Apps Script ออกจากเส้นทางที่ผู้ใช้รอ ถ้า trigger รอบไหนพังก็ไม่มีใครเห็น · ลด execution จาก ~2,000/วัน เหลือ ~432/วัน (น่าจะเป็นเหตุที่ชนโควตา runtime 90 นาที/วัน) · ต้องใส่ service account key ใน Script Properties ~2-3 ชม. | รอทำ — **งานถัดไปที่ควรทำ** |
| **SEC-08** 🔒 | **Benchmark ควรอ่านเอกสารสรุป ไม่ใช่ข้อมูลดิบ** — BM-04 (v2.10.0) เปิดให้ manager ที่มี `bmBranches` อ่าน `data/{docId}` ของสาขาอื่นได้ ซึ่งเป็น **CSV ดิบรายวัน** (แยกออนไลน์/วอล์กอิน/โซน) ทั้งที่หน้าจอแสดงแค่ล็อกเฉลี่ย/วันรายเดือน → เปิด F12 อ่าน `bgMerged`/`bnMerged` ได้ทั้งก้อน · **ทางแก้ที่รากจริง:** ให้ Apps Script (3 สาขา) เขียนเอกสารสรุปแยก `benchmark/{branch}` ที่มีแค่ **ล็อกเฉลี่ย/วัน รายเดือน** ไม่มีรายวัน ไม่มีราคา → `renderBenchmark` อ่านจากตรงนี้แทน → **ถอนสิทธิ์อ่าน `data/` ข้ามสาขาออกจาก Rules ได้ทั้งหมด** (กลับไปเป็น `docId.split('_')[0] in myBranches()` เหมือน ARCH-01 เดิม) · **แถมได้เร็วขึ้น** — อ่านเอกสารสรุป 3 ตัวเล็ก แทนโหลด CSV ดิบ 6 ก้อน · ค่าใช้จ่าย: Apps Script +~40 บรรทัด/สาขา, เอกสารเพิ่ม 3 ตัว, เขียนเพิ่ม 432 ครั้ง/วัน จากโควตา 20,000 | **รอทำ — งานถัดไปที่ควรทำหลัง v2.10.0** |
| **LAY-04** 🗺️ | **ผัง 3D ของ BG / BN** — เจ้าของยืนยันทิศทางแล้วว่า *"สุดท้ายมันก็ต้องเห็นทุกผัง เพื่อให้ทุกคนทำงานร่วมกันได้"* · **ตัวติดไม่ใช่เรื่องสิทธิ์แล้ว** (LAY-03 เปิดให้ดูข้ามสาขา + deploy เฉพาะสาขาตัวเองเรียบร้อย) แต่คือ **ยังไม่มีพิกัดจาก CAD** — `BRANCH = { SS: {} }` และ `BASE_LAYOUTS` มีแค่ SS · ต้องถอดพิกัดแถว/ล็อก/ถนน/หลังคาของ BG กับ BN ออกจากแบบ CAD ก่อน แล้วเสียบเป็น `BRANCH.BG = {...}` + `BASE_LAYOUTS.BG` (โครงรองรับอยู่แล้ว ไม่ต้องรื้อลอจิก) จากนั้นเติมชื่อสาขาลง `BRANCH_READY` และเพิ่มลิงก์ใน sidebar เว็บหลัก<br>**กติกาการทำงานร่วมที่เจ้าของกำหนดไว้แล้ว:** สาขาอื่นอยากช่วยออกแบบ → **ส่งออกไฟล์ JSON** ให้เจ้าของสาขานำเข้าเอง หรือให้ admin อนุมัติให้กรณีฉุกเฉิน — **ไม่ต้องทำสิทธิ์แกนที่ 3** (ใครเห็นสาขาไหน) เพราะ LAY-03 ให้ทุกคนดูได้หมดอยู่แล้ว และการ deploy ถูกล็อกที่ Rules แน่นแล้ว | 🟡 **BN มี preview แล้ว** (ผัง 3D v0.14.0 · 20 ส.ค. 69 — ดู Fix Log v2.10.4) เข้าดูโมเดลจริง/วาดโซนอีเวนต์/วางของได้ + มีลิงก์ใน sidebar เว็บหลักแล้ว · **พิกัดแถว/ล็อกจริงของ BG + BN ยังรอ CAD — ไม่ใช่งานโค้ด** |
| ENH-03 | แจ้งเตือน LINE อัตโนมัติ (Messaging API + Apps Script time trigger) — LINE Notify ปิดบริการแล้ว มี.ค. 2025 | รอทำ |
| ENH-04 | ระบบบันทึกยอดผ่านมือถือ แทนกรอก Sheet เอง | แนวคิด |
| ENH-05 ✅ | เทียบเป้า vs ยอดจริง + เตือนเมื่อหลุดเป้า | ✅ เสร็จ v2.6.0 |
| ENH-06 | เป้าแยกวันธรรมดา (จ–พฤ) / วันหยุด (ศ–อา) — เป้าเดียวทำให้วันธรรมดาโดนธงหลุดเป้าเกินจริง | แนวคิด (ต่อยอด ENH-05) |
| ENH-07 | Heatmap ระบายสีตามถึงเป้า/ไม่ถึง (เขียว-แดง) | แนวคิด |
| ENH-08 | ปุ่มคัดลอกสรุปเป้าวันนี้ไปแปะ LINE เอง (สะพานไป ENH-03) | แนวคิด |
| NET-05 | หน่วงเริ่มโหลดแต่ละสาขาห่างกัน 500ms (SS → BG → BN) กัน `verifyToken` ของ 3 script ยิงถาม Firebase ชนกันตอน cold — เสียเวลารวม +1 วิ เฉพาะรอบแรก | รอทำ (เจ้าของสั่งพักไว้ 28 ก.ค.) |
| NET-06 | ขยาย `FETCH_TIMEOUT` 45 → 90 วิ ให้รอบที่ช้ามากรอดไปแทนที่จะ fail แล้ว retry | รอทำ (เจ้าของสั่งพักไว้ 28 ก.ค.) |
| BM-01/02/03 ✅ | Benchmark: กราฟเส้นล็อกเฉลี่ย/วัน รายเดือน เทียบ 3 สาขา + ป้ายเตือนสาขาที่ตกต่อเนื่อง N เดือน | ✅ เสร็จ v2.5.8 |
| FCST-02/03/04 ✅ | หน้าพยากรณ์อิงเดือนปฏิทิน ตัด "30 วันข้างหน้า" | ✅ เสร็จ v2.5.7 |
| SEC-05 ✅ | ปรับความเร็ว/ลดโหลดรวน: `loadAll()` ยิง 6 ท่อพร้อมกัน (Promise.all) + ดึง token ครั้งเดียวใช้ซ้ำ + `verifyToken` cache ฝั่ง Apps Script | ✅ เสร็จ v2.5.2 |
| SEC-06 ✅ | Firestore Security Rules — user อ่านได้แค่ doc ตัวเอง + `allow write: if false` กันยกระดับสิทธิ์เอง (ทดสอบ pentest 🔴→🟢 ผ่าน) | ✅ เสร็จ v2.5.2 |

> **SEC-08 — ทำไมรอบ v2.10.0 ยังไม่ทำ:** เจ้าของประเมินแล้วว่ารับความเสี่ยงได้ เพราะ `data/` เก็บ **จำนวนล็อก/ราย ไม่ใช่เงิน** (เงินคำนวณฝั่งเว็บจากราคา ซึ่ง `showPriceNote` ปิด default) และคุมด้วย**การเลือกว่าจะใส่ `bmBranches` ให้ใคร** ไม่ใช่การซ่อนจาก F12
> **สิ่งที่ต้องรู้:** รอบ refresh 10 นาที **ไม่ได้ลดความเสี่ยงนี้** — ข้อมูลสดขึ้น แต่อ่านผ่าน console ได้ตลอดเวลาเหมือนเดิม
> **ปิดฝั่ง client ไม่ได้:** obfuscate / ลบตัวแปร / ปิด F12 กันได้แค่คนขี้เกียจ ทางเดียวที่ได้ผลจริงคือ**ไม่ส่งข้อมูลที่ไม่ต้องให้เห็นลงมาแต่แรก**

---

## 7. หมายเหตุการทำงาน
- แก้โค้ดใช้ str_replace เท่านั้น ไม่เขียนทับทั้งไฟล์
- ตรวจก่อนแก้เสมอ ถ้าเจอปัญหาอื่นบอกก่อน
- ทุก bug แก้ครอบคลุม ทุก role × ทุกสาขา × ทุกโซน
- ไฟล์ index.html แก้ต่อเนื่องที่ /home/claude ไม่ reset จาก project file

---

