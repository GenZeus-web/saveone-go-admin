- ขึ้นแชทใหม่ทุกครั้ง อ่าน WORKFLOW.md (สถานะล่าสุด+กฎ+Fix Log) ก่อนเริ่มงานเสมอ; ถ้าต้องรู้รายละเอียดระบบค่อยเปิด knowledge/
- โปรเจกต์นี้มี 2 repo ที่ deploy แยกกัน: เว็บหลัก `saveone-go-admin` (โฟลเดอร์ SaveOneGo-v2.x.x) กับ ผังตลาด 3D `3D-Market-SR` (โฟลเดอร์ Market3D-SR) — ชื่อไฟล์ index.html เหมือนกันทั้งคู่ ก่อนแก้ทุกครั้งต้องเช็คว่าเปิดถูกตัว: มี firebaseConfig = เว็บหลัก · มี BRANCH/THREE = ผัง 3D แล้วดูป้ายเวอร์ชันซ้ำอีกชั้น
- ผัง 3D มีเอกสารของตัวเองในโฟลเดอร์ Market3D-SR (7_market3d-logic.md + market3d-redesign-spec.md) แก้ผังแล้วต้องอัปเดตคู่นั้น ไม่ใช่ WORKFLOW.md
- ถามกลับให้ชัดก่อนลงมือทำเสมอ ถ้า request ไม่ชัดพอ
- ถามทีละคำถามเดียว ไม่ถามรัว
- สรุปสิ่งที่จะทำก่อน 1-2 บรรทัด แล้วค่อยทำ
- ถ้าเป็นโค้ด ใช้ str_replace เท่านั้น ห้ามเขียนไฟล์ใหม่ทั้งหมด
- ตอบสั้น ตรงประเด็น ไม่ต้องอธิบายซ้ำสิ่งที่ทำไปแล้ว
- ถ้าเจอ bug หรือปัญหาอื่นระหว่างทาง ให้บอกก่อน อย่าแก้พ่วงโดยไม่บอก
- ภาษาไทยตลอด
- ถ้าผมบอกแค่ปัญหาหรือบอกว่า "ไม่รู้จะทำยังไง" ให้เสนอ 2-3 option มาให้เลือก อย่ารอให้ผมบอกทิศทางก่อน
- จบงาน: bump version (ป้ายหัว index.html + Fix Log ใน WORKFLOW.md) → ถามผมยืนยันว่าเสร็จจริงก่อน → แล้วค่อยสร้าง index เวอร์ชันใหม่ + ย้ายตัวเก่าเข้า archive/ + อัปเดต WORKFLOW.md
