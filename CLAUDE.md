# เซฟวันโก | รายงานการจองตลาด — คู่มือทำงาน

> ไฟล์นี้ถูกอ่านอัตโนมัติทุกครั้งที่เปิด Claude Code ในโปรเจกต์นี้
> เก็บเฉพาะสิ่งที่ **git บอกแทนไม่ได้** — ประวัติการแก้ไขดูที่ `git log` / `CHANGELOG.md`

---

## ภาษาและวิธีคุย

- **ตอบภาษาไทยเสมอ** สั้น ตรงประเด็น ไม่ต้องอ้อม
- เจอปัญหาอื่นระหว่างทาง **บอกก่อน** อย่าแก้พ่วงเงียบๆ
- งานที่อ้างว่า "เสร็จ/ผ่าน" ต้องมีหลักฐานที่ตรวจซ้ำได้ ไม่ใช่ตารางติ๊กถูก

---

## สถาปัตยกรรม (ตั้งแต่ v2.8.0 — ARCH-01)

```
[Google Sheet] --(Apps Script trigger ทุก 10 นาที)--> [Firestore: data/{6 เอกสาร}]
                                                              |
                                                  [เว็บอ่านตรงนี้ < 1 วิ]
```

**Apps Script ไม่อยู่ในเส้นทางที่ผู้ใช้ต้องรอแล้ว** — มันค้าง/พังเมื่อไหร่ เว็บก็ยังเปิดเร็วเท่าเดิม
แค่ข้อมูลเก่าลง (ช้าสุด 10 นาที) · ป้ายบนหัวเว็บบอกเวลาที่ข้อมูลถูกเขียนเสมอ

**ความปลอดภัย:** `apiKey` Firebase ใน `js/firebase/init.js` เป็นค่าสาธารณะตามปกติ ไม่ใช่ความลับ
ด่านจริงคือ Firebase Auth + Firestore Rules · ห้ามเอา service account / private key เข้าโปรเจกต์นี้

---

## โครงสร้างไฟล์ — อ่าน `README.md` ก่อนแก้

⚠️ **ลำดับ `<script>` และ `<link>` ใน `index.html` ห้ามสลับ** — เดิมโค้ดอยู่ไฟล์เดียว
การแยกไฟล์คงลำดับเดิมเป๊ะ เพราะ hoisting ใช้ได้เฉพาะในไฟล์เดียวกัน
มีโค้ดที่รันทันทีตอนโหลด: `TARGETS=loadTargets()` · `Chart.register()` · `startAutoRefresh(5)`
`js/charts.js` อยู่กลางรายการ views **ไม่ใช่ความผิดพลาด** อย่าย้ายขึ้นบน

`js/*.js` เป็น classic script โดยตั้งใจ — `index.html` เรียกผ่าน `onclick="..."` กว่า 60 จุด
ต้องการ global scope · ES module ใช้เฉพาะ `js/firebase/`

---

## กฎการแก้โค้ด

- ใช้ **Edit (str_replace)** ห้ามเขียนทับทั้งไฟล์
- ทุกบั๊กต้องแก้ครอบคลุม **ทุก role × ทุกสาขา (SS/BG/BN) × ทุกโซน (ST/Non)**
  SS ปิดสิ้น ก.ย. 2569 แต่**เก็บไว้ดูย้อนหลัง** (บทเรียน + วิเคราะห์หลายมิติ) — ข้อมูลเก่าต้องเปิดได้ไม่พัง
  ฟีเจอร์ใหม่ไม่ต้องทำให้ SS · ห้ามลบ SS ออกจากเว็บ/ตัวเลือกสาขา
- `node --check` ทุกไฟล์ที่แตะ ก่อนบอกว่าเสร็จ
- งานที่อ้างอิงดีไซน์ **ต้องเทียบ token สี/มุม/ระยะ กับไฟล์ดีไซน์ให้ครบ** ก่อนบอกว่าตรง
- ถ้าดูภาพจริงไม่ได้ ให้บอกตรงๆ

---

## ขั้นตอนจบงาน

1. **แก้ + ทดสอบ** — `node --check` + ทดสอบจริงบนเบราว์เซอร์ถ้าแตะ UI
2. **bump version 2 จุด** — ป้ายหัว `index.html` (ข้างชื่อ "เซฟวันโก") + หัวข้อใหม่ใน `CHANGELOG.md`
3. **commit** — เขียนให้สรุปอัตโนมัติได้ (ดูหัวข้อถัดไป)
4. 🔴 **ถามเจ้าของยืนยันก่อน push เสมอ** — repo นี้เปิด GitHub Pages
   **push ขึ้น `main` = ขึ้นเว็บจริงทันที** ไม่มีขั้นตอน deploy แยก ไม่มีจังหวะให้ทบทวน
   (ต่างจากยุคเก่าที่ต้องอัปไฟล์เองทีละตัว ตอนนั้นมีจังหวะยั้งได้)
5. **เช็คหลังขึ้น** — เปิด https://genzeus-web.github.io/saveone-go-admin/ ดูป้ายเวอร์ชัน
   ถ้าแตะผัง 3D ด้วย (repo `3D-Market-SR` คนละตัว): **อัปผัง 3D ก่อน แล้วค่อยเว็บหลัก**
   ไม่งั้นปุ่มในเว็บหลักจะพาไปหน้าผังตัวเก่า

---

## Commit message = แหล่งข้อมูลสรุปรายสัปดาห์

ประวัติการแก้ไขไม่ได้เขียนมือแล้ว — สรุปทุกอย่างมาจาก `git log`
**คุณภาพสรุป = คุณภาพ commit message** เขียนแบบนี้:

```
<หัวข้อสั้น อังกฤษหรือไทยก็ได้> (<รหัสงาน เช่น CHT-02>)

<ปัญหาคืออะไร ทำไมถึงเกิด>
<แก้ยังไง>
<ทดสอบแล้วได้ผลยังไง — ตัวเลขจริง>
```

รหัสงานใช้ต่อจากของเดิม: `PERF` `NET` `CHT` `SEC` `PERM` `UI` `LAY` `BM` `FCST` `ENH` `ARCH` `RWD` `CACHE` `TREND`

**รายงานประจำสัปดาห์ใช้เลขเวอร์ชันเดียว** — รวมทุกอย่างที่ออกในสัปดาห์ไว้ใต้หัวข้อเดียวใน `CHANGELOG.md`
(ตัวอย่าง v3.0.0 = สรุป 20–24 ก.ย. รวม v2.11.2–v2.12.0) รายละเอียดเวอร์ชันย่อยเก็บไว้ข้างล่างตามเดิม ไม่ลบ
**เลขหลักแรก** = ผู้ใช้เห็นการเปลี่ยนแปลงใหญ่ที่ต้องปรับตัว (หน้าตา · วิธีอ่าน · ที่มาข้อมูล) ไม่ใช่ "แก้เยอะ"

---

## สาขาและสิทธิ์

| รหัส | สาขา |
|---|---|
| SS | ศรีสมาน (ปิดสิ้น ก.ย. 2569 · เก็บข้อมูลไว้ดูย้อนหลัง) |
| BG | ประตูกรุงเทพ |
| BN | บางนา |

- `branches` = สาขาที่ดูแล (เห็นข้อมูลเต็ม) · `bmBranches` = สาขาที่เทียบได้ใน Benchmark (คนละแกน)
- สิทธิ์รายฟีเจอร์ 12 ตัว คุมในหน้าจัดการผู้ใช้ (admin เท่านั้น) — **ด่านจริงคือ Firestore Rules ไม่ใช่การซ่อนปุ่ม**

---

## ของที่ deploy แยก repo

| repo | เนื้อหา |
|---|---|
| `saveone-go-admin` | เว็บหลัก (repo นี้) |
| `3D-Market-SR` | ผังตลาด 3D · เว็บหลักลิงก์ไปที่ `/3D-Market-SR/?branch=SS` และ `?branch=BN` |

> `SR` ในชื่อ repo = ชื่อเดิมตอนมีสาขาเดียว **ไม่ได้แปลว่ารองรับแค่ศรีสมาน** ไฟล์เดียวคุมทุกสาขา

---

## บทเรียนที่ต้องไม่ทำซ้ำ

- **"วัดผ่าน" ไม่เท่ากับ "ตรงดีไซน์"** (v2.11.0) — วัดค่าจาก DOM ผ่านหมด แต่เจ้าของเปิดมือถือจริงแล้วบอกว่าไม่เหมือนที่อยากได้ เพราะสิ่งที่วัดได้คือ*กลไก* ไม่ใช่*หน้าตา* · เทียบ token ทีหลังพบว่าตรง 0 ตัว
- **ลืมอัป asset ใหม่** (v2.10.4 `model-bn/` · v2.11.0 `apple-touch-icon.png`) — รอบไหนเพิ่มไฟล์ใหม่ ต้องจดไว้ในรายการ deploy ทุกครั้ง
- **บั๊กที่ console ไม่บอก** (CHT-02) — error จากสคริปต์ CDN ถูกเบราว์เซอร์ปิดบังเหลือ `"Script error."` ถ้าสงสัยว่ามี error ซ่อนอยู่ ให้เทียบกับเวอร์ชันเก่าแบบ A/B แทนการเดา

---

## เหลือทำเองนอกโค้ด (ไม่ด่วน)

1. **ติ๊ก `showRawData` ออก** ให้ manager ที่มีอยู่ก่อน (ตอนนี้บัญชีทดสอบ manager 1 บัญชี — ดูชื่อในหน้าจัดการผู้ใช้) — คีย์นี้อ่านด้วย `!== false` การเปลี่ยนค่าตั้งต้นมีผลกับผู้ใช้ที่สร้างใหม่เท่านั้น
2. **ใส่ `bmBranches`** ให้ user ที่ต้องการเทียบสาขา — ไม่ใส่ = เห็นแค่สาขาตัวเอง ไม่พัง
3. **ตัดสินใจเรื่องความสูงเสากล้องรอบนอก** (ผัง 3D) ว่าจะให้ตามผังกลางไหม

---

## สถานะล่าสุด

<!-- ส่วนที่เหลือปล่อยให้ git log เล่าเอง -->

- **เว็บจริง:** https://genzeus-web.github.io/saveone-go-admin/
- **เวอร์ชันบนเว็บจริง = ในโค้ด:** v3.0.0 (push 24 ก.ย. 2569 20:11 · `e822198` · ป้ายหัวเว็บขึ้น v3.0.0 แล้ว)
  login โบเก้ · ธีมตลาดกลางคืน · ป้ายทิศทาง · CHT-04 · ป๊อปอัปยอดวันนี้ (TODAY-01) · ตัวอักษร 3 ระดับ (FONT-01)
  branch `ui/dashboard-trend` merge เข้า `main` แล้ว
- **GitGuardian เตือน Google API Key (24 ก.ย.)** = Firebase apiKey ใน `docs/3_apps-script-code.md:188` → false positive
  ไม่ต้อง rotate · แผนป้องกัน (จำกัด API ของ key · กฎ `settingsLog`) นัดคุย 25 ก.ย.
- **ผัง 3D:** v0.14.0 (20 ส.ค. 2569)
- **Firestore Rules:** เพิ่มกฎ `settings/*` + `settingsLog` แล้ว (Publish 23 ก.ย. 2569 18:40) — ตัวกฎอยู่ที่ `docs/firestore-rules-settings.md`
  กฎจริงอยู่ใน Console ไม่ได้อยู่ใน repo · **ฟีเจอร์ที่อ่าน/เขียน Firestore ใหม่ ต้องวางกฎก่อน push เสมอ** (ท้ายกฎมี deny-all)
- **ราคาไม่ได้อยู่ในโค้ดแล้ว** — แก้ที่เมนูโปรไฟล์ → ⚙️ ตั้งค่า (admin) · ค่าใน `DEFAULT_PRICING` (utils.js) ใช้แค่ตอนยังไม่เคยบันทึก
  ราคามีวันเริ่มใช้ วันเก่าไม่ถูกคิดใหม่ (ADR 0001) · เทสต์ `node --test tests/`

---

## Agent skills

### Issue tracker

เก็บเป็นไฟล์ markdown ใต้ `.scratch/<feature>/` ในเครื่อง (ไม่ใช้ GitHub Issues). See `docs/agents/issue-tracker.md`.

### Triage labels

ใช้ชื่อ label ค่าตั้งต้น 5 ตัว (`needs-triage` `needs-info` `ready-for-agent` `ready-for-human` `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

single-context — `CONTEXT.md` + `docs/adr/` ที่ root (สร้างเมื่อจำเป็น). See `docs/agents/domain.md`.
