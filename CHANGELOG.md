# CHANGELOG — เซฟวันโก รายงานการจองตลาด

> ย้ายมาจากคอมเมนต์หัวไฟล์ `index.html` (บรรทัด 4–176 ของเวอร์ชันก่อนแยกไฟล์)
> เนื้อหาคงเดิมทุกตัวอักษร เปลี่ยนแค่รูปแบบให้เป็น Markdown

---

## v3.0.1 (25 ก.ย. 2569) — โลโก้หน้าโหลดไม่แวบตอน F5 · โลโก้รถเป็นป้ายไฟบนหน้า login

    LOGO-3D) หน้า login: เพิ่มโลโก้รถ (assets/logo.png) เหนือ "เซฟวัน GO" เป็นป้ายไฟนีออนลอยเหนือการ์ด
       เดิมรูปนี้อยู่แค่หน้า splash — หลัง SPLASH-01 ผู้ใช้ประจำแทบไม่เห็นแล้ว
       CSS ล้วน ไม่เพิ่มไลบรารี · ลอยโยก 5 วิ + กะพริบเบาๆ ทุก 7 วิ · ลดการเคลื่อนไหว = นิ่ง · มือถือ 150→124px
       ไฟล์มีขอบม่วงจากการตัดพื้น → filter บังคับสีเดียว · เลือกแสงระดับกลางจาก 3 ระดับ
       (แสงขาววงในถมช่องจักรยาน/คนขับจนอ่านไม่ออก จึงตัดออก)
    LOGO-TXT) หน้า login: ตัวอักษร "เซฟวัน GO" (HTML) → โลโก้ตัวอักษรของบริษัท "ตลาด เซฟวัน | GO"
       ไฟล์ใหม่ assets/logo-wordmark.svg (15 KB) ลากเส้นจาก LoGosaveonego.png ที่เจ้าของให้
       ต้นฉบับพื้นดำทึบ 447×447 ตัวอักษรกว้างแค่ 355px → เบลอบนจอ Retina
       ทำ: ตัดขอบ → แยกชั้นขาว/เขียว #84e62d → ขยาย 8 เท่า + เกลาขอบ → potrace เป็นเวกเตอร์ (คมทุกขนาด)
       ⚠️ deploy: ต้องมี assets/logo-wordmark.svg ขึ้นไปด้วย ไม่งั้นหน้า login ไม่มีชื่อแบรนด์
    LOGO-BR) หัวรายงาน "รายงาน เซฟวันโก · บางนา · [BN]" → โลโก้บริษัท + | + ชื่อสาขา (ตามที่เจ้าของเลือก)
       ทุกสาขาต่อชื่อ: ประตูกรุงเทพ · บางนา · ศรีสมาน (SS ข้อมูลย้อนหลังยังเปิดได้)
       ขนาด: โลโก้สูง 29px · ชื่อสาขา 27px (มือถือ 24/22px)
       ตัดป้าย BN/BG/SS ออก · ชื่อสาขาสูงเท่า "เซฟวัน" ในโลโก้ · aria-label ยังอ่านว่า "รายงาน … สาขา"
       ใช้ <svg><use> อ้างไฟล์โลโก้ → สีขาวเปลี่ยนตามธีม (--wm-ink) · โหมดสว่าง GO เขียวเข้มขึ้น #4f9e12
       หน้า login ยังเป็น <img> ขาว/เขียวเดิม (ค่าสำรองใน var)
    LOGO-HDR) หัวเว็บ: ป้าย "SG" ที่วาดด้วย CSS → ป้ายไฟรถ (assets/logo.png) ในกรอบมุมมน 48×34 ต่อเนื่องจากหน้า login
       เลือกจาก 4 แบบ (ป้ายไฟรถ · ป้าย GO · ตัวหนังสือล้วน · เส้นรถ) — โลโก้ตัวอักษรอยู่ที่หัวรายงานที่เดียว ไม่ซ้ำ
       ป้ายเวอร์ชันเป็นเม็ดยามีขอบ · โหมดสว่าง = รถสีเข้มไม่เรือง · จอ ≤420px กรอบ 40×30 ไม่ดันปุ่มขวาล้น
    LAY-05) ป๊อปอัป 5 ตัวโดนหัวเว็บบังหัวกล่อง + เลื่อนแล้วข้างหลังเลื่อนตาม (เหลือจาก TODAY-01 fix ที่แก้แค่ยอดวันนี้)
       กล่องวัน (กดจุดบนกราฟ) · ตั้งค่าราคา · ยืนยันรหัสผ่าน · จัดการผู้ใช้ · เพิ่มผู้ใช้
       แก้: ui.js เฝ้า style ของป๊อปอัปทั้ง 6 ตัว (MutationObserver) → เปิด ≥1 ตัว = body.modal-open · ปิดครบ = ปลด
       ไม่แตะฟังก์ชันเปิด/ปิดเดิม → ครอบทุกทาง (ปุ่ม ✕ · Esc · คลิกพื้นหลัง)
       ทดสอบใน Chrome 13 ขั้น ถูกทุกขั้น รวมป๊อปอัปซ้อน (ยืนยันรหัสบนตั้งค่า · เพิ่มผู้ใช้บนรายชื่อ · ยอดวันนี้ปิดขณะตั้งค่าเปิด)

    SPLASH-01) กด F5 / เปิดเว็บซ้ำ: โลโก้เซฟวันโกโผล่แวบแล้วหาย (จอกะพริบ)
       สาเหตุ: splash ตั้งจังหวะไว้ 0.3–1.5 วิ (ยุครอ Apps Script 5–10 วิ) แต่มี cache แล้ว loadAll
       โชว์ข้อมูลทันทีและสั่งซ่อน splash → เล่นไม่ทันจบ เหลือแค่แวบเดียว
       แก้: showSplash() ข้ามไปเลยถ้ามี cache ใช้ได้ · เครื่องใหม่/cache หมดอายุ ยังเห็นโลโก้ตามเดิม

## v3.0.0 (24 ก.ย. 2569) — 🌙 โฉมใหม่ทั้งเว็บ + ข้อมูลเข้าเองอัตโนมัติ · สรุปรวมสัปดาห์ 20–24 ก.ย.

    📌 รายงานประจำสัปดาห์ใช้เลขนี้เลขเดียว — รวมทุกอย่างที่ออกในสัปดาห์
       (v2.11.2 · v2.11.3 · v2.11.4 · v2.11.5 · v2.12.0 + รอบนี้) รายละเอียดแต่ละตัวอยู่ข้างล่างตามเดิม
    เกณฑ์เลขหลักแรก (2 → 3): ผู้ใช้เห็นการเปลี่ยนแปลงใหญ่ที่ต้องปรับตัว
       (หน้าตา · วิธีอ่านหน้าหลัก · ที่มาของข้อมูล) ไม่ใช่ "แก้เยอะ" · ใช้เกณฑ์นี้ต่อจากนี้

    ── ใหม่ในรอบนี้ ──
    SAFE-01) iPhone 17 Pro Max เปิดจากหน้าจอโฮม: หัวเว็บทับ นาฬิกา/WiFi/แบต/กล้อง · ปุ่ม ☰ กดไม่ค่อยโดน
       สาเหตุ: กฎเว้นแถบสถานะ (env safe-area) อยู่ต้น responsive.css แต่ `.header{padding:0 10px}`
       ในบล็อก ≤768px มาทีหลัง → shorthand ล้าง padding-top เป็น 0 (เป็นมาตั้งแต่ v2.11.0)
       แก้: ย้ายกฎไปท้ายไฟล์ · ป๊อปอัปเว้นแถบสถานะ/แถบโฮม · ☰ 38→44px
    TODAY-01 fix) ป๊อปอัปยอดวันนี้: หัวเว็บบังหัวการ์ด + ข้างหลังเลื่อนตามได้
       night.css `.layout{z-index:1}` ขังป๊อปอัปไว้ใต้หัวเว็บ (z 10) → เปิดป๊อปอัป = ยก .layout + ล็อก .content
       ตัวอักษรใหญ่: zoom คูณ max-height การ์ด (86vh×1.3) → หารกลับ + หักแถบสถานะ
    ทดสอบ: เดสก์ท็อป ภาพ A/B หัวเว็บบัง/ไม่บัง · การ์ดอยู่ในจอทั้ง 3 ระดับ · หัวเว็บเดสก์ท็อปคงเดิม 50px
       ⚠️ แถบสถานะ iPhone จำลองบนเดสก์ท็อปไม่ได้ (env = 0) — ต้องเช็คบนเครื่องจริง
    TODAY-01) ป๊อปอัป "ยอดวันนี้" สำหรับ ผอ. — เด้งครั้งแรกของวัน (ต่อเครื่อง) + ปุ่ม 📅 บนหัวเว็บ
       ทีละสาขา BG/BN (ไม่ทำให้ SS): ST / Car มีการ์ดใหญ่ ล็อก|ราย ของโซนตัวเอง
       ใต้การ์ด ออนไลน์ · วอล์กอิน (ราย / ล็อก) · รายรับ อาหาร/Car/รวม · ค่าไฟ · ป้ายวันฝน/วันหยุด
       ไม่เอาไม่มา/ลามาหัก (ผลบวกแถว = การ์ดใหญ่) — กล่องกดจุดบนกราฟยังแสดงสุทธิ + ไม่มา/ลา ตามเดิม
       ก่อนยอดวันนี้เข้า (15:00) โชว์ยอดปิดวันล่าสุด + "กลับมาดูใหม่ช่วง 15:00–16:00"
       รายรับ/ค่าไฟ ซ่อนตามสิทธิ์ผ่าน col-rev/col-elec เดิม
    FONT-01) ขนาดตัวอักษร 3 ระดับ ปกติ/ใหญ่/ใหญ่มาก (100/115/130%) ในเมนูโปรไฟล์ จำค่าต่อเครื่อง
       ใช้ zoom เพราะ CSS เป็น px ทั้งเว็บ · ไม่ซูมหัวเว็บ/sidebar/หน้า admin · กราฟคูณ devicePixelRatio กันเบลอ
    UI-HDR) หัวเว็บล้นบนแท็บเล็ต: จอ ≤1100px ปุ่ม วันนี้/ธีม/รีเฟรช เหลือไอคอน ซ่อนอีเมลข้างรูปโปรไฟล์
    UI-NIGHT) มือถือ: การ์ด KPI ใบหลัก 2 ใบเต็มแถว ใบรองแถวละ 2 (เดิม 1 ใบ/แถว เลื่อนหลายจอกว่าถึงกราฟ)
    UI-3D) หน้า login: ไฟตลาดนัดยามค่ำแบบโบเก้ (WebGL shader ไม่มีไลบรารี)
       กดเข้าสู่ระบบ = ฉากเริ่มเร่ง · รหัสผ่าน = พุ่งเข้าแสงทันที คู่ขนานกับการอ่านสิทธิ์
       (เดิมหน้าจอค้างระหว่างรอ Firestore) · ต่อกับหน้าโหลดเนียน
       หยุดวาดเมื่อเข้า dashboard · ไม่มี WebGL = พื้นไล่สีธรรมดา · ลดการเคลื่อนไหว = ภาพนิ่ง
    UI-NIGHT) ธีมตลาดกลางคืน: มืด = ดำอมน้ำตาลอุ่น · สว่าง = ครีม · หัวเว็บกระจกฝ้า
       การ์ดตัวเลข "สีต้องมีความหมาย" — ตัวเลขสีเดียวทุกใบ ใบหลัก 2 ใบเรืองเขียว
       แถบแดงหลุดเป้าจึงเด่นตัวเดียว · การ์ดลอยขึ้นทีละใบหลังหน้าโหลด
       ทั้งหมดอยู่ใน css/night.css — ลบ <link> บรรทัดเดียว = หน้าตาเดิม (ทั้งเดสก์ท็อปและมือถือ)
       มือถือใช้ธีมเดียวกัน: เปลี่ยนสี/พื้นผิว แต่คงขนาด ระยะ แท็บทรงเม็ดยา และ drawer จากดีไซน์ v2.11
    TREND-01) ป้ายทิศทางตอนนี้บนการ์ดรายรับ / จำนวนล็อก / จำนวนราย
       ↗ กำลังขึ้น · → ทรงตัว (±2%) · ↘ กำลังลง + ตัวเลขที่มา (เฉลี่ย/วัน 7 วันล่าสุด vs 7 วันก่อนหน้า)
       ค่าน้อยกว่า 20/วัน ใช้จำนวน/วันแทน % · ข้อมูลไม่ถึง 4 วัน = ไม่สรุป
       กราฟหลักคงเดิม (ผอ. ดูกราฟนี้เป็นหลัก) · node --test tests/ 18/18
    CHT-04) แกน X/Y + legend ของกราฟเส้นทุกหน้าหายจนกว่าจะเอาเมาส์ชี้
       สาเหตุ: plugin วาดเส้นซ้าย→ขวา ตัดขอบทั้งกราฟ แล้วไม่วาดใหม่ตอนจบ
       แก้: ตัดเฉพาะตอนวาดข้อมูล + วาดใหม่ตอนจบ · legend HTML ที่ซ้ำซ่อนแล้ว · แกน Y ปัดเป็นเลขกลม

    ── ระบบดึงข้อมูลอัตโนมัติ (เปิดใช้แล้วก่อนรอบนี้ · ไม่ได้มากับ push นี้) ──
    FETCH) Apps Script ดึงยอดจากเว็บจองเข้า Google Sheet เอง BG + BN วันละ 3 รอบ (15/18/22 น.)
       + รอบเช้าเก็บตกยอดเมื่อวาน (FETCH-04) · หน้ารายงานเปลี่ยน/คอลัมน์หาย = หยุดก่อนเขียน
       แล้วอีเมลแจ้ง (FETCH-GUARD · FETCH-02) · ล็อกนอกโซนอาหาร/รถ = อีเมลแจ้ง (FETCH-01)
       รองรับหน้ารายงานวันฝน (FETCH-05/06) · ผ่านจริงครบ ตรงรายงานสาขา 23 ก.ย.
       โค้ดอยู่ใน tools/fetch-reservation/ รันบน Apps Script แยก

    ── ออกไปแล้วในสัปดาห์ (สรุป) ──
    v2.12.0 SET-01) หน้าตั้งค่าราคา · ฤดูกาล · เป้ายอด แก้ได้เองจากเว็บ (admin) · ราคามีวันเริ่มใช้
    v2.11.5 PRICE-01) Benchmark คิดรายรับสาขาอื่นด้วยราคาของสาขานั้น
    v2.11.4 DAY-01) กดจุดบนกราฟ ดูรายละเอียดวันนั้น (ใช้บนมือถือได้)
    v2.11.3 CHT-03) โหมดสว่าง เส้นกราฟไม่จมหาย
    v2.11.2 ARCH-02) แยก index.html เป็นไฟล์ย่อย + แก้ error ตอนเปลี่ยนตัวกรองรัวๆ

    ไฟล์ใหม่ที่ต้อง deploy: js/login-fx.js · js/trend.js · css/night.css
    ไม่ต้องแก้ Firestore Rules (ไม่มีการอ่าน/เขียนใหม่)

---

## v2.12.0 (23 ก.ย. 2569) — ⚙️ หน้าตั้งค่า: แก้ราคา · ฤดูกาล · เป้ายอด ได้เองจากหน้าเว็บ

    SET-01) ราคาเขียนตายไว้ในโค้ด 5 ที่ + หน้าหมายเหตุราคาใน HTML ขึ้นราคาทีต้องแก้โค้ด
       และเป้ายอดเก็บในเบราว์เซอร์ใครเบราว์เซอร์มัน (คุณตั้ง คนอื่นไม่เห็น)
       ทำ: เมนูโปรไฟล์ → ⚙️ ตั้งค่า (admin เท่านั้น) 4 แท็บ
         💰 ราคา    สาขา × โซน × ฤดูกาล × ธรรมดา/ศ–อา × ออนไลน์/วอล์กอิน (ST แยกฤดูกาลได้แล้ว)
         📅 ฤดูกาล  ช่วงวันที่วนทุกปี แยกสาขา เพิ่มได้หลายชุด · ทับกัน = ช่วงสั้นกว่าชนะ
         🎯 เป้ายอด ใช้ร่วมกันทุกคน มีผลทันที ไม่ต้องใส่รหัส (ช่องในไซด์บาร์ = admin แก้ได้)
         🗂 ประวัติ  ใคร · แก้อะไร จาก→เป็น · เมื่อไหร่ · อุปกรณ์/เบราว์เซอร์ — เห็นเฉพาะ anansit@saveone.go

       กติกา (ADR 0001): ราคาเป็น "ชุด" ที่มีวันเริ่มใช้ เร็วสุดพรุ่งนี้
         วันก่อนวันเริ่มใช้คิดราคาเดิมทุกบาท → ยอดที่รายงานไปแล้วไม่เปลี่ยน
         บันทึก/ยกเลิก ต้องใส่รหัสผ่านทุกครั้ง · หน้ายืนยันบอกว่าเปลี่ยนอะไร มีผลเมื่อไหร่
         เปลี่ยนเกิน 30% ขึ้นเตือนแดง (กันพิมพ์ 300 เป็น 3000)
         ชุดที่รอมีผล ยกเลิกได้ถึง 23:59 ก่อนวันเริ่มใช้ · ตั้งล่วงหน้าได้หลายชุด
         โหลดราคาไม่ได้ + เครื่องนี้ไม่เคยโหลดได้ = ซ่อนรายรับ (ยอดล็อกยังดูได้)
       หน้าหมายเหตุราคาสร้างจากชุดราคาจริง + บอกราคาที่รอมีผล
       What-If / วอล์กอิน Car ใช้ราคาจากตาราง (ไม่ผูก +50 ในโค้ดแล้ว)

       ค่าตั้งต้น = ราคาปัจจุบันทุกช่อง → วันแรกตัวเลขบนเว็บเท่าเดิม
       ทดสอบ: ตัวเลขเดิม 480/480 กรณี · ส่งสาขาเอง 960/960
              node --test tests/ 12/12 (วันเริ่มใช้ · หลายชุด · ยกเลิก · เลื่อนฤดูกาล · ทับกัน)
              หน้าตั้งค่าบน Chrome headless (Firebase จำลอง) 36/36 — รหัสผิดไม่บันทึก,
              บันทึก/ยกเลิก/ประวัติ/เป้ายอด/manager เข้าไม่ได้/โหลดไม่ได้ซ่อนรายรับ
       ⚠️ ต้องวางกฎ Firestore เพิ่มก่อนใช้ — docs/firestore-rules-settings.md
       ไฟล์ใหม่: js/firebase/settings.js · docs/firestore-rules-settings.md

---

## v2.11.5 (23 ก.ย. 2569) — 💰 Benchmark: รายรับสาขาอื่นคิดด้วยราคาของสาขานั้นแล้ว

    PRICE-01) รายรับในการ์ด Benchmark คิดราคาตาม "สาขาที่เปิดดูอยู่" ไม่ใช่สาขาของข้อมูล
       เพราะ revST/revNon/calcDiscount อ่าน activeBranch ตรงๆ ไม่มีใครส่งสาขาเข้าไป
       ตัวอย่าง BG เสาร์ 19 ก.ย.: เปิดดู BG = 75,700 ฿ ✅ · เปิดดู SS = 45,500 ฿ ❌
       (รถ BG ถูกคิดราคา Non ของ SS ล็อกละ 30 แทน 250–350) · ตัวเลขของ SS เองถูกมาตลอด
       แก้: ฟังก์ชันราคาทุกตัวรับสาขา (br) ได้ ค่าตั้งต้น = activeBranch
            Benchmark ส่ง b ของการ์ดนั้นเอง · หน้าอื่นเรียกแบบเดิม ไม่ต้องแก้

       ย้ายกติการาคาที่ก็อปซ้ำ 5 ที่มาไว้ที่ utils.js ที่เดียว:
         - ฤดูกาลแยกเป็น HIGH_SEASON_MONTHS / seasonOf() (high = พ.ย.–ก.พ.)
         - ตารางราคา Car NON_PRICE[สาขา][season][วันธรรมดา, ศ–อา]
         - lockPrices() ราคาป้ายออนไลน์/วอล์กอิน · rainHalvesWalkIn() กติกาวันฝน
         raw.js · overview.js (ตารางวันฝน + กล่องรายวัน) · ui.js (What-If) เรียกตัวกลางแทน

       ทดสอบ: ตัวเลขเดิมตรง 480/480 กรณี (3 สาขา × 8 วัน × วันฝน × 5 กลุ่ม)
              ส่งสาขาเอง 960/960 ตรง · What-If 24/24 ตรง
              เพิ่ม tests/pricing.test.js (เทสต์ตัวแรกของโปรเจกต์) รัน node --test tests/ ผ่าน 7/7

---

## v2.11.4 (22 ก.ย. 2569) — 👆 กดจุดบนกราฟ ดูรายละเอียดของวันนั้นได้

    DAY-01) เห็นกราฟพุ่ง/ดิ่ง คำถามถัดไปคือ "วันนั้นเกิดอะไรขึ้น"
       เดิมต้องจำวันที่ไปไล่หาแถวในแท็บข้อมูลดิบ ขาดจังหวะ
       และบนมือถือไม่มี hover เลย tooltip เดิมจึงใช้ไม่ได้ตั้งแต่แรก
       แก้: กดที่ไหนก็ได้ในคอลัมน์ของวันนั้น → เด้งกล่องรายละเอียด
            ใช้ interaction:{intersect:false} ที่ตั้งไว้อยู่แล้ว
            ไม่ต้องกดโดนจุดเป๊ะ — สำคัญกับมือถือเพราะจุดเล็กเกินกว่าจะแตะโดน
       ปิดด้วย ✕ · กดนอกกล่อง · Esc

       ทุกสูตรใน openDayModal() copy มาจาก renderRaw() ตรงๆ ไม่คิดใหม่
       ไม่งั้นเลขในกล่องกับในตารางข้อมูลดิบจะไม่ตรงกัน
       รวมถึงเคสยิบย่อยอย่าง cancelShow/absentShow ที่เปลี่ยนตามโซน
       และราคาวันฝนที่หารครึ่งไม่เหมือนกันระหว่าง SS กับสาขาอื่น

       สิทธิ์: เงิน/ค่าไฟ ใส่คลาส col-rev / col-elec เหมือนตารางข้อมูลดิบ
       body.no-rev / body.no-elec ใน base.css ซ่อนให้เองตามสิทธิ์
       ไม่เช็ค userPerms ซ้ำใน JS เพื่อไม่ให้มีสองแหล่งความจริง

    DAY-02) รอบแรกทำออกมาแล้วเจ้าของบอก "สีมันเหมือนกัน ดูแล้วงง ต้องเพ่ง"
       เพราะตัวเลขทุกบรรทัดเป็น var(--ink) สีเดียวกันหมด กวาดตาแล้วแยกไม่ออก
       แก้โดยยืมภาษาสีที่แอปใช้อยู่แล้ว ผู้ใช้ไม่ต้องเรียนรู้ชุดใหม่:
         ทอง=ล็อก/ST · เขียว=ราย/รายรับ · ม่วง=Car-Non · แดง=ไม่มา/ลา · ส้ม=ค่าไฟ
       - แต่ละหมวดมีไอคอน + แถบสีซ้าย กวาดตาหาได้ไม่ต้องอ่านทุกบรรทัด
       - แยกหน่วยออกจากตัวเลข (สีจาง) ตัวเลขเด่นขึ้นโดยไม่ต้องขยายฟอนต์
       - สถานะวันเปลี่ยนจากข้อความเป็นชิปสี · รายรับรวมตัวใหญ่กว่าบรรทัดอื่น

---

## v2.11.3 (22 ก.ย. 2569) — 🎨 โหมดสว่าง: เส้นกราฟไม่จมหายอีก

    CHT-03) v2.5.4 แก้ tick/เส้นกริด/legend ให้เปลี่ยนตามธีมแล้ว
       แต่ "สีเส้นข้อมูล" ไม่ได้แก้ — BM_COLORS/MONTH_COLORS เป็นค่าคงที่
       ใช้สีเดียวกันทั้งสองโหมด = แก้ไปครึ่งเดียว
       วัดด้วยเครื่องตรวจ palette: โหมดสว่างทั้ง 3 สาขาคอนทราสต์กับพื้น #f5f6fa
       ได้แค่ 1.74 / 1.93 / 2.55 (เกณฑ์อ่านออกคือ >= 3.0) → เส้นจมไปกับพื้น
       แก้: ตาราง SERIES_LIGHT + sc() ใน charts.js จับคู่สีมืด→สีสว่างที่เฉดเดิม
            แต่เข้มพอ · scDeep() แปลงทั้ง config ที่ชั้น makeChart() จุดเดียว
            ไฟล์ views ทั้ง 5 ไม่ต้องแก้ (47 จุดที่ hardcode สีไว้)
       ผลตรวจใหม่ โหมดสว่าง: ผ่านครบทุกเกณฑ์ (ความสว่าง · อิ่มสี · คอนทราสต์
            >= 3:1 ทุกสี · แยกออกด้วยตาปกติทุกคู่ติดกัน)
       โหมดมืด: คืนค่าเดิมทุกตัว ยืนยันแล้วว่าไม่มีอะไรเปลี่ยน

    CHT-04) สีชุด 8 สี (หน้าเปรียบเทียบเดือน) แก้ 2 อย่าง
       - แดงกับเขียวเคยอยู่ลำดับ 3-4 ติดกัน คนตาบอดสีแยกไม่ออก → คั่นด้วยม่วง
       - rgba(100,200,180) อิ่มสี 0.099 ต่ำกว่าเกณฑ์ อ่านเป็นสีเทา
         → เปลี่ยนเป็น rgba(45,212,191)

    * ยังไม่ได้แก้: เลือกเกิน 8 เดือน สีจะวนซ้ำ (เดือนที่ 9 สีเดียวกับเดือนที่ 1)
      ต้องตัดสินใจเรื่องวิธีแสดงผลก่อน ยังไม่แตะรอบนี้

    KPI-01) การ์ด KPI ทั้งสองแถวเรียงตาม "ลำดับการคำนวณ" ไม่ใช่ "ความสำคัญ"
       - รายรับ: ตัวเลขสรุปอยู่ลำดับ 4 จาก 5 ต้องแปะป้าย "ตัวเลขสรุป" ช่วย
       - สถิติ: จำนวนล็อก (ตัวที่ใช้ตั้งเป้า) อยู่ลำดับ 6 สุดท้าย
         ต้องอ่านเลข "ปัญหา" 4 ตัวก่อนถึงจะเห็นยอดจริง
       แก้: เรียงผลลัพธ์ขึ้นก่อน + ให้ตัวเลขสรุปของแต่ละแถวกินกว้าง 2 ช่อง
            เลข 22px -> 30px · ถอดป้าย "ตัวเลขสรุป" ออกเพราะขนาดบอกแล้ว
            เดิมมีตัวเด่นแค่แถวรายรับ แถวสถิติไม่มีเลย ไม่สมมาตร
       จอ <=560px คืนเป็น 1 ช่อง ไม่งั้นการ์ดเดียวกินทั้งแถว
       * ไม่ยุบสองแถวรวมกัน เพราะ revSection ถูกซ่อนทั้งก้อนตามสิทธิ์ showRevenue
       * เคยทำแบบพับรายละเอียดเก็บไว้ด้วย (branch kpi/c-grouped) เจ้าของเลือกแบบนี้แทน

    KPI-02) การ์ดรายรับมี 2 ใบ "สุทธิรวม (ST+Non)" กับ "สุทธิ ST" ซึ่งซ้ำซ้อน
       เพราะ applyZone() ล้างยอดโซนที่ไม่ได้เลือกเป็น 0 อยู่แล้ว
       - เลือกโซน ST  -> ทั้งสองใบโชว์เลขเดียวกัน
       - เลือกโซน Car -> ใบ ST โชว์ 0 เปล่าๆ
       แก้: ยุบเหลือใบเดียว เปลี่ยนแค่ป้ายตามโซน
            ทั้งหมด -> รายรับสุทธิรวม (ST+Non) หรือ (ST+Car) ตามสาขา
            ST      -> รายรับสุทธิ ST
            Car/Non -> รายรับสุทธิ Car หรือ Non ตามสาขา
       ค่าที่แสดงไม่ต้องแยกเคส revSTsum+revNonSum ถูกต้องทั้ง 3 กรณีอยู่แล้ว
       ผลพลอยได้: โหมด "ทั้งหมด" ของ BG/BN เคยเขียน (ST+Non) ซึ่งผิด
            เพราะสองสาขานั้นเรียกโซนว่า Car — ตอนนี้ตามสาขาถูกต้องแล้ว

---

## v2.11.2 (20 ก.ย. 2569) — แยกไฟล์ + แก้ error ตอนเปลี่ยนตัวกรองรัวๆ

    ARCH-02) แยก index.html (4,131 บรรทัด) เป็น css/ 5 ไฟล์ · js/ 15 ไฟล์
       · js/firebase/ 3 ไฟล์ (ES module) · assets/ เหลือ index.html 878 บรรทัด
       ลำดับ <script> คงเดิมเป๊ะ เพราะ hoisting ใช้ได้เฉพาะในไฟล์เดียวกัน
       (รายละเอียดใน README.md) · พิสูจน์ว่าไม่มีอะไรเปลี่ยนด้วยการต่อไฟล์
       กลับมา diff เทียบของเดิมให้ตรงทุกไบต์

    CHT-02) กราฟ reveal (animateChartReveal) จอง requestAnimationFrame ไว้ 1.2 วิ
       แต่ dChart() ทำลายกราฟโดยไม่ยกเลิก callback ที่ค้างอยู่
       → callback ตื่นมาเรียก chart.draw() กับกราฟที่ ctx/canvas เป็น null แล้ว
       โยน error ทุกครั้งที่เปลี่ยนตัวกรองก่อน animation จบ
       (มองไม่เห็นใน console เพราะ Chart.js เป็น cross-origin จึงขึ้นแค่ "Script error.")
       แก้: เก็บ id ไว้ที่ chart._revealRAF แล้ว cancelAnimationFrame ก่อน destroy
            + กันอีกชั้นใน step() ถ้า ctx/canvas หายหรือหลุด DOM ให้หยุดเงียบๆ
       ผล: กดสลับสาขา/โซน/ช่วงเวลารัว 24 รอบ (ห่าง 100ms) error 0 · ไม่มี rAF ค้าง
       * ของเดิมก่อนแยกไฟล์มี error มากกว่า (27 ครั้ง ต่อการทดสอบชุดเดียวกัน)
         ยืนยันว่าเป็นบั๊กเดิม ไม่ได้เกิดจากการแยกไฟล์

---

## บันทึกการแก้ไข — งานเสริมความปลอดภัย (Security Hardening)
    ปัญหาเดิม: login เป็นแค่ฉากบังตา ข้อมูลจริงดึงตรงจาก Apps Script ได้
               โดยไม่ต้อง login (ไม่มีการตรวจสิทธิ์ฝั่ง server)

    สิ่งที่แก้ในไฟล์นี้ (index.html):
     1) เพิ่ม window.getIdToken() ในส่วน Firebase module
        → ดึง Firebase ID token ของ user ปัจจุบันมาใช้ยืนยันตัวตน
     2) แก้ fetchCSV() ให้ส่งข้อมูลแบบ POST + แนบ token ใน body
        (เดิมเป็น GET แปะ token ที่ URL — token หายตอน Apps Script
         redirect 302 จึงต้องย้ายมาไว้ใน body)

    สิ่งที่แก้นอกไฟล์นี้ (ฝั่ง Google Apps Script ทั้ง 3 สาขา SS/BG/BN):
     - เพิ่ม verifyToken() ตรวจ token กับ Firebase ก่อนส่งข้อมูล
       ถ้าไม่มี/ไม่ถูกต้อง → ตอบ "Unauthorized"
     - เปลี่ยนเป็น doPost (อ่าน token จาก body), doGet ตอบ Unauthorized
     - เปลี่ยนวิธีอ่านข้อมูลเป็น SpreadsheetApp.openById() อ่าน sheet ตรง
       (เลิกใช้ publish-to-web /pub URL ที่เปิดสาธารณะ)
     - แปลงคอลัมน์วันที่กลับเป็น dd/MM/yyyy ก่อนส่ง (Utilities.formatDate)
       เพื่อให้ parseD() ฝั่งเว็บอ่านได้เหมือนเดิม

    หมายเหตุ: Firebase apiKey ที่อยู่ในโค้ดเป็นค่าสาธารณะปกติของ Firebase
              ไม่ใช่ความลับ การป้องกันจริงอยู่ที่ Auth + Firestore rules

## ✅ เสร็จแล้ว v2.5.2 (SEC-05) — ปรับปรุงความเร็ว/ลดอาการโหลดรวน:
     - loadAll() ยิง 6 ท่อพร้อมกันด้วย Promise.all แล้ว (เดิม await ต่อกันทีละสาขา)
     - ดึง token ครั้งเดียวใช้ซ้ำทุกท่อแล้ว (refreshToken()/cachedToken)
     - fetchCSV retry 1 ครั้งถ้าว่าง/error กัน SS=0 ตอน cold start
     - Apps Script: verifyToken cache ผลด้วย CacheService (เก็บ 5 นาที)

---

## v2.6.4 (28 ก.ค. 2569) — จัดหน้าภาพรวมใหม่ กราฟไม่โดนดันตกจอ
     UI-04) บล็อกเป้า (แบนเนอร์เตือน + กล่องเป้าเดือน + แถบสรุปเป้า ~375px)
        ถูกวางคั่นกลางระหว่าง KPI "รายรับ" กับ KPI "สถิติการจอง"
        ผล: การ์ด KPI 2 ชุดขาดจากกัน + หัวข้อกราฟรายวันถูกดันไปอยู่ขอบล่างจอ
            ต้องเลื่อนทุกครั้งถึงจะเห็นกราฟ
        แก้: (1) ย้ายทั้งบล็อกลงไปไว้ใต้ statsSection (ก่อนกราฟ) การ์ดต่อกันแล้ว
             (2) ห่อด้วย <details id="targetPanel"> พับเก็บได้ เหลือแถบ 1 บรรทัด
             (3) renderTargetStatus เขียนสรุปลง #targetSummaryText
                 = streak + เฉลี่ย/เป้า + % เป้าเดือน (ค่าเดิมทั้งหมด ไม่คิดใหม่)
        เกณฑ์สีคงเดิม: แดง >= 3 วันติด / ส้ม 1-2 วัน / เขียววันล่าสุดถึงเป้า
        ยังซ่อนทั้งก้อนเมื่อ group !== 'all' ตามข้อจำกัดที่ตั้งใจไว้ใน ENH-05

---

## v2.6.3 (28 ก.ค. 2569) — เปิดเว็บเห็นข้อมูลทันที ไม่ต้องรอ Apps Script อีก
     CACHE-01) ปัญหาที่แก้จริง: ทุกวิธีก่อนหน้า (retry / timeout / คุมจำนวนท่อ) ยัง
        "ให้ผู้ใช้นั่งรอ Apps Script" อยู่ ต่างกันแค่รอยังไง. Apps Script Web App
        เป็นบริการฟรีที่สุ่มค้าง 20-45 วิ เป็นระยะ - ปรับพารามิเตอร์ไม่เปลี่ยนข้อนี้
        แก้: เก็บ CSV ดิบ 6 ก้อนไว้ใน localStorage (~300 KB = 6% ของโควตา)
          เปิดเว็บ -> โชว์ข้อมูลรอบก่อน "ทันที 0 วินาที" + ป้าย [ข้อมูล HH:MM - กำลังอัปเดต...]
          -> ดึงของใหม่เบื้องหลัง -> สำเร็จ: อัปเดตเงียบๆ ป้ายเป็น [อัปเดต HH:MM]
                                  -> ล้มเหลว: ใช้ของเก่าต่อ ป้ายเป็น [- อัปเดตไม่สำเร็จ]
        * บันทึก cache เฉพาะตอนได้ครบทั้ง 6 ท่อ (ไม่ cache ข้อมูลไม่ครบไว้หลอกตัวเอง)
        * cache เก่าเกิน 7 วันไม่ใช้
        * ถ้าโชว์ cache อยู่แล้ว error จะไม่เอาหน้า "โหลดไม่ได้" ไปทับ - ใช้งานต่อได้
        * แยก applyRowsBundle() ให้ทั้ง "โหลดสด" กับ "อ่าน cache" ใช้ทางเดียวกัน
     * ยังไม่ได้แก้ที่ราก - Apps Script ยังสุ่มค้างเหมือนเดิม แค่ไม่มีใครต้องรอมันแล้ว
       งานถัดไปที่แก้ราก: ย้ายไป Firestore (Apps Script ทำงานตาม time trigger แทน)

---

## v2.6.2 (28 ก.ค. 2569) — ความทนทาน + ความเร็วตอนโหลด (โหลด 71.7 วิ -> 9.3 วิ)
     [รวมงานทั้งวันของ 28 ก.ค. ไว้เป็นเวอร์ชันเดียว]

     ปัญหาที่แก้: เปิดเว็บแล้วรอนาน 1-2 นาที บางสาขาได้ 0 แถว แต่หน้าจอไม่บอกอะไร
     โชว์เลข 0 เหมือนเป็นยอดจริง (อันตรายกว่าเว็บพัง)

     -- สาเหตุจริง (พิสูจน์ด้วยตัวเลขจาก PERF-01) --
     ยิง 6 ท่อพร้อมกันเกินลิมิต concurrency ฝั่ง Google -> คลื่นแรกพังเกือบหมด
     หลังรอ 18-45 วิ (404 / Unauthorized / timeout) แล้ว retry ทีหลังผ่านหมดใน 2-3 วิ
     เวลา 71.7 วิ เกือบทั้งหมดเป็น retry + backoff ไม่ใช่งานจริง
     "ใครพัง" เป็นเรื่องสุ่ม - ไม่ใช่ปัญหาของสาขาใดสาขาหนึ่ง
     ต้นตอย้อนไปถึง v2.5.2 (SEC-05) ที่เปลี่ยนจากยิงทีละท่อเป็น Promise.all 6 ท่อ
     v2.6.0 ไม่ได้แตะโค้ดเครือข่ายเลย - แค่อาการหนักพอจนสังเกตเห็น

     PERF-03) fetchPool() คุมให้ยิงพร้อมกันไม่เกิน FETCH_CONCURRENCY (=2) ท่อ
        + เรียงงานสลับสาขา (ST 3 สาขาก่อน แล้ว NON/CAR) ให้ 2 ท่อที่วิ่งพร้อมกัน
          เป็นคนละ Apps Script เสมอ
        + refreshBranch (2 ท่อเข้า script เดียวกัน) บังคับ limit=1 ยิงทีละท่อ
     PERF-04) FETCH_TIMEOUT 45 -> 12 วิ, FETCH_BACKOFF 2 -> 1 วิ
        ท่อที่สำเร็จใช้ 2.2-7.5 วิ ไม่มีเกิน 8 วิ -> เกิน 12 วิ แทบไม่มีทางสำเร็จ
        ตัดแล้ว retry เร็วๆ คุ้มกว่ารอ (ท่อค้าง: ~48 วิ -> ~15 วิ)
     PERF-01) ตัววัดเวลา: fetchCSV log "X.X วิ" ต่อท่อ + logLoadTimes() สรุปท้ายรอบ
        (เวลาอุ่น token, ทุกท่อเรียงช้า->เร็ว, จำนวน retry, เวลารวม, ท่อช้าสุด)
     PERF-05) เกณฑ์เตือนใน logLoadTimes เทียบกับ "ผลรวมทุกท่อ / FETCH_CONCURRENCY"
        (ของเดิมเทียบกับท่อช้าสุด x1.8 ซึ่งใช้ได้เฉพาะตอนยิงขนานทั้งหมด -> เตือนผิดทุกครั้ง)

     NET-01) fetchCSV ไม่มี timeout -> ถ้า Apps Script ค้าง Promise.all ไม่ settle
        ตลอดกาล splash ค้างไม่มีวันหาย. ใส่ AbortController + retry 2 ครั้ง backoff
     NET-03) แบนเนอร์แดง #dataHealth (renderDataHealth) เตือนเมื่อสาขาไหนโหลดไม่ครบ
        เดิมเขียนแค่ console.warn ผู้ใช้ไม่มีทางรู้ว่าเลขไม่ครบ
     NET-04) catch ใน loadAll ไม่ได้ซ่อน splash -> error ถูก splash (z-index 9999)
        บังหมด เห็นเป็นหน้าโหลดค้าง. แยก hideSplash() เรียกทั้ง 2 ทาง
     CHT-01) Uncaught TypeError getContext ตอนข้อมูลว่าง 0 แถว -> guard เดิมเช็ค
        แค่ "มี element ไหม" เพิ่มเช็ค getContext + isConnected + ห่อ new Chart
        และ destroy ด้วย try/catch ไม่ให้กราฟตัวเดียวล้มทั้งหน้า
     SEC-07) [Apps Script ทั้ง 3 สาขา - deploy แล้ว] verifyToken: retry 2 ครั้งเมื่อ
        Firebase ตอบ != 200 + ยืด cache 300 -> 3000 วิ (เดิมชนกับ auto-refresh 5 นาที)

     * เคยลอง "ยิงทีละท่อในสาขาเดียวกัน" (NET-02) เพราะเชื่อว่า ST+NON ชนกันคือต้นเหตุ
       -> พิสูจน์แล้วว่าไม่ใช่ + ทำให้เวลาโหลดคูณสอง จึงถอนออก แทนด้วย PERF-03

---

## v2.6.0 (ก.ค. 2569) — ENH-05 เทียบเป้า vs ยอดจริง + เตือนเมื่อหลุดเป้า
     1) เป้าแยกสาขา x โซน (ST / Car-Non) เก็บใน localStorage
        key 'saveone_targets_v1' — sidebar มี 2 ช่อง เปลี่ยนตามสาขาที่เลือก
     2) แบนเนอร์เตือนหน้าภาพรวม: หลุดเป้าติดกันครบ 3 วัน = แดง,
        1-2 วัน = ส้ม, วันล่าสุดถึงเป้า = เขียว
     3) กล่อง "เป้าเดือนนี้" — แถบความคืบหน้า + เส้นเพซที่ควรเป็น
        + คาดการณ์ยอดสิ้นเดือน + ต้องทำกี่ล็อก/วันในวันที่เหลือ
     4) รายการ 5 วันล่าสุดเทียบเป้า (เขียว/แดง)
     * แสดงเฉพาะตอน group = ทุกกลุ่ม (เป้าเทียบยอดรวม ไม่ใช่กลุ่มย่อย)

---

## v2.5.8 (ก.ค. 2569) — Benchmark: กราฟเทรนด์ + ป้ายเตือนสาขาไหลลง
     BM-01) แท็บ Benchmark กราฟ "ล็อกเฉลี่ย/วัน แต่ละสาขา" เป็นกราฟแท่ง อ่านเป็น
        การเทียบ ไม่เห็นทิศทาง -> เปลี่ยนเป็น "เทรนด์ล็อกเฉลี่ย/วัน รายเดือน"
        แบบกราฟเส้น + คำนวณผ่าน bmMonthlyAvg() ที่ "ตัดวันที่ล็อก = 0 ออกก่อนเฉลี่ย"
        (วันปิด/ไม่มีข้อมูล เดิมถูกนับเป็นวันหนึ่ง ทำให้ค่าเฉลี่ยต่ำกว่าจริง)
        เดือนที่มีข้อมูล < BM_MIN_DAYS (7) วาดเป็นจุดกลวง = ยังไม่ครบเดือน
     BM-02) เพิ่มป้ายเตือนอัตโนมัติเหนือกราฟ (#bmAlert): bmTrend() นับเดือนที่ตก
        ต่อเนื่องนับถอยหลังจากเดือนล่าสุด -> >= BM_ALERT_N (3) = 🔴 "ไหลลงไม่หยุด",
        2 เดือน = 🟡 เฝ้าระวัง, 1 เดือน = 🟡 ยังไม่นับเป็นเทรนด์, 0 = 🟢
        พร้อมบอกค่าเฉลี่ยเดือนล่าสุด + % ต่ำกว่าจุดพีค
        (Benchmark ยังไม่ใส่ filter zone/group ตามดีไซน์เดิม)

---

## v2.5.7 (ก.ค. 2569) — พยากรณ์อิงเดือนปฏิทิน (ตัด "30 วันข้างหน้า")
     FCST-02) หน้าพยากรณ์: เดิมมีบล็อก "พยากรณ์เดือนหน้า (ฉาย 30 วันข้างหน้า)"
        ซึ่งซ้ำซ้อนกับ "คาดทั้งเดือน" + คร่อม 2 เดือน เทียบยอดจริงรายเดือนไม่ได้
        -> เปลี่ยนเป็นการ์ดคู่อิงเดือนปฏิทินทั้งคู่:
          • "📅 เดือนนี้ (คาดทั้งเดือน)"  = ล็อกสะสม / เพซเฉลี่ย/วัน / คาดทั้งเดือน
          • "🔮 เดือนหน้า (เต็มเดือนปฏิทิน)" = เฉลี่ย/วัน × จำนวนวันจริงของเดือนหน้า
     FCST-03) บล็อก "เดือนนี้" เดิมโผล่เฉพาะตอนเดือนยังไม่จบ -> วันสุดท้ายของเดือน
        การ์ดคู่เหลือข้างเดียว. แก้: แสดงตลอดถ้ามีข้อมูลเดือนปัจจุบัน
        (จบแล้ว = โชว์ยอดจริง ไม่มีป้ายเตือน/แถบความคืบหน้า/การ์ดคาด)
     FCST-04) เติมการ์ดรายรับฝั่ง "เดือนนี้" (เฉลี่ย/วัน + คาดทั้งเดือน/ยอดจริง)
        ให้สมมาตรกับฝั่ง "เดือนหน้า" — gate ตาม role เหมือนจุดอื่น
        (What If เก็บไว้เหมือนเดิม)

---

## v2.5.6 (ก.ค. 2569) — แก้บั๊ก 4 จุด + จัดหน้าพยากรณ์ใหม่
     1) กำหนดเองเลือกวันที่: parse เป็นเวลา local (เดิม new Date("YYYY-MM-DD")
        เป็น UTC ทำให้วันแรกหลุด ข้อมูลเลื่อน +1 วัน)
     2) สรุปตัวเลขสำคัญ (หน้าเปรียบเทียบ): "เฉลี่ยรายรับ" ใช้ fmtN ใส่ลูกน้ำ
        (เดิม toFixed(0) ไม่มี comma)
     3) ปฏิทินล็อก heatmap: แต่ละเดือนห่อ .hm-month + #hmWrap เป็น flex-wrap
        เรียงแนวนอนไหลไปทางขวาแล้ววนลงบรรทัดใหม่ (เดิมเรียงแนวตั้ง),
        .hm-grid ใช้ width:340px คงที่ cell ขนาดเท่าเดิม
     4) พยากรณ์: แยกเดือนปัจจุบันที่ยังไม่จบออกจากค่าเฉลี่ย 3 เดือน แล้ว
        จัดหน้าใหม่เป็น 2 กลุ่มชัดเจน:
        • "📅 เดือนนี้ (กำลังดำเนินอยู่)" = ล็อกสะสม / เพซเฉลี่ย/วันตอนนี้
          (มีลูกศร ▲▼ เทียบค่าเฉลี่ย 3 เดือน เขียว=เร็วกว่า แดง=ช้ากว่า) /
          คาดทั้งเดือน + แถบความคืบหน้า + ป้ายเตือน "ยังไม่จบเดือน"
        • "🔮 พยากรณ์เดือนหน้า" = เฉลี่ย 3 เดือนเต็ม / พยากรณ์ 30 วัน / รายรับ

---

## v2.5.5 (ก.ค. 2569) — ปุ่ม "ทั้งหมด" เลือกทุกเดือนทีเดียว
     - เพิ่มปุ่ม "ทั้งหมด" หน้าปุ่มเดือน (กด = เลือกครบ, กดซ้ำ = ล้าง)

---

## v2.5.4 (ก.ค. 2569) — หน้าเปรียบเทียบเดือน + สีกราฟโหมดสว่าง
     1) กราฟ tick/legend/เส้นกริด อ่านสีจาก chartClr() ปรับตาม light/dark
        ทุกหน้า -> โหมดสว่างเส้นกริด/ตัวหนังสือไม่จมหายอีก
     2) ย้ายแท็บ "เปรียบเทียบเดือน" มาเป็นอันที่ 2 ติด "ภาพรวม"
     3) เลย์เอาต์ใหม่: กราฟแท่งล็อกรวมรายเดือนเป็นกราฟหลัก (บน) + ตัวเลข
        บนแท่ง + ป้าย "(ยังไม่จบเดือน)" เดือนล่าสุด, การ์ด MoM ใหญ่ขึ้น,
        เพิ่มกล่อง "สรุปตัวเลขสำคัญ" + เฉลี่ยรายรับ/วัน, ย้ายกราฟเส้น
        รายวันลงล่างสุด

---

## v2.5.3 (ก.ค. 2569) — 🎨 แก้โลโก้/หน้าโหลด (splash) โหมดสว่าง
     1) ล็อกหน้าโหลด (.splash) ให้พื้นมืดเสมอ ไม่เปลี่ยนตาม light mode
        → โลโก้รถสีขาวจะได้ไม่จมหาย (หน้า login ยังตามโหมดปกติ)
     2) เอา light-mode override ของ .splash-logo-savewan/.splash-company/
        #loginPage ที่ดันให้เป็นสีสว่างออก, คืน .login-logo-text ให้เข้ม
     3) รูปโลโก้อ้าง src="logo.png" (path สัมพัทธ์) — เวลา deploy ต้องวาง
        logo.png โฟลเดอร์เดียวกับ index.html ชื่อพิมพ์เล็กตรงกัน ไม่งั้น 404
